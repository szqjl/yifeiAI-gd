# 实施计划 - 自我学习型掼蛋AI (V5)

## 目标描述
将掼蛋AI从机械的基于规则的系统 (V4) 转型为**自我学习强化学习 (RL) 系统 (V5)**。新的AI将通过自我博弈自主学习策略，从基本规则开始，随着时间的推移不断改进，模仿人类的学习过程。

## 用户审查要求
> [!IMPORTANT]
> **重大架构变更**: 这需要构建一个独立的“游戏引擎”来在本地模拟游戏以进行训练。现有的代码主要是依赖于服务器的客户端。
> **训练时间**: RL模型需要大量时间进行训练。我们将从一个基础版本开始以验证概念。
> **依赖项**: 将在 `requirements.txt` 中添加 `torch` (PyTorch) 和 `gymnasium` (Gym)。

## 提议的变更

### 1. 基础设施层 ("健身房")
创建一个本地游戏环境，模拟掼蛋规则，无需网络服务器。

#### [NEW] `src/rl_env/`
- `game_engine.py`: 一个纯逻辑类，用于管理游戏状态、回合和规则验证。
    - 复用 `src/game_logic/hand_combiner.py` 中的牌型识别。
    - 实现 `is_greater(action1, action2)` 逻辑。
    - 实现 `get_legal_actions(state)` 逻辑。
- `guandan_env.py`: 一个兼容 Gymnasium 的环境包装器。
    - `reset()`: 开始新游戏。
    - `step(action)`: 执行动作，返回 (next_state, reward, done, info)。
    - `observation_space`: 编码的游戏状态 (手牌 + 牌桌 + 历史)。
    - `action_space`: 离散动作空间 (所有可能的出牌组合)。

### 2. 代理层 ("大脑")
实现一个深度强化学习代理。

#### [NEW] `src/rl_agent/`
- `model.py`: 神经网络定义 (例如 ResNet 或 MLP)。
    - 输入: 状态向量 (我的手牌, 队友手牌概率, 已出牌)。
    - 输出: 动作概率 / Q值。
- `agent.py`: RL 代理类 (例如 DQN 或 PPO)。
    - `act(state)`: 根据策略选择动作。
    - `learn(batch)`: 更新神经网络权重。

### 3. 训练层 ("道场")
一个让AI与自己对战并学习的系统。

#### [NEW] `src/train/`
- `self_play.py`: 运行两个版本代理之间对战的脚本。
- `trainer.py`: 管理训练循环、回放缓冲区和模型保存。

### 4. 集成层
允许主客户端使用训练好的模型。

#### [MODIFY] `src/decision/`
- `rl_decision_engine.py`: 一个新的决策引擎，加载训练好的 `.pth` 模型并推断动作。

## 验证计划

### 自动化测试
- **游戏引擎测试**: 验证 `game_engine.py` 是否正确执行规则 (例如，不能用小牌管大牌)。
    - `python tests/test_game_engine.py`
- **环境测试**: 验证 Gym 接口兼容性。
    - `python tests/test_guandan_env.py`
- **集成测试**: 验证 RL 代理是否能在模拟游戏中做出有效动作。
    - `python tests/test_rl_integration.py`

### 手动验证
- **训练运行**: 执行 `python src/train/trainer.py` 并观察损失下降和胜率相对于随机代理的提升。
- **客户端测试**: 在本地运行带有 `rl_decision_engine` 的标准客户端，对抗现有的 `hybrid_decision_engine`。
