# 实施计划 - 自我学习型掼蛋AI (V5)

## 目标描述
将掼蛋AI从机械的基于规则的系统 (V4) 转型为**自我学习强化学习 (RL) 系统 (V5)**。新的AI将通过自我博弈自主学习策略，模仿人类的学习过程：从基本规则开始，通过实战积累经验，并通过研读“棋谱”（策略指南）和“复盘”（专家对局）来提炼技巧。

## 用户审查要求
> [!IMPORTANT]
> **范式转变**: 我们正在从“编写逻辑”转向“训练模型”。AI的质量将取决于**训练时间**和**数据质量**，而不仅仅是代码的正确性。
> **资源密集**: 训练需要大量的CPU/GPU时间。
> **新依赖项**: 需要引入 `torch`, `gymnasium`, `numpy`。

## 提议的架构

### 1. 基础设施层 (演武场 - The Playground)
一个高速的本地掼蛋游戏模拟环境，支持数百万次的训练步数。

#### [NEW] `src/rl_env/`
- **`game_engine.py`**: 纯逻辑、高性能的游戏状态机。
    - *特性*: 状态管理、规则验证、胜负判定。
    - *优化*: 必须比当前用于WebSocket客户端的 `game_logic` 快得多。
- **`guandan_env.py`**: 兼容 `gymnasium.Env` 的环境包装器。
    - `step(action)`: 返回 `(observation, reward, terminated, truncated, info)`。
    - `reward_function`: 核心组件，结合了：
        - **对局结果**: 胜利+100，失败-100。
        - **策略塑造**: 遵循“良好习惯”（由知识模块定义）时给予小额奖励。

### 2. 代理层 (大脑 - The Brain)
感知游戏状态并决定动作的神经网络。

#### [NEW] `src/rl_agent/`
- **`model.py`**: 神经网络架构。
    - *输入*: 手牌、桌牌、历史记录和队友状态的矩阵表示。
    - *输出*: 所有合法动作的概率分布。
- **`agent.py`**: RL算法 (例如 PPO - 近端策略优化)。
    - 支持 `act()` 进行推理，`learn()` 进行训练。

### 3. 知识整合层 (导师 - The Teacher)
这是本方案的核心差异点。它允许AI从人类知识中学习，而不仅仅是随机试错。

#### [NEW] `src/knowledge_processor/`
- **`replay_parser.py`**: 将人类对局日志 (`.json`) 转换为**行为克隆 (BC)** 的训练数据集。这让AI可以通过模仿专家来获得“先发优势”。
- **`strategy_encoder.py`**: 将文本规则（例如“逢五逢十不打对”）转化为**奖励塑造函数**。
    - *示例*: 如果AI在后期能赢的情况下提前打出了同花顺，它会收到一个小惩罚，引导它遵循文本策略。

### 4. 自我反思层 (评论家 - The Critic)
模仿人类在赛后复盘自己的对局。

#### [NEW] `src/analysis/`
- **`battle_analyst.py`**: 在每个训练回合后运行。
    - 检查“失误”（大幅降低胜率的动作）。
    - 更新“错题本”数据库，以便在后续训练中重点针对这些场景。

### 5. 训练层 (道场 - The Dojo)
发生学习的循环过程。

#### [NEW] `src/train/`
- **`pretrain.py`**: 监督学习阶段，使用 `replay_parser` 数据（模仿人类）。
- **`self_play.py`**: RL阶段，AI与自己（或自己的旧版本）对战。
- **`curriculum.py`**: 管理难度。从“简单规则”开始，逐渐过渡到“复杂策略”。

## 实施步骤

### 第一阶段：基础 (演武场)
- [ ] 实现 `game_engine.py` (快速逻辑)。
- [ ] 实现 `guandan_env.py` (Gym接口)。
- [ ] 使用随机代理进行验证。

### 第二阶段：模仿 (学生)
- [ ] 实现 `replay_parser.py` 以摄取现有的游戏日志。
- [ ] 构建 `model.py` (神经网络)。
- [ ] 运行 `pretrain.py` 以克隆 V4/人类日志中的行为。

### 第三阶段：进化 (斗士)
- [ ] 实现 `agent.py` (PPO/DQN)。
- [ ] 运行 `self_play.py` 以超越模仿基准。
- [ ] 集成 `strategy_encoder.py` 以奖励文本指南中定义的“聪明”打法。

### 第四阶段：整合 (参赛者)
- [ ] 创建 `rl_decision_engine.py` 将训练好的模型接入主游戏客户端。
- [ ] 部署到比赛平台。

## 验证计划

### 自动化测试
- **逻辑正确性**: 确保 `game_engine` 完美匹配官方规则。
- **张量形状**: 验证神经网络的输入/输出与环境匹配。

### 性能指标
- **胜率**: 对抗随机代理 (目标: >95%)，对抗基于规则的V4 (目标: >55%)。
- **合法动作率**: 神经网络输出合法动作的百分比 (通过掩码机制目标: 100%)。
