# 工作流自动重启系统使用指南

## ✅ 系统已启动

自动重启系统已启动，将自动：
1. 检测工作流状态
2. 分析训练结果
3. 根据问题自动改进训练代码
4. 重启工作流
5. 循环直到达成目标（M1胜率 > 50%）

## 🔍 当前问题分析

### 工作流状态
- **状态**: completed（已完成）
- **迭代次数**: 10/10
- **胜率**: 0.00%
- **成功**: 否

### 训练问题
1. **模型预测了所有512张卡牌**
   - 阈值设置可能无效
   - 需要进一步降低阈值范围

2. **预测比例过高（355.37倍）**
   - 需要增加过度预测惩罚

3. **损失值仍然较高（191,825.22）**
   - 可能需要调整学习率

4. **所有迭代评估失败**
   - 需要检查评估器

## 🔧 自动改进策略

系统将根据问题自动改进：

### 1. 阈值问题
- **当前**: `threshold = clamp(base_threshold * 0.01, 0.001, 0.1)`
- **改进**: `threshold = clamp(base_threshold * 0.001, 0.0001, 0.01)`
- **效果**: 进一步缩小阈值范围，避免预测所有卡牌

### 2. 预测比例过高
- **当前**: `over_prediction_penalty=1000.0`
- **改进**: `over_prediction_penalty=5000.0`
- **效果**: 增加过度预测惩罚，降低预测比例

### 3. 损失值较高
- **当前**: `learning_rate=0.00005`
- **改进**: `learning_rate=0.00001`
- **效果**: 降低学习率，更稳定的训练

## 📊 监控方法

### 方法1: 查看自动重启系统状态
```bash
python monitor_workflow_progress.py
```

### 方法2: 查看工作流状态
```bash
python check_workflow_status.py
```

### 方法3: MLflow实时监控
```bash
mlflow ui --backend-store-uri file:///d:/YiFeiAI-GD/logs/mlruns
```
然后浏览器打开 http://localhost:5000

## ⏱️ 预期时间

- **每次循环**: 约30-50分钟
  - 分析：1-2分钟
  - 改进代码：1分钟
  - 工作流运行：20-40分钟
  - 等待和检查：5-10分钟

- **完整自动循环**: 根据改进效果
  - 如果每次改进有效：可能需要3-5次循环
  - 如果改进效果不佳：可能需要更多循环

## 🎯 成功标志

系统成功时会显示：
```
🎉🎉🎉 目标达成！M1已能战胜client！🎉🎉🎉
最终胜率: XX.XX%
迭代次数: X
```

## ⚠️ 注意事项

1. **不要手动修改训练代码**：系统会自动改进
2. **保持系统运行**：不要中断自动重启系统
3. **定期检查进度**：使用监控脚本查看进度
4. **查看改进历史**：代码备份保存在 `src/train/stage7_optimized_training.py.backup_*`

## 🔍 故障排查

### 如果系统停止
1. 检查是否有错误输出
2. 检查工作流状态文件
3. 手动运行 `python auto_restart_workflow.py`

### 如果改进无效
1. 查看训练历史分析结果
2. 检查代码改进是否生效
3. 可能需要手动调整参数

## 📝 改进历史

所有代码改进都会自动备份，备份文件格式：
- `src/train/stage7_optimized_training.py.backup_YYYYMMDD_HHMMSS`
