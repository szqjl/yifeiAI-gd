---
description: 掼蛋胜负目标与 context 字段（决策/策略/RL 对齐）
globs:
  - src/decision/**
  - src/m/**
  - src/v/**
  - src/game_logic/**
---

# 掼蛋胜负目标

- **规则层**：`src/game_logic/guandan_constants.py` 中 `GAME_OBJECTIVE`（每副牌争头游，己方头游+二游即获胜）。
- **主决策入口**：M1 / V5 / Stage5 注入目标到 **context** 或 **message**：
  - M1：`context['game_objective']`、`context['winning_ranks']`
  - V5/Stage5：`message['_game_objective']`
- **对齐方式**：策略与 RL 使用 `context.get('game_objective')` 或 `message.get('_game_objective')`。详见 `docs/archive/rules/胜负与目标.md`。
