---
description: 掼蛋 AI 改动前必读迭代大脑（ISSUES + ITERATIONS），禁止仅凭聊天历史假设
alwaysApply: true
---

# 掼蛋 AI 迭代上下文

本仓库掼蛋 AI 相关开发以 **`docs/guandan-brain/`** 为真源，与聊天上下文无关。

## 执行任何「修改惯蛋 AI 行为/规则/模型/训练配置」的任务前必须

1. 阅读 `docs/guandan-brain/ISSUES.md`，确认与本次改动相关的 **open** `GUA-xxx` 及标签（`rules` / `observation` / `policy`）。
2. 阅读 `docs/guandan-brain/ITERATIONS.md` 中**最新一条**（或本轮草稿），对齐本轮目标与完成定义；若本轮未登记，先追加一条再改代码。
3. 阅读 `docs/guandan-brain/EVAL.md` 中的评测入口与通过标准；改动后更新评测结果与 ITERATIONS（含回归：pass→fail）。
4. **改 M3 决策**（`src/m/m3/`、`yf1_m3`/`yf2_m3`）：读 open 且 tag=`m3` 的 GUA、`PRINCIPLES_MAPPING.md` 相关 guard；**P0 guard 只改 `m3_decision_engine`**；组牌/牌力走 **V5+**。队胜率 KPI **只看 M3 批跑**。**M1 frozen**（`GUA-022` closed）：仅 bugfix / 协议 / 记录与 pytest 回归，**勿再开 M1 策略 GUA**（见 ISSUES「引擎维护策略」）。M1 **0/12** vs M3 **7/10** 为同机对照，非口径错误。

## 解读批跑 / 平台 / lalala 对局数据前必须

阅读 [`docs/knowledge/platform-data-interpretation.md`](../knowledge/platform-data-interpretation.md) 与 [guandan-knowledge.mdc](./guandan-knowledge.mdc) §1 / [guandan-platform-v1006.mdc](./guandan-platform-v1006.mdc) §局与副。固定口径：

| 问什么 | 看什么 | 叫什么 |
|--------|--------|--------|
| 本批 **共打几局** | `completed_games`、`settingTimes` | **局**（整局 / 一次游戏） |
| **各队赢几局** | 批末 **`victoryNum[0]` vs `[1]`**（0+2 一队，1+3 一队；**禁止四席相加**） | **局**胜负（平台整局结束计胜） |
| **共打几副** | `total_rounds`、成对 match_key | **副**（平台 **小局** / `episodeOver`） |
| 牌谱 / PASS | **`game_records` 每条 JSON = 一副** | **副**，不是局 |

**定音**：**局 ⊃ 多副**；**平台 1 局 ≠ 1 副**。小局 = 副 ≠ 整局。

**批跑 `--target-games`**：须为 **3 的倍数**（exe 每会话 **3 局**）。推荐 **3 / 9 / 12**；**勿用 10**（尾批 `batch_games=1` → fallback，队胜难对账）。见 [`EVAL.md`](../docs/guandan-brain/EVAL.md)「批跑局数档位」。

**`victoryNum` 自检**（平台下发，按规则正确）：同队 **`[0]=[2]`、`[1]=[3]`**；批跑 N 局时 **`[0]+[1]` 应 = N**（无平局时）。异常如 `[1,1,1,1]`（只合 2 局）、`[1,2,1,0]`（同队不一致）→ **不可信**，查回填 / `gameResult` 原文。

**禁止**：用 `game_records` 文件数、`episodeOver` 次数当局数；用 `victoryNum` 当副数。

## 术语与平台一致（强制）

**文档与代码所用术语，必须与平台使用说明一致。** 术语双轨会在联调、批跑解读、Guard/残局分支处**静默漏判**；产品发布后再统一命名，改动面大且易引入行为回归。完整条文见 [`AGENTS.md`](../AGENTS.md)「术语与平台一致」。

**真源（按优先级）**：

1. `offline_platform/掼蛋平台使用说明书v1006.pdf`
2. `docs/guandan-brain/掼蛋AI算法对抗平台使用说明.md`
3. [guandan-platform-v1006.mdc](./guandan-platform-v1006.mdc)
4. [`platform-data-interpretation.md`](../docs/knowledge/platform-data-interpretation.md)

| 类别 | 平台标准 | 禁止混用（无注释例外时） |
|------|----------|-------------------------|
| 牌型 / `action[0]` | `Single` `Pair` `Trips` `ThreeWithTwo` `ThreePair` `TwoTrips` `Straight` `StraightFlush` `Bomb` | `bomb` `straight_flush` `three_with_two` 等 snake_case 动作类型名 |
| 特殊动作 | `PASS` `tribute` `back` | 自造动作类型字符串 |
| `stage` | `episodeOver`（一副）`gameOver`（整局结束） | 把小局叫「局」、把 `gameOver` 叫「副」 |
| 等级 | `curRank` `selfRank` `oppoRank` | 协议外别名 |
| 计数 | **局** / **副**（见上表） | 牌谱条数当局数 |

**允许的内部名（须在定义处注释「平台名 ↔ 内部名」）**：

- V7 `group_type` 子结构：`trip_in_three_with_two` / `pair_in_three_with_two` → `ThreeWithTwo`；`pair_in_three_pair` → `ThreePair`；`trip_in_steel_plate` → `TwoTrips`
- 原子组牌键 `pair` / `trips` / `straight` → 经 `GROUP_TO_ACTION` 映射到 `Pair` / `Trips` / `Straight`；对接 `actionList` 时必须走平台名
- `GroupingPlan` 字段（如 `straight_flushes`）仅为数据结构名，**不是** `action[0]`

**硬禁止**：

- 新增**从未由组牌产出、却被当作 `group_type` 消费**的幽灵键（如 `three_with_two`、`three_pair`、`two_trips`；历史教训见 ITERATIONS `v7-group-type-platform-unify`）
- 在 `action[0]`、`get_action_type` 返回值、残局 `recommended_types` / `banned_types`、ISSUES/ITERATIONS 牌型描述中混用非平台牌型名
- **无注释**地把内部 snake_case 键当作与平台 PascalCase 等价

**动手前**：新类型名 / 文档小节 / JSON 字段 → 先查真源；若保留内部名 → 代码行内注释 + 文档一句对照。**改名统一须带 pytest 回归**，禁止大规模重命名无测试兜底。

## 禁止

- 仅凭对话历史推断「上次修了什么」或「约定是什么」而不查上述文件。
- 在同一迭代内无目标地同时改多处 unrelated 逻辑（除非 ITERATIONS 明确列出并说明原因）。
- 引入与平台说明书不一致的术语且不在定义处标明映射（见上节）。

## 完成后

- 在 `ISSUES.md` 更新相关条目的状态与 `closed_in`（若关闭）。
- 在 `ITERATIONS.md` 追加一行：目标 GUA、改动摘要、评测摘要、下轮 priority。
