# 进军线上：掼蛋AI扩展计划

## 目标
将自我学习型掼蛋AI (V5) 从本地训练环境扩展为具有竞争力的在线代理，能够在各种平台上与其他AI和人类玩家进行对战。

## 1. 协议适配层 (PAL)
目前，客户端 (`yf1_v4.py`) 是为南京邮电大学比赛平台硬编码的。为了在其他地方游玩，我们需要一个灵活的通信层。

### 提议的变更
*   **`src/communication/adapter.py`**: 创建一个抽象基类 `GameAdapter`。
    *   `connect(url, token)`: 连接服务器。
    *   `send_action(action)`: 发送动作。
    *   `receive_message()` -> 返回标准化的 `GameState` 对象。
*   **实现**:
    *   `NJUPTAdapter`: 现有的逻辑封装。
    *   `TencentAdapter`: (假设) 用于腾讯游戏平台。
    *   `BrowserAdapter`: 使用 Selenium/Puppeteer 在没有API的网页平台上游玩。

## 2. 拟人化行为 (图灵测试)
在与人类对战时，机械式的瞬间响应会暴露身份，并可能被标记为机器人。

### 提议的变更
*   **`src/decision/behavior_modulator.py`**:
    *   **反应时间**: 根据动作的复杂性添加可变延迟。（例如：出单张牌=快，复杂策略=慢）。
    *   **失误模拟**: 如果胜率过高，在低风险情况下偶尔犯一些“人类”错误（以隐藏身份）。
    *   **聊天/表情**: 基本的交互能力（例如：“打得好”，“漂亮”）。

## 3. 在线学习管道 (持续进化)
“道场”不应止步于本地自我博弈。现实世界的对局能提供最优质的数据。

### 提议的变更
*   **云同步**:
    *   `src/data/cloud_sync.py`: 每次比赛后自动将 `game_records/*.json` 上传到中央 S3 存储桶或服务器。
*   **主动学习**:
    *   训练服务器下载这些新日志。
    *   `replay_parser.py` 提取“新奇情况”（自我博弈中未见过的状态）。
    *   模型针对这些边缘情况进行再训练，并将更新后的模型推送到客户端。

## 4. 部署与扩展
为了同时进行多场比赛（例如：多账号冲榜）。

### 提议的变更
*   **容器化**:
    *   创建 `Dockerfile` 以打包 环境 + 代理 + 客户端。
*   **编排器**:
    *   使用 `docker-compose` 或 Kubernetes 并行运行 10-100 个代理。
    *   **中央配置**: 一个监控胜率并动态更新 `config.yaml` 的仪表板。

## 路线图

### 第一阶段：抽象化
- [ ] 重构 `yf1_v4.py` 以使用 `GameAdapter`。
- [ ] 标准化 `GameState` 内部格式。

### 第二阶段：拟人化
- [ ] 实现 `behavior_modulator.py`。
- [ ] 向 `RLDecisionEngine` 添加随机延迟逻辑。

### 第三阶段：云端化
- [ ] 搭建日志服务器。
- [ ] 实现回放自动上传功能。

### 第四阶段：征服
- [ ] 部署到公共平台。
- [ ] 分析对抗人类 vs 其他AI的表现。
