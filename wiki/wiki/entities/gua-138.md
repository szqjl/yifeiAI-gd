---
type: entity-gua
title: "GUA-138 grouping_engine 推理性能优化"
sources:
  - docs/guandan-brain/issues/GUA-138-completion.md
tags:
  - gua-draft
  - pending-registration
  - lru-cache
  - performance
status: draft
related_gua:
  - GUA-061
  - GUA-137
  - GUA-139
date: 2026-07-16
---

# GUA-138 grouping_engine 推理性能优化

## 基本信息

| 字段 | 值 |
|------|-----|
| 编号 | GUA-138 |
| 标题 | grouping_engine 推理性能优化 |
| 状态 | **draft（2026-07-08 待登记）** ⚠️ |
| 优先级 | 推断 P1（性能） |
| 关联模块 | [module-grouping-engine] |

> ⚠️ **数据完整性警告**：本条目完成定义文档已写就，但 ISSUES.md 主表在 GUA-054 行附近被截断，**GUA-138 是否已正式登记到主表需核实**。

## 核心改动

### _GroupingPlanCache LRU(64)

```python
class _GroupingPlanCache:
    """LRU(64) cache: (hand, cur_rank) → GroupingPlan"""
    def __init__(self, capacity: int = 64):
        self._cache: OrderedDict = OrderedDict()
        self._capacity = capacity
    
    def get_or_compute(
        self,
        hand: List[Card],
        cur_rank: int,
        compute_fn: Callable
    ) -> GroupingPlan:
        key = (frozenset(hand), cur_rank)
        if key in self._cache:
            self._cache.move_to_end(key)
            return self._cache[key]
        plan = compute_fn(hand, cur_rank)
        self._cache[key] = copy.deepcopy(plan)  # 深拷贝防污染
        if len(self._cache) > self._capacity:
            self._cache.popitem(last=False)
        return plan
```

### 关键设计

| 设计点 | 选择 | 理由 |
|--------|------|------|
| **深拷贝** | `copy.deepcopy(plan)` | 防止下游修改污染缓存 |
| **失效机制** | `cur_rank` 变化即失效 | 不同进贡阶段牌级不同 |
| **key 设计** | `frozenset(hand)` | 牌组合的不可变表示 |
| **容量** | 64 | 覆盖典型对局的手牌变化序列 |
| **淘汰策略** | LRU | 局部性原理 |

### 性能对比

| 阶段 | 耗时 | 倍数 | 来源 |
|------|------|------|------|
| 无缓存（直调） | 3.8ms / 次 | 1x | [GUA-137] 基线 |
| **LRU 缓存命中** | **< 0.1ms / 次** | **38x ~ 100x** | 本 GUA |
| 增量计算（未来） | O(1) | 理论最优 | [GUA-139] |

## GUA-061 frozen 声明的歧义

> ⚠️ 本 GUA §6 声称「GUA-061 已 frozen」，但与 [[module-grouping-engine]] 显示仍在迭代存在矛盾。

**澄清需求**：frozen 含义需明确为以下之一：

| 选项 | 含义 | 兼容性 |
|------|------|--------|
| (a) 接口 frozen | API 签名不变 | ✅ 与 GUA-138 兼容 |
| (b) 内部允许 LRU 外部包装 | 缓存层在外 | ✅ 与 GUA-138 兼容 |
| (c) 整个模块 frozen | 含未来 GUA-139 增量计算 | ❌ 与 GUA-139 冲突 |

**建议认定**：选项 (b) — 接口 frozen，允许外部 LRU 包装层。

## 上游 / 下游

- **上游算法**：[GUA-061] grouping_engine + [GUA-137] 调用方
- **下游 / 后续**：[GUA-139] 增量计算（基于本 GUA 的 cache 基础设施）

## 相关 Wiki 页面

- [[gua-137]] — 数据源上游
- [[gua-061]] — 模块 frozen 状态
- [[gua-139]] — 后续增量计算
- [[module-grouping-engine]] — 模块详情
- [[sprint-precision-upgrade-chain]] — 升级链位置
