# 工作流监控指南

## ✅ 工作流已重新启动

**启动时间**: 2026-01-12 13:18:07  
**状态**: 运行中（running）  
**当前迭代**: 1/10  
**目标胜率**: 50.0%

## 🔧 已应用的修复

### 1. 损失函数修复
- ✅ 阈值计算：缩小到0.001-0.1范围
- ✅ 过度预测惩罚：从平方改为对数惩罚
- ✅ 稀疏性奖励：从指数改为倒数函数
- ✅ 参数调整：惩罚系数从576,650降至1000

### 2. 数据加载器修复
- ✅ 已过滤空action_cards样本
- ✅ 真实卡牌数从0.79提升到1.44

## 📊 修复效果验证

### 损失值改进
- **修复前**: 80,168,580,121.39（800亿）
- **修复后**: 4,989.92（已降至合理范围）✅

### 仍需观察
- **预测卡牌数**: 需要新训练验证（当前仍为512.00）
- **预测比例**: 需要新训练验证（当前仍为355.37倍）

## 📈 监控方法

### 方法1: 快速查看进度
```bash
python scripts/workflow/monitor_workflow_progress.py
```

### 方法2: 详细状态检查
```bash
python scripts/checks/check_workflow_status.py
```

### 方法3: MLflow实时监控
```bash
mlflow ui --backend-store-uri file:///d:/YiFeiAI-GD/logs/mlruns
```
然后浏览器打开 http://localhost:5000

### 方法4: 训练效果摘要
```bash
python view_training_summary.py
```

## 🎯 关键监控指标

1. **损失值**: 应该保持在 < 10,000
2. **预测卡牌数**: 应该从512降至 < 20
3. **预测比例**: 应该从355倍降至 < 10倍
4. **真实卡牌数**: 应该保持在1.44左右或更高
5. **胜率**: 应该逐步提升（目标50%）

## ⏱️ 预期时间

- **每次迭代**: 约20-40分钟
  - 游戏对战：5-10分钟（如果需要）
  - 模型训练：10-30分钟
  - 评估和优化：1-2分钟

- **完整工作流**: 根据迭代次数
  - 如果第1次就达标：约20-40分钟
  - 如果需要多次迭代：可能需要数小时

## 🔍 故障排查

如果工作流停止或出现问题：

1. **检查工作流状态**:
   ```bash
   python scripts/checks/check_workflow_status.py
   ```

2. **查看训练历史**:
   ```bash
   python view_training_summary.py
   ```

3. **检查日志文件**:
   - 工作流日志：`logs/m1_training_workflow.log`
   - 训练日志：查看MLflow UI

4. **检查模型文件**:
   - 模型路径：`models/bc_model_stage7_optimized.pth`
   - 训练历史：`models/bc_model_stage7_optimized_training_history.json`

## 📝 注意事项

1. **不要中断工作流**：工作流会自动优化和迭代
2. **保持MLflow UI打开**：建议在新终端运行MLflow UI
3. **定期检查进度**：使用监控脚本查看进度
4. **等待新训练完成**：当前显示的是旧训练记录，需要等待新训练开始
