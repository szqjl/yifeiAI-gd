# Cursor AI 项目规则

## Planning with Files 规则

### 必须遵守的规则

1. **执行复杂任务前必须检查规划文件**
   - 在执行任何多步骤任务（3+步骤）前，必须先检查 `docs/planning/ACTIVE_PLANS.md`
   - 如果存在相关任务规划，必须先读取对应的 `task_plan.md` 文件
   - 在执行过程中，定期读取 `task_plan.md` 以保持目标清晰

2. **任务规划文件优先级**
   - 如果用户提到"继续之前的任务"、"按照计划"等关键词，必须检查 `docs/planning/ACTIVE_PLANS.md`
   - 如果存在相关规划，优先按照规划文件执行
   - 在执行过程中，将发现记录到对应的 `notes.md` 文件

3. **规划文件更新规则**
   - 每完成一个阶段，必须更新 `task_plan.md` 中的复选框和状态
   - 重要发现必须记录到 `notes.md`
   - 错误和解决方案必须记录到 `notes.md` 的错误记录部分

4. **文件读取时机**
   - 任务开始时：读取 `task_plan.md` 了解目标和阶段
   - 每个阶段开始前：重新读取 `task_plan.md` 确认当前状态
   - 遇到问题时：读取 `notes.md` 查看是否有相关记录
   - 完成阶段后：更新 `task_plan.md` 和 `notes.md`

### 规划文件结构

```
docs/planning/
├── ACTIVE_PLANS.md          # 活跃任务索引（必须检查）
├── README.md                # 使用说明
├── 使用指南.md              # 详细指南
└── [任务名称]/
    ├── task_plan.md         # 任务计划（必须读取）
    ├── notes.md             # 研究笔记（必须更新）
    └── deliverable.md       # 最终输出（完成后创建）
```

### 掼蛋 AI 胜负目标（决策/策略/RL 必读）

- **规则层**：`src/game_logic/guandan_constants.py` 中定义 `GAME_OBJECTIVE`（每副牌争头游，己方头游+二游即获胜）。
- **主决策入口**：M1 / V5 / Stage5 会注入目标到 **context** 或 **message**：
  - M1：`context['game_objective']`、`context['winning_ranks']`。
  - V5/Stage5：`message['_game_objective']`。
- **目标对齐**：策略、规则与 RL 应使用 `context.get('game_objective')` 或 `message.get('_game_objective')` 做优先级、奖励设计等对齐。详见 `docs/rules/胜负与目标.md`。

### 关键词触发

当用户使用以下关键词时，必须检查规划文件：
- "继续之前的任务"
- "按照计划执行"
- "查看任务规划"
- "继续 [任务名称]"
- "按照 task_plan"
- "查看 notes.md"

---

**创建时间**: 2026-01-07
**版本**: v1.0
