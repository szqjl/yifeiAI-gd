# 服务器端启动方式和等级重置逻辑分析

## 1. 服务器启动方式

根据文档 `掼蛋平台使用说明书1006.md`：

### Windows启动方式：
```bash
guandan_offline_v1006.exe 10
```
- 参数 `10` 表示**游戏次数**
- 游戏次数的含义：**一方从2打到A，并且双下且当前等级为我方等级**
- 这意味着服务器会连续进行多局游戏，直到达到指定次数

### 关键发现：
1. **游戏次数参数不是初始等级设置**
   - 参数10表示要进行10轮完整的游戏（从2打到A）
   - 不是设置初始等级为10

2. **多局游戏连续进行**
   - 服务器会连续进行多局游戏
   - 等级会在多局之间**累积**，不会重置

## 2. 等级重置逻辑

### 根据文档分析：

#### 正常情况：
- **游戏开始时**：双方等级应该都是 `'2'`
- **每局游戏**：从2级开始，逐步升级到A级

#### 异常情况（你遇到的问题）：
- **对方等级7，当前等级7**：说明这不是第一局游戏
- **可能原因**：
  1. 服务器之前已经运行了多局游戏，对方已经升到了7级
  2. 服务器没有在每局开始时重置等级
  3. 服务器支持"等级差测试模式"（但文档中未提及）

### 文档中的等级重置规则：

从文档第180-187行：
```
根据规则，当有一方处于A等级，经过2小局游戏后仍未获得游戏胜利，则自动降为2等级，继续从2等级打至A等级。
```

**离线平台的特殊处理**：
```
离线平台:任意一方从A降级为2，记为一次，当这次数达到50次时，该次游戏记为平局
```

这说明：
- 服务器**不会自动重置等级**
- 等级会在多局游戏中**累积**
- 只有当达到A级且2小局未获胜时，才会降级

## 3. 检查建议

### 方法1：检查服务器是否是新启动的
```bash
# 检查服务器进程
Get-Process | Where-Object {$_.ProcessName -like "*guandan*"}
```

### 方法2：检查游戏记录中的等级变化
- 查看最近几局游戏的等级设置
- 如果等级不是从2开始，说明是连续游戏

### 方法3：重新启动服务器
- 关闭当前服务器进程
- 重新运行 `guandan_offline_v1006.exe 10`
- 第一局游戏应该双方都是2级

## 4. 代码中的等级信息获取

### beginning阶段：
- 服务器**可能不发送**等级信息
- 代码中从 `data.get("selfRank")` 获取，但可能为 `None`

### play阶段：
- 服务器**应该发送**等级信息
- 在 `context` 中包含 `selfRank`, `oppoRank`, `curRank`

### 建议修改：
在 `beginning` 阶段，如果服务器没有发送等级信息，应该在第一个 `play` 消息中获取并更新 `game_info`。

## 5. 结论

**你遇到的"对方等级7"问题**：
1. **最可能的原因**：服务器已经运行了多局游戏，对方在前几局中升到了7级
2. **解决方案**：重新启动服务器，确保从第一局开始
3. **验证方法**：检查第一局游戏的等级是否都是2

**服务器端没有等级重置逻辑**：
- 服务器设计为连续多局游戏
- 等级会在多局之间累积
- 只有降级机制，没有重置机制

