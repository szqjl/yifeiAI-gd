# 开始训练指南

## ✅ 数据状态

当前训练数据已准备好：
- **训练样本**: 13,409个
- **对局文件**: 1,314个
- **数据格式**: 已转换完成

**无需重新转换数据，可以直接开始训练！**

---

## 🚀 开始训练

### 方法1：使用GUI（推荐）

1. **打开训练GUI**：
   ```bash
   run_training_gui.bat
   ```

2. **确认训练参数**（已自动设置为优化值）：
   - 训练轮数：**50**（已优化）
   - 批次大小：**64**（已优化）
   - 学习率：**0.0003**（已优化）
   - 预测阈值：**0.3**（代码中已优化）

3. **选择训练类型**：
   - 选择"预训练（Behavior Cloning）"

4. **点击"开始训练"**

5. **观察训练进度**：
   - 进度条会实时更新
   - 日志窗口显示训练过程
   - 每个epoch显示损失值和学习率

### 方法2：命令行训练

```bash
cd D:/YiFeiAI-GD
python src/train/pretrain.py
```

或指定参数：

```python
from src.train.pretrain import train_bc

train_bc(
    data_dir="game_records",
    epochs=50,          # 优化后的轮数
    batch_size=64,      # 优化后的批次大小
    lr=0.0003,          # 优化后的学习率
    model_path="models/bc_model_v1.pth"
)
```

---

## 📊 训练过程

### 预期训练日志

```
Starting Behavior Cloning Pre-training...
Data directory: game_records
Epochs: 50, Batch size: 64, Learning rate: 0.0003
Loaded 13409 samples.
Using device: cpu
Model: input_dim=512, output_dim=512 (matching inference code)
Epoch 1/50, Loss: 0.6802, LR: 0.000300
Epoch 2/50, Loss: 0.6046, LR: 0.000300
...
Epoch 10/50, Loss: 0.0292, LR: 0.000300
Epoch 11/50, Loss: 0.0280, LR: 0.000150  # 学习率衰减
...
Epoch 50/50, Loss: 0.0200, LR: 0.000019
Model saved to models/bc_model_v1.pth
```

### 训练时间估算

- **数据量**: 13,409个样本
- **批次大小**: 64
- **轮数**: 50
- **预计时间**: 约10-20分钟（CPU）或2-5分钟（GPU）

---

## ⚠️ 重要提示

### 1. 模型架构变化

由于添加了Dropout层，**旧模型需要重新训练**：
- ✅ 新模型架构：包含Dropout（防止过拟合）
- ❌ 旧模型：不包含Dropout，与新架构不兼容

### 2. 训练参数已优化

所有参数已自动设置为优化值：
- 训练轮数：50（从20增加）
- 学习率：0.0003（从0.0005降低）
- 批次大小：64（从32增加）
- 预测阈值：0.3（从0.4降低）
- 学习率衰减：每10轮衰减50%
- Dropout：0.2比率

### 3. 训练完成后验证

训练完成后，运行评估：

```bash
python src/train/evaluate_model.py
```

或运行完整分析：

```bash
python src/train/analyze_training_effectiveness.py
```

---

## 📈 预期效果

基于优化方案，预期改进：

| 指标 | 当前 | 预期 | 提升 |
|------|------|------|------|
| **完全匹配准确率** | 9.54% | **22-47%** | +12-37% |
| **预测过少问题** | 61.2% | **40-50%** | -11-21% |

---

## 🎯 总结

1. ✅ **数据已准备好**：13,409个样本，无需重新转换
2. ✅ **参数已优化**：所有优化已实施
3. ✅ **可以直接训练**：打开GUI或运行命令行
4. ✅ **预期效果良好**：准确率将显著提升

**立即开始训练吧！** 🚀

