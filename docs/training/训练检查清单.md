# 模型训练检查清单

## ✅ 已修复的关键问题

### 1. 状态编码统一 ✅
- [x] 预训练代码使用 `suit * 15 + rank` 编码
- [x] 推理代码使用 `suit * 15 + rank` 编码
- [x] 环境代码使用 `suit * 15 + rank` 编码
- [x] 所有编码方式完全一致

### 2. 模型维度统一 ✅
- [x] 输入维度：512维
- [x] 输出维度：512维
- [x] 动作空间：512维
- [x] 所有地方维度一致

### 3. 代码修复完成 ✅
- [x] `src/train/pretrain.py` - 编码已修复
- [x] `src/train/self_play.py` - 维度已修复
- [x] `src/train/trainer.py` - 方法调用已修复
- [x] `src/rl_agent/agent.py` - 默认维度已修复
- [x] `src/rl_agent/model.py` - 默认维度已修复
- [x] `src/rl_env/guandan_env.py` - 状态编码已实现
- [x] `src/decision/rl_decision_engine.py` - 维度已修复

## 训练前检查

### 环境准备
- [ ] Python环境已安装
- [ ] 依赖包已安装：`torch`, `numpy`, `gymnasium`
- [ ] 模型目录已创建：`models/`
- [ ] 训练数据目录已准备：`game_records/`

### 数据准备
- [ ] 有足够的训练数据（建议至少1000局）
- [ ] 数据格式正确（JSON格式）
- [ ] 数据质量良好（包含有效的手牌和动作）

### 代码检查
- [ ] 所有编码方式一致
- [ ] 所有维度匹配
- [ ] 没有语法错误

## 训练步骤

### 步骤1：预训练
```bash
python src/train/pretrain.py
```

**检查**：
- [ ] 数据加载成功
- [ ] 损失值下降
- [ ] 模型文件生成：`models/bc_model_v1.pth`

### 步骤2：强化学习训练
```bash
python src/train/self_play.py
```

**检查**：
- [ ] 预训练模型加载成功（如果存在）
- [ ] 奖励值提升
- [ ] 训练过程稳定
- [ ] 模型文件生成：`models/ppo_model_v1.pth`

### 步骤3：验证模型
```bash
# 检查模型文件
ls -lh models/ppo_model_v1.pth
```

**检查**：
- [ ] 模型文件存在
- [ ] 模型文件大小合理（几MB）
- [ ] 推理时模型正确加载

## 训练后验证

### 功能验证
- [ ] 模型可以正确加载
- [ ] 模型输出维度正确（512维）
- [ ] 模型输出的索引在有效范围内（0-59）
- [ ] 模型可以映射回卡牌

### 性能验证
- [ ] 模型输出合法动作
- [ ] 模型决策合理（与规则引擎对比）
- [ ] 模型性能提升（如果可能）

## 常见问题排查

### 问题1：模型输出无效索引
- [ ] 检查编码是否一致
- [ ] 检查模型是否正确训练
- [ ] 检查模型文件是否正确加载

### 问题2：训练损失不下降
- [ ] 检查数据质量
- [ ] 检查学习率设置
- [ ] 检查编码是否正确

### 问题3：模型性能差
- [ ] 增加训练数据
- [ ] 延长训练时间
- [ ] 调整超参数

## 相关文档

- `docs/training/模型训练指南.md` - 详细训练指南
- `docs/training/快速开始训练.md` - 快速开始指南

