# Git仓库清理报告

## 📊 清理前后对比

### 清理前状态
- **仓库大小**: 1.00 GiB (约1024MB)
- **警告**: 超过Gitee免费版500MB限制
- **主要问题**: Git历史中包含大量大文件

### 清理后状态
- **仓库大小**: 97.33 MiB (约97MB)
- **减少**: 约927MB (90.5%的减少)
- **状态**: ✅ 远低于Gitee免费版500MB限制

## 🧹 清理内容

### 1. 从Git跟踪中移除的文件

#### Testscore目录（约26MB）
- `Testscore/Test1` (12.63MB)
- `Testscore/Test2` (13.29MB)
- `Testscore/client1`
- `Testscore/client2`

#### Logs目录（约50+个日志文件）
- 所有 `logs/*.log` 文件
- 包括：batch_executor日志、yf1_v4日志、yf1_v5日志、yf2_v4日志、yf2_v5日志等

### 2. 从Git历史中清理的文件

#### 模型文件（130+个文件）
- 所有 `models/*.pth` 文件（约1.5GB+）
- 所有 `models/*.pkl` 文件
- 所有 `models/*.h5` 文件

#### Testscore目录历史
- 从所有历史提交中移除了Testscore目录

#### Logs目录历史
- 从所有历史提交中移除了logs目录中的大文件

## 📝 更新的配置

### .gitignore 更新
添加了以下规则：
```
# Testscore directory (contains large binary files)
Testscore/

# Game records and logs
game_records/
logs/
*.log
```

## ⚠️ 重要提醒

### 1. 强制推送要求
由于清理了Git历史，需要强制推送到远程：
```bash
git push --force --all
git push --force --tags
```

**注意**: 强制推送会重写远程历史，团队成员需要重新克隆仓库。

### 2. 本地文件保留
- ✅ 所有文件在本地仍然存在
- ✅ 只是从Git跟踪中移除
- ✅ 模型文件、日志文件等仍然可以正常使用

### 3. 分支状态
- `m1-dev`: 已清理
- `m1-dev-clean`: 未清理（因为是无历史分支）
- `main`: 已清理
- `v6-dev`: 已清理

## 📈 清理效果

| 指标 | 清理前 | 清理后 | 减少 |
|------|--------|--------|------|
| 仓库大小 | 1.00 GiB | 97.33 MiB | 927MB (90.5%) |
| Pack文件数 | 7个 | 1个 | 6个 |
| 对象数 | 6277 | 6978 | - |

## ✅ 验证

运行以下命令验证清理效果：
```bash
# 检查仓库大小
git count-objects -vH

# 检查被跟踪的大文件
git ls-files | python -c "import sys, os; files = [(line.strip(), os.path.getsize(line.strip()) if os.path.exists(line.strip()) else 0) for line in sys.stdin]; files.sort(key=lambda x: x[1], reverse=True); [print(f'{size/1024/1024:.2f}MB - {path}') for path, size in files if size > 1024*1024][:10]"
```

## 🎯 后续建议

1. **定期清理**: 定期检查并清理大文件
2. **使用.gitignore**: 确保新的大文件不会被意外提交
3. **外部存储**: 考虑将模型文件存储在外部（如云存储）
4. **Git LFS**: 如果升级Gitee会员，可以考虑使用Git LFS管理大文件

## 📅 清理时间

- **清理日期**: 2025-01-27
- **清理工具**: git filter-branch
- **清理脚本**: `clean_large_files.py`, `clean_git_history.py`

