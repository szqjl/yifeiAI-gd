# GUA-160 完成定义

> **GUA-160**：队友六张冲刺时，主攻应先领自然小散单
> **登记/关单**：2026-07-22
> **严重级别**：P0
> **关联**：GUA-078（残局管线）、GUA-091（中局推荐）、GUA-116（主攻领出）、GUA-158（异常降级保护）

## 锚点

`20260722202506820193 [yf1_v8]-[opponent_1_3]-[6]-[2].json` 步 63。

- yf1 手牌 13 张，组牌为 `Straight(2-6) = H2+C3+H4+S5+H6`、`StraightFlush(9-K)`、`Pair/J`，自然散张为 `D3`。
- 队友 yf2 仅剩 6 张；自由领 `Single/D3` 可同时清理本家最小散张并给队友冲刺接牌窗口。
- 旧残局 Q1 先选了使用 `D3` 替换 `C3` 的错误 Straight 变体，组牌保护拦截后又经推荐匹配与安全阀降级，最终错误单出顺子 core 的 `H2`。

## 修复

1. 残局 banned/Q1 之前新增队友冲刺送单优先级：自由领出、主攻/超强主攻、队友恰剩 `6` 张时，从 post-group `card_mask` 中选择最小自然散张 `3–9`。
2. 级牌、王、对子或顺子等 core 拆单不进入该候选；队友不等于 6 张时不触发，继续使用既有残局/Q2 规则。
3. 修正 GUA-116 顺子推荐 rank：牌张可排序输出，但平台 rank 必须从组牌顺序首张取得，禁止按花色字符串排序后的首张推导。

## 验收

- [x] 锚点构造态队友剩 6 张时选择 `Single/3 ['D3']`。
- [x] 不拆 `Straight(2-6)` 的 `H2/C3/H4/S5/H6`。
- [x] 队友剩 7 张时不触发特例，正常选择完整 `Straight/2`。
- [x] GUA-116 与 GUA-160 定向测试通过。
