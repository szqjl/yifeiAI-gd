# 缺陷登记簿（Defect Register）

> 编号规则：`GUA-001`、`GUA-002`… **永不复用**。关闭后若复发，新开编号并 `duplicate of GUA-xxx`。  
> **状态**以文档与提交为准；后续在表中直接维护。  
> **来源扫描**（写入本条时已对照）：`docs/掼蛋AI相关比赛汇总.md`、`docs/reports/README_优化项目.md`、`docs/reports/YF掼蛋优化实施报告.md`、`docs/guandan-brain/notes/WORKFLOW_RESTART_LOG.md`、`logs/`（含 `yf*_m1_*.log`、`batch_executor_*.log` 等）。

> **已重构为 Obsidian 式组织**（2026-06-17）。完成定义详细段落已提取到 `issues/` 目录，主表仅保留索引 + 交叉引用。

## 如何使用本文件（Agent 必读）

| 场景 | 操作 |
|------|------|
| **了解当前缺陷全景** | 读下方「当前条目」表格 → 关注 open 项 + P0 标签 |
| **查某个 GUA 详情** | 表格「备注」列含简案；已关单项的完成定义见 `issues/GUA-xxx-completion.md` |
| **登记新缺陷** | 在底部「模板」复制表格行 → 填入 GUA 编号；若需详细完成定义 → 新建 `issues/GUA-xxx-completion.md` |
| **按版本/引擎筛选** | 看「引擎维护策略」表确认 active/frozen 状态；搜索表格「版本」列 |

## 引擎维护策略（定音 · 2026-05-31 · 评审确认）

| 引擎 | 定位 | 维护状态 |
|------|------|----------|
| **M1** | 首个实验 AI（`yf1_m1` / `yf2_m1` + `src/decision/`） | **frozen**（**GUA-022 closed**）— **非交付线**；**勿再开 M1 策略 GUA**；仅 **bugfix**（crash / 协议 / 记录）与 **pytest 回归** |
| **M3** | **主交付** + **`IDecisionProvider` 底座**（`yf1_m3` / `yf2_m3`、`src/m/m3/`、`src/contracts/`） | **active** — 策略迭代、批跑 KPI、GUA-026+ 跟踪 |
| **V7** | NN 实验线（`yf1_v7` / `yf2_v7` + `UltimateWinRateEngineV7`） | **active**（`v7-dev`）— **GUA-037+** 改造；**队胜率 KPI 观测**见 `ITERATIONS` V7-007；**禁止 import `src.m.m3.*`**（见 `V7-实施方案.md` §1.2） |
| **V5+** | 组牌 / 牌力 / 知识层长期路线 | 规划 / 按需 — **P0 以外**的组牌与牌力评估 **不走 M3 硬编码扩张** |

**队胜率 KPI**：自 **2026-05-31** 起**只看 M3 批跑**。**多样本观测**（净盘、同 exe）：S1 **7/10**、S2 **11/12**、S3 **8/10**、S4 **8/10** → **合计 34/42（81.0%）**（见 `ITERATIONS`「M3 队胜率多样本观测」）。M1 净盘 **0/12** 为 frozen 基线对照，**非**口径错误。

### 实现分工（评审定音）

| 优先级 / 能力 | 落点 | 说明 |
|---------------|------|------|
| **P0 guard**（可 pytest + 批跑验收的 if-guard） | **M3**：`src/m/m3/m3_decision_engine.py`（及 `m3_utils.py`）；**V7**：`src/decision/ultimate_win_rate_engine_v7.py` 内 **V7-native** 过滤壳（**GUA-045**，不 import M3） | 与 [`PRINCIPLES_MAPPING.md`](PRINCIPLES_MAPPING.md) **M3 · P0** 一致；V7 原则对齐见 [[GUA-045-completion]] |
| **组牌 / 牌力 / 混合决策** | **V5+**（`src/v/`、V 挂接 `IDecisionProvider`） | 不在 M3 引擎内堆整篇策略；M3 保持 lalala 式 guard + 契约底座；V7 组牌类见 GUA-045 Phase 2 / **V5+-04** |
| **M1 共用层** | `src/decision/`（frozen） | 历史 PHASE2 与 V shim；**不接新开策略迭代** |

### 复盘发现 → 实现 → 验收（定音 · 2026-06-01）

掼蛋 **重配合、高策略、牌型多样**；108 张均分 4 家时，**两次发牌完全相同**的概率约 **\(10^{-58}\)** 量级（\((27!)^4/108!\)），**具体 replay 步数几乎不会复现**。因此：

| 层级 | 该做什么 | 验收 |
|------|----------|------|
| **M3** | **原则型 guard**（夺权压顺、接风不拆结构、队友在时跟对/别乱单 等） | **`pytest` 构造态** + 回归；replay 仅作**发现缺陷的样例**，**不得**以「再跑某批某副牌」为关单 pass 标准 |
| **V5+** | **222333+顺子+炸弹** 等 **整手结构** 与 **多步配合** / 搜索 | 组牌枚举 + 牌力；因局面几乎不重复，才需要 V 系列 |

真源详表：[`PRINCIPLES_MAPPING.md`](PRINCIPLES_MAPPING.md) §「复盘与验收理念」。

**GUA-022**：**closed**（2026-05-31）；队胜率 KPI 自该日起迁 **M3**（见 [`ITERATIONS.md`](ITERATIONS.md)「M1 frozen 定音」行）。

**批跑局数**：`--target-games` 须 **3 的倍数**（3 / 9 / 12）；见 [`EVAL.md`](EVAL.md)「批跑局数档位」。**勿新开 10 局**等非整批目标。

## 当前条目

> **status**: `open`（蓝色）= 待处理；`closed` ✅ = 已关单。  
> **完成定义**：已关单项的详细实施条件见 `issues/GUA-xxx-completion.md`。  
> **当前活跃 P0**：GUA-054、GUA-055、GUA-059、**GUA-063**、GUA-064、**GUA-065**、**GUA-068**、**GUA-069**、**GUA-070**、**GUA-071**（行牌启发式选择替代 NN argmax）、**GUA-072**（card_mask退化保护✅ + 规则记牌引擎🔴）、**GUA-075**（决策架构改造：排除法→推荐法双路径）、**GUA-078**（残局智能体独立管线：预处理器+决策器+调试脚本）、**GUA-079**（回放诊断：全阶段决策崩坏三层根因）、**GUA-080**（GUA-072 提前拆炸→主攻变助攻/弱牌；中炸 vs 三连对）、**GUA-081**（三带二推荐拆炸 fallback + 贡还手牌同步）、**GUA-082**（主路径 R12 跟单不拆对）、**GUA-083**（推荐匹配失败误 PASS + 录牌 actionList 抽样）、**GUA-084**（五星炸保护 / SF 保炸候选）、**GUA-085**（回退 NN 路径 actIndex 错位 + 领出拆 SF/炸核单张）、**GUA-091**（`stage_2` 中局入口 `_stage_mid_dispatch()`）、**GUA-094**（中局推断层 `_inference_phase_relation` 最小闭环）、**GUA-096**（净盘后强制落盘 v7-win-rate-history）、**GUA-097**（IP 规则对照批跑 helper）、**GUA-098**（决策溯源日志 DecisionTracer）、**GUA-099**（残局 Q1 报单放走：PASS 排序错误 + 级牌配牌类型误判）、**GUA-100**（残局 Q1 规则表自洽：remaining=1/3/5 推荐单张却禁 Single）、**GUA-102**（前中段整牌冲刺点火：可从最小跟单切到主动开炸抢节奏）、**GUA-108**（组牌引擎应枚举“拆低价值炸换顺子降手轮”候选）、**GUA-117**（助攻出牌：Layer2 + Layer0 B1–B6 + §6-7 pytest ✅，方案 A 齐套待关单）、**GUA-116**（主攻 P1–4 领出 ✅）

| ID | 状态 | 严重级别 | 标签 | 版本 | 简述 | 现象 / 复现要点 | 涉及模块 | 备注 |
|----|------|----------|------|------|------|-----------------|----------|------|
| GUA-001 | closed | P3 | observation | docs | 比赛汇总文档曾无法正确阅读 | `docs/掼蛋AI相关比赛汇总.md` 乱码 | 文档 | `closed_in` 文档 v2.0 UTF-8（2026-04-21）；**非**客户端版本问题 |
| GUA-002 | closed | P1 | policy | m1 | M1 不必要 PASS（主动/被动） | 有合法动作仍 PASS；策略链失败后直接 PASS | M1 `phase_handlers` | 见 `docs/reports/m1/M1_PASS问题修复完成报告.md` |
| GUA-003 | closed | P1 | policy | m1 | M1 优先级空候选等价 PASS | `candidates` 为空时曾直接 PASS | `enhanced_priority_system`、`phase_handlers` | 见 `docs/reports/m1/M1_PASS问题修复总结.md` |
| GUA-004 | closed | P1 | rules | m1 | M1 Opening 阶段问题 PASS 集中 | 校验过严致动作被滤；opening 问题 PASS 统计高 | Opening 路由 / 校验 | `docs/fixes/M1_20251224_问题修复记录.md` |
| GUA-005 | closed | P1 | policy | m1 | M1 endgame_early PASS 率过高 | 残局前期过保守 + 校验/优先级叠加 | 残局前期 handler | 同上 |
| GUA-006 | closed | P2 | policy | m1 | yf2_m1 相对 yf1_m1 改善滞后 | yf2 PASS、问题 PASS 仍偏多 | M1 双客户端 | `docs/reports/m1/M1行为改善分析总结.md` |
| GUA-007 | closed | P1 | policy | m1 | M1 Opening 被动不当 PASS | 对手出对/单张仍有可选动作却 PASS | `OpeningPassiveHandler` | 同上；与 `M1_PASS问题分析报告.md` 中阶段分析一致 |
| GUA-008 | closed | P1 | observation | m1 | M1 记录缺少 victoryNum / 评估异常 | `gameOver` 过早保存；需 `gameResult` 与 `victoryNum` | `yf1_m1.py`、`game_recorder`、评估器 | [GAME_RECORD_SAVE_FIX.md](../fixes/GAME_RECORD_SAVE_FIX.md)、[EVALUATOR_COMPATIBILITY_REPORT.md](../fixes/EVALUATOR_COMPATIBILITY_REPORT.md) |
| GUA-009 | open | P3 | policy | v4 | V4 RL 未启用 | 对比说明：RL 部分集成但未作为默认路径 | V4 混合决策 | `docs/V4_V5_COMPARISON.md` | `docs/versions/V4_V5_COMPARISON.md` |
| GUA-010 | open | P1 | observation, policy | v4, v5 | 对局记录中决策信息不全 | `candidates_count=0` 等，score/layer 空，难评混合链 | hybrid / V4–V5 栈 | 对照 `docs/analysis/YF决策问题分析与修复.md`；与回放/评测管道一致时需样本 |
| GUA-011 | closed | P1 | rules | v5 | 红心配（逢人配）识别与使用不当 | 曾硬编码 H2；应按 `cur_rank` 用 `H{rank}`；小顺浪费红心配 | `card_grouping_strategy` | `docs/fixes/GAME_ISSUE_FIX_SUMMARY.md` |
| GUA-012 | closed | P2 | policy | v5 | 天然单张未优先、拆三张出单 | 有天然单张却拆三张 | `yf1_v5.py`、`yf2_v5.py` | 同上 |
| GUA-013 | open | P2 | observation | v5, 引擎? | 疑似重复出牌（手牌跟踪） | 记录中同张牌似重复；例 D9 | 引擎或客户端状态 | `GAME_ISSUE_FIX_SUMMARY.md` 建议查 `guandan_offline` 侧 |
| GUA-014 | open | P2 | policy | 共用 | 拆牌与优先级不合理 | 拆三张、未优先多余单张、钢板/对子等（多报告归纳） | 多版本共用决策层 | `docs/analysis/YF决策问题分析与修复.md`；根因在 policy 居多 |
| GUA-015 | open | P2 | policy | v6 | V6 路线与验收未闭环 | README 与实施报告：**队友保护、动态优先级、yf1/2_v6、胜率对 lalala/V4/V5** 仍为规划或待验 | V6 规划 | `docs/reports/README_优化项目.md`、`docs/reports/YF掼蛋优化实施报告.md`（`.kiro/specs` 任务 3–11） |
| GUA-016 | open | P1 | policy | 训练 | 训练样本大量空 action_cards | PASS 样本稀释；需加载器过滤 | `simple_data_loader`、Stage7+ | [TRAINING_FIXES_SUMMARY.md](notes/TRAINING_FIXES_SUMMARY.md)；`logs/mlruns` 联调 |
| GUA-017 | open | P1 | policy | 训练 | 损失尺度与预测行为异常 | 过度预测、阈值与惩罚需迭代（含 WORKFLOW 重启那轮参数） | Stage7、`logs/mlruns` | [WORKFLOW_RESTART_LOG.md](notes/WORKFLOW_RESTART_LOG.md)；`logs/batch_executor_*.log` 对照运行窗口 |
| GUA-018 | open | P2 | policy | 训练 | 策略理解率指标曾为 0 / 与完全匹配耦合过严 | 需联合损失与「基本正确」定义 | 训练指标 | `docs/fixes/STRATEGY_UNDERSTANDING_FIX.md` |
| GUA-019 | closed | P1 | policy | 训练 | （已合并跟踪）损失爆炸与阈值专项 | 与 GUA-017 同源：[WORKFLOW_RESTART_LOG.md](notes/WORKFLOW_RESTART_LOG.md) 中阈值 clamp、对数惩罚等 | 同 GUA-017 | **closed**；`duplicate of GUA-017`（同一轮日志治理，避免双开） |
| GUA-020 | closed | P2 | observation, policy | m1 | 验证 yf2_m1 是否仍明显弱于 yf1_m1 | 成对 `game_id` 扩样后差距不显著 | `yf1_m1.py`、`yf2_m1.py`、共用 `src/decision/` | `closed_in` 2026-04-21：见 `ITERATIONS.md`；`logs/yf1_m1_*.log` / `yf2_m1_*.log` |
| GUA-021 | closed | P1 | policy | m1 | M1 「问题 PASS」仍偏多（复盘） | 改后新局 3 个成对 `game_id` 合并：近似问题 PASS **0**（见 `ITERATIONS`「GUA-021 共用层收紧」行） | `phase_handlers`、`stage_router`、`rule_based_decision_engine_m1`、`intelligent_router` | `closed_in` 2026-04-21；全量 `game_records` 若混改前旧局，合并合计仍可能 **>0**，评测建议按「改后 `game_id`」子集统计 |
| GUA-022 | closed | P1 | policy | m1 | ~~M1 队胜率攻关~~ → **M1 frozen，队胜率 KPI 迁 M3** | M1 净盘 12 局 **`[0,3,0,3]`×4 → 0/12**；M3 同机 **7/10**；多轮 PHASE2 策略未改队胜 | `src/decision/`（frozen） | **`closed_in` 2026-05-31**：M1 **非交付线**，定音 **frozen**（见上表）；不再迭代 M1 取胜策略；队胜率见 **M3 批跑** / `ITERATIONS` GUA-022/026 行 |
| GUA-023 | open | P1 | observation, infra | agent-hub | Kanban worker 经 OpenCode ACP 无法执行（0 tool calls → crash 循环） | `copilot-acp` + `opencode acp`：Hermes 收不到 `<tool_call>`，worker 未 `kanban_complete` 即退出；例 `t_b53fc45b` | Hermes `copilot_acp_client`、profile `opencode-eng` | 根因与处置见 [`AGENT_HUB.md`](AGENT_HUB.md)「方案 A」「接下来要做的（2026-05-21）」；与 **GUA-022** 策略无关（Kanban 任务误标 GUA-022 测试） |
| GUA-024 | closed | P1 | policy | m3 | M3 play 阶段几乎全 PASS | 两轮根因：（1）`curAction[-1]` 误用；（2）**细化 debug 发现** `curAction`/`greaterAction` 偶发 **str** → dispatch 全 miss → 恒 PASS；已加 `_ensure_list` 规范化 | `m3_decision_engine` | `closed_in` 2026-05-29～30；541958 验证 dispatch 100% list、100+ 非 0 决策 |
| GUA-025 | closed | P1 | observation | infra | 回放 yf1 初始手牌与出牌流水不一致 | `_merge_same_game_records` 用 start_time **5 秒窗口**，batch 多局误把 round 12 手牌合并进 round 19；表现为「出了没有的牌 / 出牌后手牌扣不对 / 误显 4 个 3 炸弹」 | `game_recorder._merge_same_game_records`、`yf_replay` | `closed_in` 2026-05-30；改按文件名 opponent+round+level 匹配；`tests/test_game_recorder_merge.py` 4 passed |
| GUA-026 | closed | P2 | policy | m3 | M3 三带二/拆牌策略偏激进 | round 19 真实记录 yf1 出 555+22、QQQ33 等（非回放 bug）；**2026-05-30**：`_ThreeWithTwo` 增级牌/炸弹保护、禁拆 trips 常态路径、移除逢人配三带二 fallback | `m3_decision_engine` | **`closed_in` 2026-05-31**：`test_m3_gua026.py` **3 passed** + GUA-029/031/032 回归 **24 passed**；净盘 **12 局** 队胜 **11/12**、近似问题 PASS **0**（日志 `logs/batch_executor_20260531_213847.log`）；未再现 **555+22 拆炸** 类回归。牌谱全量含 **H+curRank** 三带二 **39** 手（被动压牌边界，留 PRINCIPLES §二十 P2）；[`10_three_with_two_skills.md`](../knowledge/skills/04_common_skills/10_three_with_two_skills.md) §二十 **主战术真源** |
| GUA-027 | closed | **P0** | observation, policy | m3 | M3 对 WebSocket 场态消息用法不完整/不准确 | 对照 v1006 说明书：**被动比牌应信 `greaterAction` + `publicInfo.playArea`**；M3 仅在 `curAction==PASS` 时用 greater，否则用 `curAction[1]` 算强度；playArea 未参与决策。19 局审计 ~40% 单牌步 greater 与「本圈最大」不一致 | `m3_decision_engine`、`game_logic/trick_state.py`、`yf_replay` | `closed_in` 2026-05-30；`resolve_effective_greater` + `TrickSequenceTracker`；**7+22 pytest passed** |
| GUA-028 | closed | P1 | observation, rules | m3 | M3 与 v1006 说明书三项未对齐 | **TripsPair** 未分派致 PASS；**indexRange** 未 clamp 回包；**publicInfo.rest** 未同步剩牌 | `platform_act.py`、`m3_decision_engine`、`yf1_m3`/`yf2_m3` | `closed_in` 2026-05-30；`test_m3_platform_align_gua028.py` 5 passed |
| GUA-029 | closed | **P0** | policy, rules | m3 | M3 炸弹可执行规则包（R1–R6） | 回放 `20260530172743739854` 曾现 yf1 **5×8** 全程未出；根因 `choose_bomb` 读 `action[-1]` → TypeError → PASS | `m3_utils.choose_bomb`、`m3_decision_engine` | **`closed_in` 2026-05-31**：R1–R6 + `test_m3_gua029` **8 passed**；净盘批跑 **10/10**；炸弹 yf1 **175** + yf2 **152**。完成定义见 [[GUA-029-completion]] |
| GUA-030 | closed | P2 | docs, policy | m3, v5 | **原则+战略映射表**：`01_basic_principles` / `02_strategy_overview` / `03_basic_strategy` → 引擎归属（**P0→M3**，**P1+→V5**） | 无代码缺陷；需将 skills 落实为可测 guard / V5 知识层，避免整篇硬编码进 M3 | [`PRINCIPLES_MAPPING.md`](PRINCIPLES_MAPPING.md)；真源见 §一–§七 | `closed_in` 2026-05-31；三篇映射评审通过；M3 P0/P1 实现由 GUA-029 及后续迭代跟踪。完成定义见 [[GUA-030-completion]] |
| GUA-031 | closed | P1 | policy | m3 | M3 传牌 `numof*` guard + 队友让道扩全牌型 | 被动仅 `_Single/_Pair` 等部分牌型做队友 PASS；`_active` 无 `numoffri==1` 送小单 / `numoffri==5` 喂牌；`numofnext==1` 防小单不完整 | `m3_decision_engine`（`_active`、`_passive`、各 `_Xxx` handler） | **`closed_in` 2026-05-31**：P-F02 扩 `_Trips/_ThreePair/_TwoTrips/_Straight`；PASS-P02/P03/P04；`test_m3_gua031.py` **7 passed**。完成定义见 [[GUA-031-completion]] |
| GUA-032 | closed | P1 | observation, policy | m3 | M3 **记牌+算牌**确定性规则 + `remain_cards_classbynum` 同步 | 文档 §14–§15、§18–§20、**§二十二**（孤张定律 CG-T06、口诀 13）；5/10 法则、顺/夯点位预判（**CALC-M03/M04**）；`remain_cards_classbynum` stale | `m3_decision_engine._update_play_state`、`m3_utils` | **`closed_in` 2026-05-31**：`sync_remain_cards_classbynum` + `_update_play_state` 同步；`test_m3_gua032.py` **6 passed**。完成定义见 [[GUA-032-completion]] |
| GUA-033 | closed | P1 | observation, rules | m3, infra | M3 **批末 `victoryNum` / `gameResult` 解析错误**（批级回填污染） | **客户端已修**；**平台根因**：本包 v1006 exe **argv 无效**，单次会话 **固定 3 局** → WebSocket `settingTimes=3` | `yf1_m3.py`、`yf2_m3.py`、`game_result_utils.py` | **`closed_in` 2026-05-31**；`test_m3_gua033.py` **11 passed**。完成定义 + 平台根因见 [[GUA-033-completion]] |
| GUA-034 | closed | P1 | policy | m3 | M3 **残局：队友走光后 rank 小牌首出 + 被动不拆结构压牌** | round **38** yf2：102–107 步 — 接风 `_active` 拆对 **3** 出单；对手 **6/对6** 可压却 **PASS**。**根因**：`_active` 仍走 `rankone/ranktwo` 清小点；缺「队友 rest=0 → 1v2 拦头游」模式 | `m3_decision_engine`（`_active`、`rankone/ranktwo/rankthree`、`_Single`、`_Pair`） | **`closed_in` 2026-06-01**：方向 A — `_is_solo_sprint` + END-M02–M04；`test_m3_gua034.py` **6 passed**。完成定义见 [[GUA-034-completion]] |
| GUA-035 | closed | P1 | policy | m3 | M3 **END-M02+：solo 接风按对手剩张过滤牌型** | GUA-034 END-M02 仅固定优先三带二/三张/对子；缺：**任一对手剩 1 张 → 不宜出小单**等 | `m3_decision_engine._gua035_solo_wind_pick`、`_gua034_solo_active_pick` | **`closed_in` 2026-06-01**：END-M02+-01–04；`test_m3_gua035.py` **6 passed**。完成定义见 [[GUA-035-completion]] |
| GUA-036 | closed | P1 | policy | m3 | M3 **控权 + 接风配合**（非 solo） | batch7 round38 复盘：**①** 接风拆 2 打单，未跟队友对子线；**②** 敌出杂顺可压却 PASS。**根因**非「缺顺子函数」，而是 guard 缺口 + `_Straight` 过窄 | `m3_decision_engine`：`_active` 接风、`_Straight` | **closed_in** 2026-06-01。完成定义见 [[GUA-036-completion]]。KPI 观测：GUA-036 样本均值低于 GUA-034/035，方差属发牌随机波动（详统计检验 note） |

**统计检验 note（2026-06-02 补充）**：GUA-034/035 26/33=78.8% vs GUA-036 36/69=52.2%（S9后），**差值 26.6pp，p=0.009**；95% CI [8pp, 45pp]。**结论**：GUA-036 **显著差于** GUA-035，非发牌随机波动可解释。

| GUA-037a | closed | P0 | V-nn, v7 | v7 | V7 静态特征工程（state_牌态 124 维） | `_extract_features` 静态部分全零填充；需重写手牌 108 维 + 级牌/红心配 9 维 + 6 维 + hand count 1 维 = 124 维 | `src/v/nn/features/static_features.py` | `closed_in` 2026-06-07。Part 3（2026-06-14）：叠加局面信念分类器 → 4 维 soft 向量至索引 188-191；特征利用率 37.5% |
| GUA-037b | open | P1 | V-nn, v7 | v7 | V7 动态特征工程（LSTM 历史编码） | 在 037a 静态特征基础上叠加 LSTM 历史编码（出牌历史 + numof* 序列），目标总维度 188 维 | `src/v/nn/features/dynamic_features.py` | 完成定义见 `V7-实施方案.md` §2 Phase 1 GUA-037b；可与 GUA-038 并行 |
| GUA-038 | **closed** ✅ | P1 | V-nn, v7 | v7 | V7 M3 知识蒸馏（BC 热启动） | V7-internal 录牌 → BC 训练 | `src/v/nn/recorder/v7_recorder.py`、`bc_dataset.py`、`bc_trainer.py` | **`closed_in` 2026-06-07**：`tests/test_v7_bc.py` **34/34 passed**。06-16 首次物理落地训练（1208 样本，val_acc=82.57%）。06-17 M3 胜局重训（5555 样本，val_acc=35.19%，1 epoch 早停）→ 退化触发 GUA-059 |
| GUA-039a | open | P2 | V-nn, v7 | v7 | V7 自对弈 DMC + ZMQ 桥 + Actor 原型 | 搭建 V7 自对弈基础设施 | `src/v/nn/training/actor.py`、`learner.py`、`replay_buffer.py`、`zmq_bridge.py` | 完成定义见 `V7-实施方案.md` §2 Phase 3；**关单前提**：评审后方可启动 GUA-039b |
| GUA-039b | open | P2 | V-nn, v7 | v7 | V7 自对弈 top-K + PPO + 30 局评估 | 在 039a 基础上叠加 top-K=2 过滤 + PPO policy net + 两阶段 30 局 lalala 评估基线 | `scripts/v7/eval_vs_lalala.py` | 完成定义见 `V7-实施方案.md` §2 Phase 3；仅在 GUA-039a 关单后启动 |
| GUA-040 | open | P1 | V-nn, v7 | v7 | V7 模型权重管理（COS manifest + 版本切换） | 搭建 V7 模型权重的 COS 上传/下载/版本切换基建 | `models/v-nn/manifest.json`、`scripts/v7/weight_manager.py` | 完成定义见 `V7-实施方案.md` §2 Phase 1 并行 GUA-040 |
| GUA-041 | **closed** ✅ | P1 | V-nn, v7 | v7 | V7 路径债清理 | 消除 V7 客户端与启动器中的 D 盘硬编码路径债 | `config/v7_paths.yaml`、`src/utils/v7_paths.py` | **`closed_in` 2026-06-05**：pytest 6/6 passed |
| GUA-042 | **closed** ✅ | P1 | V-nn, v7 | v7 | ABL-GD 168 伪动作评估（含开源可行性） | 调研 ABL-GD（CCFAI 2025）168 伪动作方案的可获取性与可移植性 | `docs/analysis/abl-gd-eval-2026-06.md` | **`closed_in` 2026-06-07**：结论为弃用该方案 |
| GUA-043 | **closed** ✅ | P1 | V-nn, v7 | v7 | 专利规避设计审计（CN113018837A 边界） | 审计掼蛋 AI 算法专利 CN113018837A 的权利要求边界 | `docs/governance/patent-audit-cn113018837a.md` | **`closed_in` 2026-06-07**：结论为 V7 核心技术路径与专利无实质重叠 |
| GUA-044 | **closed** ✅ | P1 | observation, infra | v7, m1, m3, batch | **批跑四席未就绪即开局 + 首局空等卡顿** | V7 批跑 `wait_for_clients_connected` 误报 **0/4** 仍继续；第 4 席连上即开局 | `batch_executor/client_ready.py`、`websocket_manager.py`、`lalala_adapter.py` | **`closed_in` 2026-06-06**：四席就绪门闩 + 就绪表；`tests/test_client_ready.py` **3 passed**。完成定义见 [[GUA-044-completion]] |
| GUA-045 | **closed** ✅ | **P0** | policy, v7 | v7 | **V7 决策根因：零特征模型 + 无 P0 Guard** | `UltimateWinRateEngineV7` 对 `actionList` **argmax**；零条 guard；回退「首个非 PASS」 | `src/decision/ultimate_win_rate_engine_v7.py`；`src/v/nn/guards/v7_guards.py` | **`closed_in` 2026-06-07**：V7-native `filter_action_list` + V7-R01~R06；`tests/test_v7_gua045.py` **24/24 passed**。完成定义见 [[GUA-045-completion]] |
| GUA-046 | open | P3 | observation, infra | v7, m1, m3, batch | **副间 ~10s 停顿（服务器内在结算/发牌间隔）** | V7 批跑日志显示每副牌决策集中在 **1 秒内**打完，之后服务器进入 **~10s** 无消息空窗期 | 无（服务器行为） | **订正 2026-06-07**：GUA-047 原误判已 closed；本条恢复原状 |
| GUA-047 | **closed** ✅ | **P1** | observation, infra | v7, m1, m3, batch | **【误判关单】** 同副牌内 4 席全停 ~20s → **实际是 batch_executor 日志 dump 延迟** | `batch_executor/restart_manager` 将服务器 stdout **延迟 dump** 到日志（延迟 73s）；实际 4 席节奏完全同步 | `docs/analysis/批跑cmd窗口观察.md`；`batch_executor/restart_manager.py` | **closed_in 2026-06-07**：原 GUA-047 误判已订正。新开 GUA-048 跟踪日志 dump 延迟 |
| GUA-048 | open | P2 | observation, infra | v7, m1, m3, batch | **批跑 73s 卡顿：客户端 game_ready 写盘超时（根因 A）+ ServerStdoutReader 启动晚（根因 B）双根因** | **73s 卡顿 = 60s game_ready 超时 + 13s dump 落幕叠加**。`wait_for_all_clients_game_ready(timeout=60)` 硬超时 | `batch_executor/client_ready.py:180`、`executor.py:514`、`executor.py:547` | **登记 2026-06-07**（P2，回退 open）。关单条件：① game_ready 写盘 < 10s；② ServerStdoutReader 立即启动；③ pytest；④ ≥3 局批跑验收。**相关**：GUA-049（game_ready 写盘慢专条，P1） |
| GUA-049 | open | **P1** | observation, infra | v7, m1, m3, batch | **【根因锁定】** 客户端 `mark_game_ready` 写盘 4 进程 race condition，3 子根因 | `batch_executor/game_ready.json` **只有 3 entry**（缺 yf2_v7）。**A1**：`_game_save` 非原子写 → JSON 损坏；**A2**：`mark_game_ready` 无 try/except + asyncio 静默异常；**A3**：期望 4 entry + 60s 硬超时 | `batch_executor/client_ready.py:52`、`231`；`websocket_manager.py:276` | **登记 2026-06-07**（P1，根因已锁定）。关单条件：① temp+rename 原子写；② try/except；③ pytest 4 进程并发 100 次 0 损坏；④ ≥3 局批跑验收。**相关**：GUA-048 |
| GUA-050 | open | **P0** | V-nn, v7 | v7 | **局面信念向量 8 维**（扩展 GUA-037a） | V7 需从 `P(action|state)` 升级为 `P(action|state, belief)`。叠加 8 维信念 | `src/v/nn/features/static_features.py` | **登记 2026-06-16**（P0）。特征维数 124→132。完成定义：信念向量在线推理 ≤1ms；pytest ≥6 case；V7 净盘 3 局观测 |
| GUA-051 | **closed** ✅ | P1 | V-nn, v7 | v7 | **稠密 Reward 信号 9 种**（扩展 GUA-039a） | 9 种中间 reward：出牌成功+0.05/接风+0.1/掼蛋+0.3/级牌控制+0.2/配合+0.1/送对家+0.15/炸弹±0.5/本方升级+2.0/对方升级-1.0 | `src/v/nn/training/reward.py` | **closed_in** 2026-06-16：`reward.py` 实现 + `RewardAccumulator` + 12 项 pytest |
| GUA-052 | **closed** ✅ | P1 | V-nn, v7 | v7 | **108 张牌全量追踪 + 排除法推断**（扩展 GUA-037b） | 增补：已出牌、对手/队友出牌牌型、各家剩张、级牌状态；排除法推断对手手牌 | `src/v/nn/features/memory_tracker.py` | **closed_in** 2026-06-16：`memory_tracker.py` 实现 + 24 维 state_vector + 10 项 pytest |
| GUA-053 | open | P2 | V-nn, v7 | v7 | **对手池多样性**（扩展 GUA-039b） | 保留历史 checkpoint 对手池，每次 self-play 从对手池随机选 | `src/v/nn/training/opponent_pool.py`（待建） | **登记 2026-06-16**（P2，最低优先级），等 GUA-039a/039b 关单后启动 |
| GUA-054 | open | **P0** | V-nn, v7, policy | v7 | **V7 组牌质量中间表示**（grouping_scanner 9 维，借鉴 v4 策略库经验） | 基于 **6 大组牌核心原则** + v4 策略库组牌优先级 → 9 维 grouping_score | `src/v/nn/features/grouping_scanner.py`（新建） | **登记 2026-06-17**（P0）。完成定义：9 维特征；pytest ≥10 case；接入 MT 24→33 维（共 229 维）；V7 净盘 3 局副胜率不下降。**级牌约束**：禁止 `from src.m.m3 import ...` |
| GUA-055 | open | **P0** | V-nn, v7, policy | v7 | **V7 动作空间二阶段过滤**（借鉴 v4 ActionSpaceOptimizer，纯函数式） | 启发式 Top-K 100（纯函数式）→ V7 NN 精细评估 | `src/v/nn/features/action_space_filter.py`（新建） | **登记 2026-06-17**（P0）。完成定义：Top-K 启发式不调参；pytest ≥6 case；V7 引擎 `decide()` 二阶段接入；总耗时 <0.8s。**先 GUA-054 后 GUA-055** |
| GUA-056 | open | P1 | V-nn, v7, training | v7 | **V7 双上节奏 reward**（v4 缺失项补足，扩展 GUA-051） | 队友前 2 名跑光时 +1.0 | `src/v/nn/training/reward.py`（追加） | **登记 2026-06-17**（P1）。必须 GUA-039b 30 局 vs lalala 基线完成后再启动 |
| GUA-057 | open | **P1** 🔴 | V-nn, v7, policy | v7 | **V7 记牌模块（NN版）**（4×27 维 = 108 维剩余牌概率分布） | LSTM/Transformer encoder-decoder → 各家剩余牌分布概率。属于 [[组牌-NN衔接设计]] §十「2-4 周」路线。与组牌引擎相同 TDD 迭代模式（投喂→分析→修正→再投喂）。前置条件：① GUA-072 规则记牌 closed（先跑通规则版验证收益）；② GUA-071 heuristic 副胜率稳定 ≥15%。不依赖 GUA-039b 自对弈基线（heuristic 已替代 NN argmax）。区别于端到端 BC：输入 4 条出牌序列完整、输出剩余牌分布低维、有 ground truth（restCards） | `src/v/nn/features/card_counting_network.py`（新建） | **登记 2026-06-17**，**战略更新 2026-06-19**：前置依赖从 GUA-039b+GUA-054 降为 GUA-072+GUA-071稳定基线。TDD 迭代模式：MemoryTracker 追踪数据→规则记牌 v1→NN 记牌 v2 逐步升级 |
| GUA-058 | open | **P1** | V-nn, v7, policy | v7 | **V7 策略模块**（4 分类：进攻/防守/观望/保对家） | 组牌+记牌 → 4 类策略分类。属于 [[组牌-NN衔接设计]] §十「长期」路线。TDD 迭代模式同组牌引擎。前置条件：GUA-057 closed（NN 记牌可用）+ GUA-071 heuristic 稳定。当前 Guard 已有角色感知（主攻/助攻/超弱），策略模块在此基础上做更长线规划 | `src/v/nn/features/strategy_network.py`（新建） | **登记 2026-06-17**，**战略更新 2026-06-19**：前置依赖从 GUA-057+GUA-039b+GUA-054 降为 GUA-057+GUA-071稳定。Guard 角色体系（determine_role）已提供 4 级角色分类，策略模块可在此基础上做 4 类策略分类 |
| GUA-059 | open | **P0** | V-nn, v7, training | v7 | **GUA-038 BC v2 退化根因定位**（副胜率 11.8%→0%，训练→实战脱钩） | 三次批跑曲线：第 2 次 12 局副胜率 11.8%；第 3 次 12 局副胜率 0%。训练侧与实战侧完全脱钩 | `scripts/train_bc_v7.py`、`src/v/nn/training/bc_dataset.py` | **登记 2026-06-17**（P0，所有 P1 GUA 的硬前置）。关单硬条件：≥30 局 vs lalala 队胜率 + 副胜率均不下降。**相关**：GUA-038（退化触发源）；GUA-060（BC 调参终止） |
| GUA-060 | **closed** | **P0** | V-nn, v7, training | v7 | **V7 BC val_acc 锁死 36.46% — 终止 BC 调参路线** | 5541 样本 → val_acc=36.46% 6 epoch 锁死；argmax collapse 强证据。BC 模仿学习 argmax collapse 是**理论必然** | `scripts/train_bc_v7.py`、`bc_trainer.py` | **`closed_in` 2026-06-17**：BC 调参路线终止。GUA-061 模块化架构 |
| GUA-061 | **closed** ✅ | **P0** | V-nn, v7, policy, architecture | v7 | **V7 转向模块化架构：M3 组牌逻辑提取 + GroupingEngine** | 从 M3 提取组牌逻辑→封装 GroupingEngine→接入 V7 特征管线→组牌增强 BC 重训对比 | `src/v/nn/features/grouping_engine.py`（新建） | **`closed_in` 2026-06-18**：P0①②③全部完成。① GroupingEngine 新建 298 行 + 31 pytest；② 接入 memory_tracker/bc_dataset/V7 engine 双路径；③ BC 重训对比：grouping_engine(24维) vs grouping_scanner(9维) val_acc 均为 37.36%，BC argmax collapse (GUA-060) 无法区分特征质量。**真实验证待 GUA-039b 自对弈**。**发现**：M3 原始组牌无回收能力/灵活性评分，GUA-061 忠实提取了弱逻辑 → 后继 GUA-062。**2026-06-18 流水线接通**：① 生产端 `yf1_v7/yf2_v7` 从 grouping_scanner(9维)→grouping_engine(24维) 切换 ② `_extract_features` MT维度 33→48 对齐 ③ `decide()` 动作选择现已走 24 维分组评分。 |
| GUA-062 | **closed** ✅ | **P0** | V-nn, v7, policy, architecture | v7 | **组牌引擎 v2：静态回收评估 + 灵活性 + 真回溯多方案** | GUA-061 提取的 M3 组牌逻辑缺两个核心维度（回收能力 0.3 + 灵活性 0.2）+ 仅 4 策略贪心标签。基于 `人类掼蛋决策流程完整分析.md` §阶段0/§4.1 + `04_card_grouping_skills.md` §一 重建评分体系 | `src/v/nn/features/grouping_engine.py`（升级） | **`closed_in` 2026-06-18**：pytest 49 passed。详见 issues/GUA-062-completion.md。**批跑 2026-06-18T20:28**：V7 vs Lalala 3局，局胜 0/3(0%)，副胜 3/54(5.6%)，达A 9/54(16.7%)，双上0次。末级分布：2-5级 21(38.9%) / 6-10级 13(24.1%) / J-K 11(20.4%) / A 9(16.7%)。流水线接通（grouping_engine 24维+bc_model_v3）后首次批跑，模型决策率100%。3副胜：R7(cR=2,pos2先跑)/R10(cR=8,pos2先跑)/R27(cR=6,pos0先跑)。NN出牌无方案约束→衔接待解(GUA-063) |
| GUA-063 | **open** 🔄 | **P0** | V-nn, v7, policy, architecture | v7 | **组牌→出牌衔接：is_core 不保护顺子/三张 + 贡前手牌 + R16队友送单** | **已实施（2026-06-18）**：Phase 1 card_mask、Phase 2 `_group_consistency_filter` 角色驱动前置过滤、Phase 3 触发式中局重分组。**重开根因（2026-06-18 副牌回放诊断）**：<br>**① is_core=0.0 导致顺子/三张被 NN 拆散**：`_group_consistency_filter` 中顺子和三张的 `is_core=0.0`，filter 不保护这些结构牌型。实际回放：副 17-2 yf1 组牌方案含 3-4-5-6-7 顺子 + 888 三张，但 NN 出牌时拆 D5 打单、拆 S8 打对——`_group_consistency_filter` 角色为主攻时本应剔除拆结构动作，却因 `is_core=0.0` 放行。<br>**② 贡前手牌问题（GUA-067 联动）**：`initial_hand` 来自 gameStart 的 `handCards`（贡前 27 张），训练侧 `current_hand = initial_hand - played_cards` 推算错误。已修复：`adjust_initial_hand_for_tribute_back` 在 4 条贡牌/还贡路径上实时调整 `initial_hand`。<br>**③ 顺子扫描高→低违背去小单化（已修复 2026-06-18）**：`_detect_straights` 原从高→低窗口扫描，2-7 六连张先锁 3-4-5-6-7 留单 2。**根因**：掼蛋核心原则是「去小单化」——越小的单越难顺掉，组顺子首要目标就是吸收小单。**已修**：窗口扫描改为低→高（2-6 优先于 3-7），把大单（7-K-A）留给其他组合（对子/三带二），大单比小单更易匹配搭档。<br>**④ 大顺子压制力需区分初始组牌 vs 动态出牌**：大顺子有压制力（逼炸/盖牌），但这属动态出牌决策层面。初始组牌阶段不应以牺牲去小单化为代价追求大顺子；战时如需升级可从最小组起的方案派生（如对手出 2-6 后可升级为 3-7）。<br>**⑤ R16 队友送单精细化（2026-06-20）**：队友剩1张时放行全部送单，但下家也是1张时不能放行（下家直接跟牌头游）。条件：teammate剩1张 AND 下家(opp1)≠1张 → 放行全部。<br>**解法**：① 将顺子和三张标记为 `is_core=1.0`（**已修 2026-06-18**：`to_card_mask` 中 straights/trips → `is_core=True`），让 `_group_consistency_filter` 保护这些结构牌型不被 NN 拆散；② GUA-067 已修复贡前手牌；③ 顺子扫描改为低→高（**已修 2026-06-18**）；⑤ R16 已实施（2026-06-20）。 | `grouping_engine.py` → `_detect_straights`（扫描方向）、`to_card_mask`（is_core）、`memory_tracker.py` → `ultimate_win_rate_engine_v7.py`；`v7_game_recorder.py`（GUA-067） | **重开 2026-06-18**，**全部子项已修复 2026-06-18**，**R16 已实施 2026-06-20**。原闭单见 [[组牌-NN衔接设计-软引导vs硬约束]]。① is_core=1.0（顺子/三张）已修；③ 顺子去小单化已修；② GUA-067 贡牌调整已实施；⑤ R16 已实施；pytest 50/50 pass（含 4 new R16 case）。**批跑 2026-06-20**：3局42副，队胜0/3(0%)，副胜10/42(23.8%)，达A 6副。card_mask零退化，R16本批未触发。可关单条件：批跑验证顺子/三张不再被 NN 拆散。**相关**：GUA-062（组牌引擎 v2）、GUA-067（贡牌手牌修复）、GUA-072（card_mask退化保护） |
| GUA-064 | open | **P0** | V-nn, v7, observation, training | v7 | **BC argmax collapse 日志确证：2048 维输出仅用 2 维** | 2026-06-18 两轮 6 局 86 副日志分析：V7 动作索引分布 actIndex=0(PASS) 50.1%、actIndex=1(首候选出牌) 48.9%、actIndex≥2 仅 20 次(0.9%)。PASS 率与 Lalala 持平(V7 50-52% vs Lalala 53-55%)，非败因。卡牌验证 WARNING 28 次(≈每3副1次)，低频不致命。**根因**：BC 模仿学习 argmax collapse 是理论必然(GUA-060)，2048 维候选空间中 99%+ 从未被选择。组牌引擎(GUA-061/062)+衔接(GUA-063)均未改变此结构 | `ultimate_win_rate_engine_v7.py`、`bc_trainer.py`、`bc_model_v3.pth` | **登记 2026-06-18**（P0，所有 V7 策略 GUA 的硬前置）。实锤数据来源：4 个客户端日志 `yf*_v7_20260618_2145*.log` + 2 个服务端日志 `v7_vs_lalala_20260618_2145*.log`。**2026-06-19 战略转向**：不再尝试修 NN argmax（温度采样/beam search 均治标不治本），改为直接替代为启发式选择 `_heuristic_select`（GUA-071），复用组牌引擎的 TDD 迭代模式。NN 权重文件（bc_model_v3.pth）保留，待 GUA-039b 自对弈 RL 就绪时重新接入。参见 [[组牌-NN衔接设计]] §九。 |
| GUA-065 | open | **P0** | V-nn, v7, policy, guard | v7 | **导入 M3 队友识别与保护到 V7 Guard 体系** | V7 当前完全缺失队友识别与保护逻辑——不知道「队友是谁」、不保护队友控牌权、不送队友关键张。M3 已有 8 条队友相关规则（`_is_teammate_greater`、`_gua031_passive_teammate_yield`、`_gua031_active_min_single`、`_gua031_active_feed_five`、`_gua036_teammate_trick_anchor`、`_gua036_team_wind_pick`、`_is_solo_sprint`、`_gua029_try_bomb` R5 不炸队友），但 M3 队友保护也被评估为「极弱」（M3_DIAGNOSIS.md：仅检查 `(myPos+2)%4==greaterPos`，无队友手牌分析、无主动送牌）。导入方式：① V7 guard 新增 R07（队友控牌不压除非残局冲刺）、R08（队友剩 1 张送最小单）、R09（队友剩 5 张送 Pair/ThreeWithTwo）；② `_group_consistency_filter` 场景 [队友] 下不拆对子（队友可能接）；③ 接风优先跟随队友线（Pair/Pomb）。| `src/v/nn/guards/v7_guards.py`（追加 R07-R09）、`ultimate_win_rate_engine_v7.py`（导入 `greaterPos`/`numofplayers` 上下文）、`grouping_engine.py`（`_group_consistency_filter` 队友场景） | **登记 2026-06-18**，**已实施+批跑 2026-06-19**。实施：3 文件改动 ~155 行，pytest 83/83 pass。批跑：3 局（6 局含诊断），队胜 0/6（0%），副胜 28/110（**25.5%**，环比 +18.5pp vs 前次 7.0%）。pytest 条件①已满足 ✓。队友保护大幅提升副胜率但局级仍 0——GUA-064 argmax collapse 仍是硬瓶颈。关单尚需：② 批跑日志证实队友控牌时 PASS 率变化；③ 证实不拆队友牌型。**相关**：GUA-031（M3 队友保护已建）、GUA-034（solo 冲刺）、GUA-036（接风队友配合） |
| GUA-067 | open | P1 | V-nn, v7, training, data | v7 | **训练数据 initial_hand 为贡前手牌，current_hand 推算错误** | `initial_hand` 来自 gameStart notify 的 `handCards`（发牌后、还贡前的 27 张）。`bc_dataset.py` 策略2 `current_hand = initial_hand - played_cards` 只能减不能加：已还走的牌多算、收到的贡牌少算 → 训练样本手牌特征偏移 | `v7_game_recorder.py`、`yf1_v7.py`、`yf2_v7.py`、`bc_dataset.py` | **登记 2026-06-18**。**已实施（GUA-067 联动 GUA-063）**：`adjust_initial_hand_for_tribute_back()` 在 4 条贡牌/还贡路径上实时调整 `initial_hand`：收贡→add、收还→add、进贡→remove、还牌→remove。修改文件：`v7_game_recorder.py`（新增方法）、`yf1_v7.py`/`yf2_v7.py`（各 2 处调用）。关单条件：批跑验证 `initial_hand` 张数 = 27（贡后）且不含已贡出牌。 |
| GUA-068 | **open** | **P0** | V-nn, v7, guard, policy | v7 | **V7-R11 全局抑制牌检查 + 节流：对手出不可压牌时的炸决策** | M3 `_Single()` 仅凭 `pass_num`/对手剩牌判断是否炸，**不知道全局还有多少压制牌未出**。场景：对手出级牌♠8，大王小王全未出 → M3 可能浪费炸弹；大王小王全出完 → 该炸不炸。V7-R11 通过 MemoryTracker 先查「已出 HR/SB/级牌/高点数」，再按剩余抑制牌数做节流。三阶段：①抑制牌≥2（还有人能压）→ 坚决 PASS；②抑制牌=1+没人PASS → PASS 等队友；③抑制牌=0（真无人能压）→ 允许炸弹 | `src/v/nn/guards/v7_guards.py`（+R11 + 3 辅助函数 ~200行）、`ultimate_win_rate_engine_v7.py`（+2行注入 `_memory_tracker`）、`tests/test_r11_integration.py`（6 case） | **登记 2026-06-19**。实施：`_rule_r11_unbeatable_card_throttle` + `_count_remaining_suppressors`（查 MemoryTracker 已出牌）、`_compute_pass_num`（从 recentPlays 计算当前轮 PASS 数）、`_is_single_beater`（掼蛋单张真实排序：HR>SB>级牌>A>K>…>2）。Guard 管道接入在 R05 之后 R01 之前。pytest 6/6 pass + 模块导入正常。与 M3 `_Single()` 关键区别：**有全局牌记忆**（大王出了几张/小王出了几张/级牌出了几张）。关单条件：批跑验证 R11 触发频率 + 副胜率影响。**综合批跑 2026-06-19**：9局108副，队胜0/9(0%)，副胜4/108(3.7%)，达A 20/108(18.5%)。R11独立触发频率待分析。**相关**：GUA-052（MemoryTracker）、GUA-045（V7 Guard 壳） |
| GUA-069 | **open** | **P0** | V-nn, v7, guard, policy | v7 | **超弱/助攻角色不应拆核心牌型：`_group_consistency_filter` 全放行 BUG** | yf2_v7 手牌含 4x4 炸弹（S4,S4,C4,D4），组牌引擎正确识别为炸弹且标记 `is_core=1.0`，但 `_score_power` 未对钢板计分 → power_score=1 → "超弱"角色 → `_group_consistency_filter` 中 `role in ("助攻","超弱")` 全放行所有动作 → C4 Single 未被过滤 → NN 选中 → 浪费唯一炸弹。**举一反三**：同样影响"助攻"角色下任何 core 牌型（炸弹/同花顺/顺子/三张）。**根因链**：① 钢板缺 power 记分 → 手牌被低估为"超弱"；② 超弱/助攻全放行绕过了 is_core 保护 | `ultimate_win_rate_engine_v7.py`（`_group_consistency_filter` 移除角色全放行）、`grouping_engine.py`（`_score_power` 钢板+1）、`tests/test_gua069_weak_role_core_protection.py`（8 case） | **登记 2026-06-19**。实施：① 移除 `role in ("助攻","超弱")` 前置全放行 → 所有角色走 `_action_breaks_core` 过滤；② `_score_power` 新增 `score += len(plan.steel_plates)` 防止钢板手牌被低估。pytest 8/8 pass + 回归 26/26 pass。关单条件：批跑验证超弱手牌下不再拆核心牌型。**综合批跑 2026-06-19**：9局108副，队胜0/9(0%)，副胜4/108(3.7%)，达A 20/108(18.5%)。超弱拆核心待独立回放验证。**相关**：GUA-063（组牌衔接 is_core 保护）、GUA-062（组牌引擎 v2） |
| GUA-070 | **open** | **P0** | V-nn, v7, guard, policy | v7 | **R12 拆对子出单禁制 + ThreeWithTwo对子is_core标记 + 小钢板减分移除** | 回放诊断发现三个联动问题：<br>**① 三带二吞噬对子 + is_core=0 导致拆对子静默放行**：`_detect_three_with_two` 贪心吞掉对子（如777-22），`to_card_mask` 将5张整体标记为 `is_core=0.0`，拆対出单（Single ♣2）不被 `_group_consistency_filter` 检测 → NN 选中 → 破坏结构化对子。<br>**② 有现成单张时不应拆对子出单**：`curRank=3` 时单2是最小单，NN 出 Single ♣2 为劣质动作。应新增 R12 规则：手中有自然单张时，禁止从对子中拆出单张。<br>**③ 钢板不管大小都是加分项**：`_score_power` 对小钢板（≤6）存在 -1 减分，用户确认应移除（钢板稀有牌型，不应减分）。<br>**已实施（2026-06-19）**：① `to_card_mask` ThreeWithTwo 拆分为 trip(is_core=1.0,gsize=3) + pair(is_core=1.0,gsize=2) 两个独立子组；② `_group_consistency_filter` 新增 R12 通用检查：Single动作取自 gsize=2 组 + `_has_any_natural_single()` → 过滤（覆盖普通对子 + ThreeWithTwo对子）；③ 移除 `_score_power` 小钢板 ≤6 减分逻辑。3 文件改动：`grouping_engine.py`（to_card_mask +8/-4行、_score_power -7行）、`ultimate_win_rate_engine_v7.py`（_group_consistency_filter +R12 +15行、_has_any_natural_single +25行）、`tests/test_gua069_weak_role_core_protection.py`（test_steel_plate_small_net_zero→net_positive）。pytest 81/82 pass（1预存BJ编码问题）。关单条件：① ✅ ThreeWithTwo对子 is_core=1.0 已被 `_get_broken_core_type` 检测；② ✅ `_has_any_natural_single` 正确区分自然单张/wild card；③ 待批跑验证 R12 触发频率与副胜率影响。**综合批跑 2026-06-19**：9局108副，队胜0/9(0%)，副胜4/108(3.7%)，达A 20/108(18.5%)。R12独立触发频率待分析。**相关**：GUA-063（is_core保护）、GUA-069（超弱core保护） |
| GUA-071 | **open** 🔴 | **P0** | V-nn, v7, architecture | v7 | **行牌启发式选择替代 NN argmax：纯 Guard 驱动决策管线** | **战略转向（2026-06-19）**：GUA-064 确证 BC argmax collapse 不可修复（温度采样/beam search 均治标），且 Guard 体系已证明有效（GUA-065 +18.5pp 副胜率）。决策：将 `_model_decision` 的 NN argmax 替换为 `_heuristic_select`，决策权完全移交给规则体系。【组牌-NN衔接设计】§九。实现方案：① 在 `ultimate_win_rate_engine_v7.py` 新增 `_heuristic_select(action_list, game_state)` 方法，按优先级选择（非PASS优先→同优先级取最小→角色/场景感知）；② `decide()` 中使用 `_heuristic_select` 替代 `_model_decision`；③ 保留 `bc_model_v3.pth` 加载路径，通过开关控制（`use_nn=False` 走启发式，未来 RL 就绪后切回）；④ pytest ≥10 case 覆盖常见局面。 | `ultimate_win_rate_engine_v7.py`（+`_heuristic_select` ~90行、`decide()` 改动 ~15行、`get_statistics()` +1行） | **登记 2026-06-19**（P0）。**已实施（2026-06-19）**：`_heuristic_select` 四条优先级规则（①队友控牌→PASS、②对手急眼→炸、③非PASS优先、④取正序最小节约牌力）。pytest 待补。**首批跑（2026-06-19）**：9局168副，队胜0/9(0%)，**副胜4/168(2.4%)**，达A 46/168(27.4%)。末级：2:28 / 3:6 / 4:4 / 5:24 / 6:8 / 7:6 / 8:16 / 9:8 / A:28 / J:12 / K:8 / Q:10 / T:10。⚠️ **副胜率 2.4% vs 关单基线 25.5%（GUA-065）→ 严重未达标**。vs GUA-066/068/069/070 综合批跑（3.7%）：-1.3pp 基本持平。达A 从 18.5% 升至 27.4%（+8.9pp）。**根因**：`_heuristic_select` 仅四条规则，无法替代 NN+Guard 叠加效果（GUA-065 的 +18.5pp 是在 NN 仍运行+Guard 校验叠加下达成的）。需要在 heuristic 中融入牌力评估/剩余牌数/炸数估计等 Guard 级智能。**关联**：GUA-064（argmax collapse 硬瓶颈）、GUA-065（队友保护基线）、组牌-NN衔接设计 §九（战略转向文档）。 |
| GUA-072 | **open** 🔴 | **P0** | V-nn, v7, policy | v7 | **规则记牌引擎 ~50行 + card_mask 退化保护：给 heuristic_select 喂信念输入** | 子项 A（card_mask 退化保护，**已实施 2026-06-20**）：① `_run_grouping_engine` except 升级记录 handCards/curRank/traceback；② `decision_context_from_act` 加 handCards 字段；③ `_basic_classify()` 降级炸弹识别；④ `_log_card_mask_degeneration_diagnostics()` 批跑诊断。4 文件 ~200行，零 lint。子项 B（规则记牌引擎，**部分实施**）：`RuleCardCounter` + `_inject_belief_vector` + `joker_signal`；出牌后 always-on 归属推断。**子项 C（贡牌/抗贡算王，已实施 2026-06-01）**：对齐 [`04_calculation_skills.md`](../knowledge/skills/04_common_skills/04_calculation_skills.md) §二.1 + [`06_game_flow.md`](../knowledge/rules/01_basic_rules/06_game_flow.md)——`MemoryTracker.record_anti_tribute` / `record_tribute_transfer` / `sync_tribute_phase_from_state`；进贡/还贡**转移**不再误标 `PLAYED`；双贡均级牌→四王归吃贡方、单贡 HR 分布、抗贡双 HR、出牌习惯（HR 对子→还有 SB、先 HR 后 SB）；`yf1_v7`/`yf2_v7` 注入 `tributeResult`/`antiPos`/`backResult`；pytest `tests/test_memory_tracker_tribute_joker.py`（11 case）。**仍 open**：批跑副胜率环比未达标；关单条件 ④ 待验。**子项 D（heuristic/中局消费 joker 归属，已实施 2026-06-01）**：`_joker_belief_from_state` + `_heuristic_select` 规则 ⑩⑪ + `_stage_mid_dispatch` / `_recommend_lead_impl` / `_recommend_min_press_impl` 消费 `hr_with_opponents`；pytest `tests/test_gua072_joker_heuristic_mid.py`（6 case）。 | `src/v/nn/features/memory_tracker.py`、`rule_card_counter.py`、`ultimate_win_rate_engine_v7.py`、`yf1_v7.py`、`yf2_v7.py`、`tests/test_memory_tracker_tribute_joker.py`、`tests/test_gua072_rule_card_counter.py`、`tests/test_gua072_joker_heuristic_mid.py` | **登记 2026-06-19**（P0），**子项A 2026-06-20**，**子项C/D 2026-06-01**。批跑验证 2026-06-20：card_mask 零退化。关单条件：① 规则记牌正确；② pytest ≥8 pass ✅；③ `_heuristic_select` ≥1 条用信念 ✅（子项D）；④ 批跑副胜率环比。**关联**：GUA-071、GUA-052、GUA-068（R11 抑制牌）、GUA-078/111（残局 `_seat_may_hold_*` 消费 `card_state` HR/SB）、GUA-098（`joker_signal` trace） |
| GUA-073 | open | **P1** | V-nn, v7, policy, knowledge | v7 | **docs/knowledge/skills 知识库接入：逐条映射 15 个技能文件到 V7 代码管线** | `docs/knowledge/skills/` 下 15 个 MD 文件含 ~400+ 条人类掼蛋经验知识（相生相克、牌语判断、残局策略、单张/对子/三带二/顺子/炸弹技巧等）。当前 V7 Guard+Heuristic 体系仅接入 ~30%（算牌/记牌/牌型结构保护），大量牌语判断、残局策略、传牌技巧未利用。需逐条标注：哪些可立即映射为 Guard 规则（硬过滤）、哪些可融入 heuristic_select 打分（软排序）、哪些需 V5+ 组牌引擎（中长期）。产出：`docs/guandan-brain/knowledge-integration-mapping.md`（知识接入映射表）。本 GUA 关单条件：① 映射表完成（15 文件 × 知识点 → 代码位置 + 接入粒度 + 优先级）；② P0 可接入项已拆分为独立 GUA；③ `_heuristic_select` 中至少使用 3 条映射表产出的打分规则。**关联**：GUA-030（PRINCIPLES_MAPPING 已做 foundation 三篇映射）、GUA-071（heuristic_select）、GUA-072（信念输入） | `docs/guandan-brain/knowledge-integration-mapping.md`（新建）、`v7_guards.py`（Guard 层新增规则）、`ultimate_win_rate_engine_v7.py`（heuristic_select 接入新打分维度） | **登记 2026-06-19**（P1）。拆分为 2 个子阶段：① 映射表文档（本任务）→ ② 按 P0 接入项拆分独立 GUA 实施。已完成阶段1的基础分析（15 文件已全读取），待输出映射表。 |
| GUA-074 | closed ✅ | P2 | V-nn, v7, architecture | v7 | **组牌方案数从 6-7 套缩减到 3 套，节约算力和时间** | `enumerate_groupings()` 返回 Top 3 方案（从 6-7 缩减为 3）；f[9] 归一化分母从 6.0 改为 3.0；`_enumerate_plans` 仍生成全部 6 策略以支持跨方案灵活性评分，截取在外部调用点 | `src/v/nn/features/grouping_engine.py`（`enumerate_groupings` 返回 `plans[:3]` + 文档注释 + f[9] 归一化） | **`closed_in` 2026-06-20**。改动 7 处（1 行返回值 + 6 处文档/归一化）。零 lint。**关联**：GUA-062（组牌引擎 v2） |
| GUA-075 | open 🔴 | **P0** | V-nn, v7, architecture, policy | v7 | **V7 决策架构改造：排除法→推荐法双路径** | V7 当前所有管线都在做排除法（Guard 砍掉错误候选项、NN 背 slot 位置答案），没有一条管线主动从合法候选中选出最优。改造方案：新增 `_recommend_play()` 推荐器（四场景策略：领出/打上家/卡下家/让对家）+ `_match_actionList()` 匹配器（三要素精确匹配）→ 主路径；推荐失败时走现有管线回退。目标：80%+ 决策步走主路径。方案文档：`V7-决策架构改造-从排除法到推荐法.md`。实施任务：① `_recommend_play()` 推荐器、② `_match_actionList()` 匹配器、③ `decide()` 双路径改造、④ pytest + 批跑验收 | `ultimate_win_rate_engine_v7.py`（+`_recommend_play` ~200行、+`_match_actionList` ~40行、`decide()` 改造 ~30行） | **登记 2026-06-20**（P0）。关单条件：① 新管道能跑通无异常；② ≥80% 决策步走主路径；③ 回退路径可用；④ 牌型选择不再受 slot 位置影响；⑤ 批跑胜率不低于现有基线。**关联**：GUA-063（组牌衔接）、GUA-071（heuristic 兜底）、GUA-064（argmax collapse）。**记录增强（2026-06-28）**：`decide()` 全部返回路径写入 `_last_decision_layer`（残局管线/GUA-075推荐/NN/NN+heuristic覆盖/启发式/规则回退/决策超时）+ `_last_decision_score` + `_last_decision_candidates`；yf1_v7/yf2_v7 读取写入 `record_decision`；`v7_game_recorder` 新增 `candidates_count` kwarg。pytest 24/24 pass。|
| GUA-076 | **closed** ✅ | **P0** | V-nn, v7, grouping, bug | v7 | **组牌方案完整性BUG：27/27方案均为非27张** | `enumerate_groupings()` 防御性 AssertionError 改为 warning + 剔除不完整方案。根因分析：1000 副随机手牌全部通过完整性检查，2026-06-20 统一新流水线重构可能已修复原丢牌根因。`AssertionError` → `warnings.warn()` + 自动剔除不完整方案（全不完整时 fallback 保留原 plans 防止空列表崩溃）。pytest 60/60 pass（50 随机手牌 + 10 统计/边界 case）。批跑 3 局无退化（card_mask 退化 0、GUA-076 warning 0）。 | `grouping_engine.py`（`enumerate_groupings` 完整性校验改为 warning + 剔除，±25行；+`warnings` 导入）、`tests/test_gua076_plan_completeness.py`（新建 60 case） | **`closed_in` 2026-06-21**。关单条件全部满足：① 根因已由重构修复（未定位到当前可复现丢牌路径）；② 1000 副随机手牌全部 27/27；③ pytest 60/60 pass；④ 批跑 3 局无退化。**关联**：GUA-062（组牌引擎 v2）、GUA-063（组牌衔接）、GUA-074（方案数量缩减）。 |
| GUA-077 | open 🟡 | **P1** | V-nn, v7, grouping, strategy | v7 | **精确拆牌规划：组牌引擎缺全局多轮规划维度** | `grouping_engine.py` 已做单轮静态拆牌（6 策略枚举 + 4 维评分 → 最优方案），但缺少**全局多轮规划维度**：不知道「这手出完之后下一手怎么走」，无法规划 2-4 轮的出牌序列。来源：guandan.nullkit.com 残局 advanced_01（10张精确拆牌两手三带二走完）、advanced_05（11张三手连环三带二）。核心缺失：① 无出牌序列规划（best_plan 给静态分组，不告诉决策管线第一手出什么/第二手出什么）；② 无手数间牌型衔接（如第一手三T带KK→留 AAA+66 天然三带二接上）；③ 无对手反制应对（出第一手后被炸弹截胡时 plan B）。当前 `_recommend_play`（GUA-075）仅做单步推荐，无法解决多步规划。关单条件：① `GroupingPlan` 新增 `play_sequence` 字段（有序出牌步骤列表）；② 至少 1 种策略枚举支持多轮序列输出（三带二连环规划优先）；③ pytest ≥6 case 覆盖 advanced_01/05 场景；④ heuristic/推荐器可消费 `play_sequence` 首步做决策引导。**关联**：GUA-062（组牌引擎 v2）、GUA-075（推荐法双路径 → 单步→多步自然演进） | `grouping_engine.py`（+`play_sequence` 字段 + `_plan_play_order()` ~80行）、`end position.md`（残局知识）、`tests/test_gua077_global_planning.py`（新建） | **登记 2026-06-21**（P1）。依赖 GUA-075 推荐管线稳定后接入（推荐器消费 play_sequence 首步）；不影响当前 GUA-075 批跑。**来源**：guandan.nullkit.com 残局题库技巧分析 |
| GUA-078 | open 🟡 | **P0** | V-nn, v7, policy, endgame | v7 | **V7 残局智能体独立管线（EndgamePreprocessor + EndgameDecider + 调试脚本）** | 新建三级残局管线，在 `decide()` 入口统一注入 `_endgame_context`，按 Q0→Q3 优先序执行残局决策。设计理念：<br>**① EndgamePreprocessor（上下文注入器）**：读取 `numofplayers`（由上游 `_inject_numofplayers` 已注入），计算四家角色：敌方（myPos±1）→ `endgame_rule`（1-10 张：danger_level + recommended_types + banned_types）+ `BAOSHU_RULE`（1-4 张强化封锁：likely_hand + block_with + never_play）；队友（myPos+2）→ `assist_prefer`（精确喂牌：1单/2对/3三张/4对/5顺或三带二）；自己（myPos）→ `should_sprint`（两手整牌+有炸→冲刺）。激活条件：任何一家玩家 ≤10 张。**不修改 actionList**，纯上下文注入。<br>**② EndgameDecider（四级决策引擎）**：读取 `_endgame_context`，按优先级链条执行：**Q0 自己冲刺**（should_sprint → 出最大整炸抢头游，"先炸后整"vs"先整后炸"按出牌权+敌方状态动态选）→ **Q1 封锁敌方**（敌人≤10 → banned 硬排除 + recommended 优先 + baoshu.block_with 备选 + L3 降级放宽禁令）→ **Q2 助攻队友**（teammate≤4 → assist_prefer 喂牌）→ **Q3 炸弹兜底**（非冲刺/封锁/助攻 → should_bomb 四象限判决：炸完能走+不炸必输/炸完能走但非必须/不炸必输但炸不走/炸不走+不会输）。任一命中返回 action，全未命中→交由上游 GUA-075/Guard/NN/heuristic 管线兜底。<br>**③ 大单张动态阈值**（`_resolve_big_single_threshold`）：根据 MemoryTracker 中剩余大牌数，K→Q→J 三级动态降级，避免误判"大单张"。<br>**④ 独立调试脚本**（`scripts/checks/check_endgame_agent.py` ~470行）：三种模式——独立模式（任意手牌+四家剩余→完整 Q0-Q3 trace）、扫描模式（扫 game_records_v7 → 激活率+Q0-Q3命中率统计）、单记录模式（指定文件+步→残局快照）。扫描结果（32 记录/756 决策点）：激活率 42.1%，Q1=65，Q2=89，命中率 48.4%。Q0/Q3 扫描未命中因 mock actionList 无组牌引擎输出，实战胜率会正常激活。关单条件：① 残局管线接入 V7 `decide()` 主决策路径（在 Guard 之前、GUA-075 主路径之前）；② 批跑验证 Q0/Q1/Q2/Q3 实战触发；③ 残局命中后的副胜率 ≥ 未命中基线；④ 不误判不误炸（大单张阈值正确）；⑤ 独立调试脚本可运行 | `src/v/nn/endgame/endgame_preprocessor.py`（~500行，上下文注入器）、`src/v/nn/endgame/endgame_decide.py`（~669行，Q0-Q3 决策引擎）、`scripts/checks/check_endgame_agent.py`（~470行，独立调试脚本） | **登记 2026-06-21**（P0）。三级管线（预处理器+决策器+调试脚本）已完成编码，`scripts/checks/check_endgame_agent.py --scan` 验证通过。**管线已在 `decide()` line 222 接入**（懒导入+Q0→Q3决策链），但实战静默的根因是 `_inject_numofplayers` 回退为 [27,27,27,27] → enemies={} → Q1 跳过。**2026-06-21 已修复**（GUA-079 层③）：回退时读 `publicInfo.rest`。**关联**：GUA-075（推荐法双路径 → 残局管线应在推荐法之前插入）、GUA-052（MemoryTracker 供大单张动态阈值）、GUA-063（组牌引擎供 should_sprint 判定）、GUA-065（队友保护与 Q2 助攻喂牌联动） |
| GUA-079 | open 🔴 | **P0** | V-nn, v7, diagnosis | v7 | **2026-06-21 回放诊断：V7 全阶段决策系统性崩坏（单牌/组牌/残局三层根因）** | **批跑**：202606211659，3局32副，队胜0/3(0%)，副赢0/32(0%)，残局命中31.1%(90/289)。**回放局1**（20260621165937）：<br><br>**① 单牌选择倒置（步6/12/16）**：yf1 有单A→出大王压♦8（步6）；yf2 有单A→拆K对出K压♦10（步12）；yf2 有大小王→对手出A时PASS（步16）。**根因**：`_heuristic_select`（GUA-071）仅4条元规则，无「单牌最小压制」概念——不知道A应优先于K、K优先于王。输入缺失牌力锚点（需GUA-072规则记牌或牌力评分），输出缺失压制力排序。<br><br>**② 拆炸凑压 + 组牌锁死（步32/34）**：yf2有6张10+4张3→拆两炸凑三带二（步32）；yf1有4张Q+3张2+对子4→拆Q炸+2三张出三带二（步34）。理想方案：Q三张+对4，但**对子4已被组牌引擎锁入其他牌组**→决策引擎无自由对可用→被迫拆2。**根因**：组牌引擎 `to_card_mask` 死锁牌张到组后，决策引擎无法临时「借调」已锁对子；`_group_consistency_filter` 不阻止拆核心牌型（GUA-069/070修复范围未覆盖的场景）。<br><br>**③ 残局管线静默（步57→74）**：对手剩1张时yf2有4J炸弹→PASS（步61）；对手出单逼近头游→yf2有王不出（步65）；对手剩1张→yf2出K留王→yf1有A过牌→对手A压K获二游（步72→74）。**根因**：GUA-078 残局管线已编码但**未接入 `decide()` 主路径**，实战中完全不生效。`_heuristic_select` 4条规则中无「对手剩1张必须压」规则。残局激活扫描（289激活点/31.1%命中）也证实：69%激活窗口无任何推荐。<br><br>**致败链条**：开局浪费大王→中局消耗炸弹→残局无牌可打+管线静默→0/32副。**修复优先级**：P0-① 将 GUA-078 残局管线接入 `decide()`（遏制残局爆炸性溃败）；P0-② `_heuristic_select` 新增「最小压制」规则（不跳过A）；P0-③ 组牌引擎增加「临时借调」API 或决策侧支持覆盖 `card_mask` | `ultimate_win_rate_engine_v7.py`（heuristic_select + decide + card_mask）、`grouping_engine.py`（to_card_mask锁死）、`endgame/`（未接入） | **登记 2026-06-21**（P0，三层根因诊断）。**2026-06-21 17:37 已修复层③（残局管线静默）**：根因非「GUA-078 未接入 decide()」（管线已接入 line 222），而是 `_inject_numofplayers()` 在 MemoryTracker 未初始化时回退为 `[27,27,27,27]`，导致 EndgamePreprocessor 认为所有对手有 27 张 → enemies={} → Q1 直接 `return None`。修复：回退时从 `publicInfo[i].rest` 读取平台实时剩张数（`_inject_numofplayers` ±7行）。验证：对手 1 张 + 手牌有炸 → Q1 立即命中出炸弹。**层①②待修复**。**关联**：GUA-071（heuristic_select 仅4条规则）、GUA-078（残局管线接入→实为 numofplayers 数据问题）、GUA-069/070（拆核心牌型保护范围不足）、GUA-072（规则记牌未上线→无牌力锚点）、GUA-075（推荐法双路径未完成→无「最小压制」择优）。**回放原文**见 `replay_word.md` 2026-06-21 |
| GUA-080 | open 🟡 | **P1** | V-nn, v7, grouping, policy | v7 | **组牌：GUA-072 拆炸时序 + scanner 降级观测** | **① 拆炸押后**（已编码）。**② R-G080-4**（2026-06-28 **closed**）：3+9 局批跑零 scanner/card_mask 降级。**③ 组牌参数冻结**（2026-06-28）：9 局副胜 3.4%，**禁止**再调 `grouping_engine` 权重/拆炸/枚举；瓶颈转决策链 GUA-075/078/072。 | `grouping_engine.py` | **登记 2026-06-22**。完成定义 [[GUA-080-completion]]。**不关单**（副胜率未达标）。**关联**：GUA-054/061/062/072、GUA-079② |
| GUA-081 | open 🔴 | **P0** | V-nn, v7, policy, data | v7 | **三带二推荐拆炸 fallback + 贡还手牌验证同步** | **A** 回放 `20260628091707150272` 步7：GUA-075 推荐 888+22 拆 8 炸 → mask 拦截 → 回退 NN 出整炸。**最小修复**：`_build_three_with_two_press` 跳过部分拆 bomb/同花顺 core 的三张，试下一档（如 999+对）。**B** `adjust_initial_hand_for_tribute_back` 只改 `initial_hand`，`_validate_action_cards` 读 `all_players_hands` → 还贡后 S8×2 误报。**修复**：贡还 add/remove 同步 `all_players_hands`。 | `ultimate_win_rate_engine_v7.py`（`_build_three_with_two_press`）、`v7_game_recorder.py`（`adjust_initial_hand_for_tribute_back`） | **登记 2026-06-28**。完成定义 [[GUA-081-completion]]。**A/B 代码已实施**；关单待回放步7不再 Bomb/8 + 批跑无 S8 验证 WARN。**关联**：GUA-075、GUA-067、GUA-080（组牌冻结） |
| GUA-082 | open 🟡 | **P0** | V-nn, v7, policy | v7 | **R12 跟单不拆对（主路径 + heuristic 回退）** | **A** 回放 `20260628091707150272` 步23：有散 J 压 Single/7，主路径拆 10 对 → `_collect_single_follow_candidates` + `_quick_guard_validate`。**B** 回放 `20260628091703972753` 步64：有自然单 CK 却拆对 9 出 S9 → `_heuristic_select` 误把拆对单张当组局一致（+10000）且 PASS 同享该加分。**修复 B**：`_is_group_consistent` 拒单张 gsize>1；R12 −20000 + 选优跳过；PASS 不参与组局 +10000。 | `ultimate_win_rate_engine_v7.py` | **登记 2026-06-01**；**2026-06-01 续**：pytest `test_gua075_r12_main_path.py` 5/5 + `test_gua082_r12_heuristic_fallback.py` 5/5；关单=构造态 pytest（replay 仅锚点）。**关联**：GUA-070/075/081 |
| GUA-083 | open 🔴 | **P0** | V-nn, v7, policy, observation | v7 | **GUA-075 `_ensure_valid` 失败误走「无同型 PASS」+ 录牌 actionList 抽样** | **A** 回放 `20260628091707150272` 步53 yf2：引擎推荐 `Pair/A`，`_ensure_valid` 匹配失败后落入 R11 synthetic PASS，未进 `decide()` 回退。**修复**：③跟上家/④卡下家（含改炸）在 `impl` 非 None 且 `_ensure_valid` 失败时 `return None`；真无同型（`impl` None）仍 R11 PASS。**B** `decision_context_from_act`：`actionList_size≤8` 写入 `actionList_sample`；匹配失败 WARNING 含抽样。 | `ultimate_win_rate_engine_v7.py`（`_recommend_play`）、`v7_game_recorder.py`（`summarize_action_list_for_context`） | **登记 2026-06-01**。pytest `test_gua083_ensure_valid_fallback.py` + `test_v7_action_list_context.py`。**A/B 代码已实施**；关单待 WF-12 步53 复验（牌谱含 `actionList_sample`）。**关联**：GUA-075/081/082、WF-12 |
| GUA-084 | open 🟡 | **P1** | V-nn, v7, grouping, policy | v7 | **五星炸保护 + SF 分支 BOMB_FIRST 候选 + 三带二禁吃炸对** | 回放 `20260628091704590941`：五星 10 在 `SF_FIRST`+`break_bombs=True` 整炸进 singles → 777/999 三带二各配一对 10。**已实现 R-G084-1/2/3**（`grouping_engine.py`）。 | `grouping_engine.py` | **登记 2026-06-01**。完成定义 [[GUA-084-completion]]。pytest `test_gua084_bomb_core_protect.py` 10/10；关单待 3 局 R-G080-4 零退化。**关联**：GUA-080/072/063、PB-001 |
| GUA-085 | open 🟡 | **P0** | V-nn, v7, policy | v7 | **回退 NN 路径 actIndex 错位 + 领出拆 SF/炸核单张** | 回放 `20260628091704590941` 步1（**发现锚点**，批跑不可复现同副）：`flt_map[model_idx]` 错位 → TWT/K 拆 H8。**修复**：内容回查 + 领出保核。**关单**：pytest 构造态 [[GUA-085-completion]] + R-G080-4；**禁止**「再跑同副首出」伪关单。 | `ultimate_win_rate_engine_v7.py` | pytest 4/4 ✅；待 R-G080-4。**关联**：GUA-075/084/064、WF-12 |
| GUA-090 | **open** | **P0** | V-nn, v7, architecture, policy | v7 | **`stage_0_1` 开局入口 `_stage_open_plan()`：从“最小合法牌”改为“开局意图驱动”** | 当前 `dispatch_by_stage` 已给 `21-27` 张打上 `stage_0_1` 标签，但开局阶段长期共用 `_recommend_play()` 的通用领出逻辑，导致出现“合法但无阶段意图”的高耗损领单（级牌/高单）和助攻场景不够主动让道。**本轮已实施最小版本（2026-06-30）**：在 `ultimate_win_rate_engine_v7.py` 中新增 `_stage_open_plan()`，由 `stage_0_1` 优先接管开局阶段；当前已覆盖三类意图：① 队友控牌 → `PASS` 让道；② 有低耗损散单时优先试探单张；③ 若只剩高耗损单张（按当前单张强度阈值判定，覆盖 K/A/级牌/王），优先安全小对开路，而不是直接领高耗损单张。 | `src/v/nn/ultimate_win_rate_engine_v7.py`、`tests/test_gua090_stage_open_plan.py` | **登记 2026-06-30**。当前状态：构造态实现 + pytest 已过；**待关单**：① 净盘批跑验证 `stage_0_1` 不再频繁领高耗损单张；② 如仍出现开局无阶段意图领出，再补“跟线/保回收”子意图；③ `GUA-098` 接入后在 trace 中可见 `stage_0_1` 入口命中。**关联**：GUA-075、GUA-089、GUA-101 |
| GUA-091 | **open** | **P0** | V-nn, v7, architecture, policy | v7 | **`stage_2` 中局入口 `_stage_mid_dispatch()`：统一消费 `_belief + _phase_relation + role + greaterAction`** | 当前 `stage_2` 只是标签，真正决策仍退回 `_recommend_play()` 的四场景通用逻辑，导致中局无法显式回答“什么时候该卡关键敌方、什么时候该节牌力压上家、什么时候该让队友 lane 持续、什么时候该为残局保炸”。**本轮最小闭环目标（2026-06-30）**：在 `ultimate_win_rate_engine_v7.py` 中新增 `_stage_mid_dispatch()`，作为 `stage_2` 的统一入口；它不再直接堆牌例特判，而是先吃 `_belief`（剩张 / 炸弹风险 / 当前控牌压制可能性）与 `_phase_relation`（关键敌方、牌型粗分类、队友承接、同型压制外部性），再复用现有 `lead / min_press / max_press / bomb` 基元落动作。当前已覆盖四类中局意图：① 队友控牌时让道保 lane；② 领出时按 role / 队友承接 / 关键敌方剩张决定试探；③ 跟上家时在“节牌力换手权”和“切关键敌方”之间切换；④ 卡下家/关键敌方时在“最大同型压制”和“残局保炸后改炸兜底”之间切换。 | `src/v/nn/ultimate_win_rate_engine_v7.py`、`tests/test_gua091_stage_mid_dispatch.py` | **登记 2026-06-30**。**停手条件**：`stage_2` 不再只是 `_current_stage` 标签，而是实际入口；新增构造态 pytest 至少覆盖“队友控牌让道 / 卡关键下家 / 上家场景节牌力 / 无同型时关键敌方改炸”四类；本步仍允许底层复用 `GUA-075` 的动作基元，但**禁止**新增按牌例直出 actIndex 的中局特判。**后续**：① 接 `GUA-098` 把 `stage_2 intent` 落到 trace；② 跑 `GUA-097` 至少 1 组 baseline/enable 对照。**关联**：GUA-075、GUA-089、GUA-094、GUA-097、GUA-098、GUA-101 |
| GUA-094 | **open** | **P0** | V-nn, v7, architecture, inference | v7 | **规则版推断层 `_inference_phase_relation` 最小闭环：先把中局可消费的标签/概率生产出来** | 当前 V7 已有 `MemoryTracker` 与 `_belief`，但还缺「Layer 1 事实 → Layer 2 推断 → stage_2 中局入口」之间的显式中转层，导致中局仍只能吃零散规则，无法统一消费“敌方剩牌型粗分类 / 队友承接能力 / 同型压制是否还在外面 / 炸弹风险”。**本轮最小闭环目标（2026-06-30）**：① 在 `rule_card_counter.py` 实现 `infer_phase_relation(game_state)`，先输出不改 actIndex 的软标签/概率；② 在 `ultimate_win_rate_engine_v7.py::decide()` 早期把结果注入 `game_state['_phase_relation']`；③ 补构造态 pytest，验证关键字段产出稳定；④ 为后续 `GUA-091 stage_2` 和 `GUA-098 trace` 预留统一消费入口。此步只做“生产推断结果 + 注入上下文”，**不**直接改中局出牌索引。 | `src/v/nn/features/rule_card_counter.py`、`src/v/nn/ultimate_win_rate_engine_v7.py`、`tests/test_gua094_phase_relation.py` | **登记 2026-06-30**。**停手条件**：`game_state['_phase_relation']` 至少稳定产出 `critical_enemy_seat / enemy_shape_hint / teammate_cover_confidence / same_type_suppressor_outside / enemy_bomb_risk_max` 五类信号；pytest 覆盖“敌方粗分类 / 队友承接 / 当前控牌同型压制外部性 / 注入成功”四个方向；关单前还需按 `GUA-097` 至少跑 1 组 baseline/enable 对照。**下游**：**GUA-119** 助攻真 2.1 消费 `teammate_frequent_type`（或等价字段）。**关联**：GUA-072、GUA-089、GUA-091、GUA-097、GUA-098、GUA-101、**GUA-119** |
| GUA-096 | **open** | **P0** | V-nn, v7, infra, governance | v7 | **净盘后强制落盘 `v7-win-rate-history.md`（治理层护栏）** | V 系列 6 个月无对战 KPI（`v4v5v6-lessons-2026-06.md` §0）：训练指标完整，对战胜率 0 数据。新增 `scripts/hooks/post_batch_log.py`：读 `batch_executor/latest_victory_num.json` + 数 `game_records_v7/*.json` → 算 0+2 vs 1+3 队胜率 → 追加一行到 `v7-win-rate-history.md`。vn 不可读退出码 1（强制人工补登）；失败不抛异常。 | `scripts/hooks/post_batch_log.py`、`docs/guandan-brain/v7-win-rate-history.md` | **登记 2026-06-29**。pytest `tests/test_gua096_097_098_v7_infra.py::test_gua096_post_batch_log_syntax` ✅。**关单**：首条自动落盘产生 + §七.11 第 5 项预测（未来 4 周 ≥8 次净盘批跑）。**关联**：GUA-097/098、§七.11 |
| GUA-097 | **open** | **P0** | V-nn, v7, infra, ablation | v7 | **IP 规则对照批跑 helper（机械化「加规则不测」教训）** | V7 §七.9 计划 IP-01~IP-21 共 21 条，叠加风险 = V 老路（M3 GUA-036 副胜率 78.8%→52.2%）。新增 `scripts/hooks/ip_ablation_runner.py`：IP_REGISTRY 注册 21 条 + 4 模式（baseline / enable --ip-id / diff --ip-id / list）+ 联动 GUA-096 落盘 + 自建 `iterations/v7-gua097-ablation-log.md`。 | `scripts/hooks/ip_ablation_runner.py`、`docs/guandan-brain/iterations/v7-gua097-ablation-log.md` | **登记 2026-06-29**。pytest `test_gua097_ip_registry_complete` + `test_gua097_list_mode_runs` 2/2 ✅。**关单**：21 条 IP 至少 5 条跑 baseline + enable 双跑（GUA-094 落地前提）。**关联**：GUA-094/096、§七.6 预测 2、§七.11.3 反推 1 |
| GUA-098 | **open** | **P0** | V-nn, v7, infra, tracing | v7 | **决策溯源日志 `DecisionTracer`（机械化 V5 静默失败教训）** | V5 因 lalala 路径错静默退到兑底——代码都在，决策是兑底，没人发现（`v4v5v6-lessons-2026-06.md` §4.1+§8.1）。新增 `src/v/nn/tracing/decision_trace.py`：6 方法（`begin_step` / `record_layer1` / `record_layer2` / `record_guard` / `end_step` / `flush_to_jsonl`）写 `game_decision_traces/{game_id}.jsonl`；`enable=False` 全 no-op 不阻塞主决策。 | `src/v/nn/tracing/decision_trace.py`、`src/v/nn/tracing/__init__.py`、`game_decision_traces/` | **登记 2026-06-29**。pytest 4 case 全 PASS（basic / multi_step / disabled / flush）。**关单**：GUA-089/090/091/092 阶段化决策入口接入 tracer + `game_decision_traces/` 落盘非空。**关联**：GUA-089/090/091/092/094/013、§七.11.3 反推 2 |
| GUA-099 | **open** | **P0** | V-nn, v7, policy, endgame | v7 | **残局 Q1 报单放走：`PASS` 排序错误 + 平台级牌配牌类型误判** | WF-12 回放 `20260630082350363704`：`69/79` 时 `@3` 已从 `Pair/T` 打到只剩 1 张，`yf2` 手牌明明有可压 `Pair/2=[S2,H2]`，却在 **残局管线 Q1** 直接 `PASS`。根因两层：**①** `src/v/nn/endgame/endgame_decide.py::_get_cards` 把 `["PASS","PASS","PASS"]` 视作 3 张牌，导致 `_sort_by_recapture_first()` 在 Q1“任意 non_banned”兜底时把 `PASS` 排到对子候选前；**②** `src/v/nn/guards/v7_guards.py::get_action_type` 对平台格式 `[type, rank, [cards]]` 一律递归看实牌，致 `["Pair","J",["CJ","H2"]]` 这类级牌配对子被误判成 `Free`，削弱同型识别/残局封锁。**修复方向**：`PASS` 视为空动作；平台格式对 `Single/Pair/Trips/ThreeWithTwo/Straight/ThreePair/TwoTrips` 优先信声明牌型，仅 `Bomb/StraightFlush` 继续按实牌校验以保留 R13 假炸过滤。 | `src/v/nn/endgame/endgame_decide.py`、`src/v/nn/guards/v7_guards.py`、`tests/test_gua078_endgame_tracker_decide_entry.py`、`tests/test_gua071_action_format.py` | **登记 2026-06-30**。回归目标：对手剩 1 张、我有合法同型可压时，Q1 不得再把 `PASS` 选到前面；级牌配对子平台动作必须识别成 `Pair`。**关联**：GUA-078、GUA-079、GUA-083、WF-12 |
| GUA-100 | **open** | **P0** | V-nn, v7, policy, endgame | v7 | **残局 Q1 规则表自洽：推荐/封锁配置不得互相打架** | WF-12 继续分析 `20260630082350346668/63704` 的 `76/79` 发现：当 `@1` 出 `Single/9(H9)` 且实际只剩 3 张时，`yf1/yf2` 都命中 **残局管线 Q1** 后直接 `PASS`。进一步定位到 `src/v/nn/endgame/endgame_preprocessor.py::endgame_rule`：`remaining=3` 配置为 `recommended_types=['单张','对子']`，但 `banned_types=['Single','Trips']`，先天自相矛盾，`apply_banned_filter()` 会把所有合法单张先删光。相同冲突还出现在 `remaining=1`（`['最大单张']` vs `['Single']`，且 `BAOSHU_RULE[1].never_play=['Single']` 也会继续删光单张）和 `remaining=5`（`['Pair','Trips','大单张']` vs `['Single']`）；后续静态校验还暴露 `BAOSHU_RULE[4]` 曾存在 `block_with=['Pair', ...]` 与 `never_play=['Pair']` 的死配置。**2026-06-30 续修**：异常扫描器进一步暴露 `recommended_filtered_to_pass_only` 子类，根因是 **secondary enemy 的 banned 并集** 会删掉主目标 `recommended_types`，以及 **当前敌方控牌同型** 会被残局表硬禁（如 `remaining=2` 的 Pair、`remaining=3` 的 Trips）。现已在 `endgame_decide.py` 中把“主目标 recommended/block_with”与“当前敌方 greaterAction 同型”提升为 **protected types**，secondary enemy / `never_play` 不得删掉这些类型。**继续收敛 2026-06-30**：剩余主类 `enemy_critical_pass_with_legal_beater` 复查后确认已被当前代码修复，残留 20 条只是旧牌谱的历史 `chosen_action=PASS`。异常扫描器现改为“**当前代码重放后仍 PASS 才报**”，不再报历史旧账；同批 70 份 `game_records_v7` 现扫出 **0 条** 未收敛异常。 | `src/v/nn/endgame/endgame_preprocessor.py`、`src/v/nn/endgame/endgame_decide.py`、`src/v/nn/endgame/endgame_anomaly_scanner.py`、`scripts/checks/check_q1_rule_table_consistency.py`、`scripts/checks/check_endgame_anomalies.py`、`tests/test_gua078_endgame_tracker_decide_entry.py`、`tests/test_gua100_q1_rule_table_validator.py`、`tests/test_gua100_endgame_anomaly_scanner.py` | **登记 2026-06-30**。关单目标：① `endgame_rule` 的推荐/禁用配置不再互斥；② `BAOSHU_RULE` 的 `block_with/never_play` 不再互斥；③ pytest 参数化覆盖 `remaining=1..6`；④ `apply_banned_filter()` 在推荐单张的 `1/3/5` 场景不再把 `Single` 全删光；⑤ 新增静态 validator 与异常扫描器作为批跑前护栏；⑥ `recommended_filtered_to_pass_only` 聚类清零；⑦ `enemy_critical_pass_with_legal_beater` 当前代码重放归零。**关联**：GUA-078、GUA-099、WF-12、PB-002 |
| GUA-101 | **open** | **P0** | V-nn, v7, architecture, policy, planning | v7 | **开局/中局专用大脑最小落地总控：按 `GUA-090 → GUA-094 → GUA-091` 顺序推进** | 最新净盘批跑 `3局/19副` 仅 `1副胜`，且牌谱统计显示主路径已不再是 NN：`GUA-075推荐=531`、`残局管线=318`、`NN(+覆盖)=17/910`。说明主问题已前移到**开局/中局没有独立大脑**，不是残局显性 bug，也不是旧 NN 主路未退干净。现状缺口：`dispatch_by_stage` 已写入 `_current_stage`，但 `stage_0_1` / `stage_2` 仍无专用入口，只有 `stage_3` 接到 `EndgameDecider`；中局虽有 `_belief` 注入，但尚未形成「推断层 → 中局专用决策器」闭环。**最小实施清单**：**Step 1 / GUA-090**：实现 `_stage_open_plan()`，让 `stage_0_1` 从“最小合法牌推荐”升级为“开局意图驱动”（试探/保回收/让道/安全小对），产出 `intent + recommendation`，并要求首批构造态回归覆盖“开局不应高耗损领单 2/A/K/王”与“助攻角色优先让道/跟线”；**Step 2 / GUA-094**：实现推断层最小闭环 `_inference_phase_relation`，先走规则版 IP，不等 NN，至少输出：敌方剩牌型粗分类、队友承接能力、同型压制是否仍在外面、炸弹风险，且要求中局决策器只能消费概率/标签，不能直接改 actIndex；**Step 3 / GUA-091**：实现 `_stage_mid_dispatch()`，专管 `stage_2` 的攻守切换、控权、保对家、临时偏离组牌，输入必须吃 `MemoryTracker + _belief + role + greaterAction`，禁止继续把 `_recommend_play()` 当成中局通用大脑。 | `src/v/nn/dispatcher.py`、`src/v/nn/ultimate_win_rate_engine_v7.py`（`decide` / `_recommend_play` / 新增 `_stage_open_plan` / `_stage_mid_dispatch`）、`src/v/nn/features/memory_tracker.py`、`src/v/nn/features/rule_card_counter.py`、`tests/`（新建 `test_gua090_*`、`test_gua094_*`、`test_gua091_*`） | **登记 2026-06-30**。**停手条件 / 关单口径**：① `GUA-090` 完成后，`stage_0_1` 不再直接走通用 `_recommend_play()` 领出逻辑，且新增回归可稳定拦住高耗损单张领出；② `GUA-094` 完成后，中局可读到规则版推断结果，且 `GUA-097` 至少完成 1 组 baseline/enable 对照；③ `GUA-091` 完成后，`stage_2` 不再是标签而是实际入口，能基于 `belief` 做攻守切换；④ `GUA-098` 随 090/091 接入生产 `decide()`，落盘 `game_decision_traces/` 非空；⑤ 三步全部完成后，再跑净盘 3 局看副胜是否仍长期低于 `10%`，若仍低于则该现有方案进入重新评估。**关联**：GUA-075、GUA-078、GUA-089、GUA-094、GUA-095、GUA-097、GUA-098、docs/analysis/v7-kpi-vs-m3-vs-lalala-2026-06-29.md §7.13 |
| GUA-102 | **open** | **P0** | V-nn, v7, policy, architecture, midgame, grouping | v7 | **前中段“整牌冲刺点火”意图：当 `group_type_map` 完备度极高时，允许从“最小跟单”切到“主动开炸抢节奏”** | WF-12 回放 `20260630151011373323` 步 `24/99`：对手 `@3` 出 `♣2` 后，`yf1` 已接近整牌冲刺态，牌面结构接近“多炸 + 两组三带二 + 一对 + 少量残单”，理论上可通过主动开炸切走单张回合，进入“三火三轮”冲刺头游；但当前主路径仍在 `stage_0_1` 直接走 `GUA-075` 的最小跟单，推荐 `Single/9`，没有显式的“整牌完备度高 → 主动点火炸弹”意图。这里要解决的不是“无同型时是否允许改炸”，而是**在仍可最小同型跟牌时，是否因为整牌冲刺态已成熟而主动改用炸弹抢节奏**。**首轮收敛口径**：① 基于 `group_type_map + role + bomb_count + natural_turn_count + singles_residue` 识别 `sprint_fire_ready`；② 仅在主攻、手持多炸、自然轮次短、残单少、且主动开炸可明显把牌局从散单互跟切入整牌冲刺时，允许炸弹优先级盖过“最小合法单压”；③ 不允许写死某个点数牌（如 K），必须消费组牌/记忆/推断层给出的结构信号。**与旧规则边界**：`GUA-068/R11` 的“无同型/长期拿不到牌权时改炸”规则仍保留且已在起作用，但它解决的是**被动改炸**；本条新增的是**主动点火**，不能混为一谈。 | `src/v/nn/ultimate_win_rate_engine_v7.py`、`src/v/nn/features/rule_card_counter.py`、`tests/test_gua090_stage_open_plan.py`、`tests/test_gua091_stage_mid_dispatch.py` | **登记 2026-06-30**。**停手条件**：① 能稳定产出 `sprint_fire_ready` 或等价结构信号；② `stage_0_1`/`stage_2` 至少一处实际消费该信号，而不是继续无差别最小跟单；③ 新增回归覆盖“整牌完备度高时主动开炸”与“非冲刺态仍保持节炸”两侧；④ `GUA-098` trace 中可见该意图命中，能与 `GUA-068/R11` 的被动改炸路径区分。**关联**：GUA-068、GUA-075、GUA-090、GUA-091、GUA-094、GUA-098、GUA-101、WF-12 |
| GUA-103 | **open** | **P0** | V-nn, v7, policy, endgame | v7 | **残局 Q1：bomb-like 封锁候选必须优先最小足够成本，禁止无收益升炸 / 多耗逢人配** | WF-12 回放 `20260630155538336842`：`39/79` 对手 `@1` 出 `♠K ♥K ♣K ♠B ♠B` 后剩 1 张，`40/79` `yf2_v7` 明明已有 `Bomb/6=[♠6 ♥6 ♣6 ♦6]` 足以压制，却在 **残局管线 Q1** 选择了 `Bomb/6=[♠6 ♥6 ♣6 ♦6 ♥3]`，无收益多耗 1 张。继续回放 `20260701175356865029`：`49/79` 对手 `@3` 出 `Bomb/Q=[♥Q ♥Q ♣Q ♦Q]`，`50/79` `yf1_v7` 的 `actionList` 已有纯 `5星 Bomb/5=[♠5 ♥5 ♥5 ♣5 ♣5]` 可压，但 Q1 仍选择 `6星 Bomb/5=[♠5 ♥5 ♥5 ♣5 ♣5 ♥K]`，多耗红桃级牌 `♥K` 逢人配。根因不在“候选缺失”，而在 Q1 候选排序复用“回收优先→张数多优先”，且首轮修复只覆盖了“对方非炸牌型”，漏掉“对方也是炸弹 / 同花顺”的 bomb-like 通道。**5 问准入结论**：① 这是一类局面：Q1 已决定用 bomb-like 封锁且平台给出多个合法可压候选时，应选成本最低的足够候选；② 阶段 A 先落为 Q1 `hard safety constraint`，后续迁到统一评分器 `min_sufficient_cost` 候选约束；③ 属 P0 止血，避免无收益损炸、损逢人配；④ 验证闭环为构造态回归 + trace 可见 `actionList` 与选中项；⑤ 迁移出口为统一评分层的“控制收益 - 炸弹/逢人配消耗成本”。**收敛口径**：Q1 对 bomb-like 候选内部按“张数少优先、少耗逢人配优先、同张数牌点小优先”排序；**2026-06-01 收窄**：拆纯点数整炸留同点孤张时（WF-12 `48445203` 步58）优先完整整炸，仍保留四炸+逢人配节炸语义。 | `src/v/nn/endgame/endgame_decide.py`、`tests/test_gua078_endgame_tracker_decide_entry.py` | **登记 2026-07-01；扩口 2026-07-01**；**2026-06-01 续**：`_bomb_splits_pure_rank_leaving_orphan`。**停手条件**：① `TWT/K`→纯四炸；② 对炸→纯五星；③ **新增** 步58 五星不得拆四炸留单5；④ 不影响 GUA-078/099/100/104/105/107/110/111 回归。**关联**：GUA-078、GUA-099、GUA-100、WF-12、[`V7-架构演进与新增规则准入治理.md`](V7-架构演进与新增规则准入治理.md) |
| GUA-104 | **open** | **P0** | V-nn, v7, policy, endgame | v7 | **残局 Q1：队友已控牌时，不得再用同花顺/更大炸反压队友** | WF-12 继续分析同副回放 `20260630155538336842`：`40/79` `yf2_v7` 已用 `Bomb/6=[♠6 ♥6 ♣6 ♦6 ♥3]` 控牌，`41/79` 对手 `@3` PASS 后，`42/79` `yf1_v7` 却又在 **残局管线 Q1** 出 `StraightFlush/3=[♦3 ♦4 ♦5 ♦6 ♦7]` 反压队友。根因两层：**①** `_q1_hold_teammate_max_control()` 先信 `get_action_type(greaterAction)`，而平台声明型 5 炸 `['Bomb','6',['S6','H6','C6','D6','H3']]` 当前会被 guard 判成 `Free`，导致“队友已控牌”停手逻辑直接短路；**②** Q1 现有逻辑没有把“队友已控牌时，bomb-like 反压动作一律禁止”做成硬约束。**首轮收敛口径**：① `greaterAction` 的队友控牌识别对平台声明型 `Bomb/StraightFlush` 走声明优先，不再因 guard 返回 `Free` 漏判；② 只要队友已控牌，Q1 不得再选 `StraightFlush` / 更大 `Bomb` 这类 bomb-like 反压动作；③ 原有“队友已用最大单张/对子/三带二控牌时 PASS”逻辑继续保留。 | `src/v/nn/endgame/endgame_decide.py`、`tests/test_gua078_endgame_tracker_decide_entry.py` | **登记 2026-07-01**。**停手条件**：① 构造态回归覆盖“队友 4 炸控牌”和“队友 5 炸控牌（平台声明型、guard 误判 `Free`）”两类场景；② 两类场景下，另一名我方 Q1 都必须 `PASS`，不得再用同花顺/更大炸反压队友；③ 不影响既有 GUA-078/103 回归。**关联**：GUA-078、GUA-099、GUA-103、WF-12 |
| GUA-105 | **open** | **P0** | V-nn, v7, policy, endgame, memory | v7 | **残局 Q1：敌方剩 1 张且我方领牌时，先整牌锁敌，再看安全单；不得再机械领 K** | 同副 WF-12 回放 `20260630155538336842` 继续暴露第二层根因：`42/79` `yf1_v7` 先用同花顺反压队友后，`46/79` 又在 **残局管线 Q1** 明知 `@1` 只剩 1 张时领出 `Single/K=[♠K]`，随后 `47/79` 被敌方单张级牌 `3` 反压拿走头游。这里不是“残局管线没启动”，而是 **Q1 启动了，但 `remaining=1` 仍硬套 `recommended_types=['最大单张']`**，只按“大单张阈值”筛单张，既不判断这张单是否真的外部无压制，也不比较“手里已有顺子/对子/三带二等整牌型，是否能直接锁死报单敌人”。**首轮收敛口径**：① `enemy rest=1` 且我方领牌时，Q1 顺序改为“先找能直接锁住报单敌人的整牌型，再看单张”；② 若没有整牌型、只能出单时，再结合 `MemoryTracker` 判断是否存在“当前外部无压制”的安全单；③ 若连安全单都没有，也不得退回去机械领 `K`，至少应改领当前最强单张，而不是把 `K` 误当控牌。 | `src/v/nn/endgame/endgame_decide.py`、`tests/test_gua078_endgame_tracker_decide_entry.py` | **登记 2026-07-01**。**停手条件**：① 构造态回归覆盖“有顺子可锁敌时，不得再优先领 `K/A/王/级牌` 单张”；② 覆盖“无整牌型时，若有安全单则出安全单；若无安全单，也不得再退回去领 `K`”两类场景；③ 不影响既有 GUA-078/103/104 回归。**关联**：GUA-078、GUA-100、GUA-103、GUA-104、WF-12 |
| GUA-106 | **open** | **P0** | V-nn, v7, policy, architecture, midgame, memory | v7 | **`stage_2`：敌方报尽单张且队友尾位大概率可接时，不得抢先主动开炸/改炸** | WF-12 回放 `20260630155538336842`：`47/79` 对手 `@1` 出 `Single/3=[♦3]` 后已报尽，`48/79` `yf2_v7` 却不是 PASS 让尾位队友承接，而是走 **`GUA-075推荐` / `stage_2 mid_sprint_fire_bomb`** 直接出 `Bomb/9=[♠9 ♠9 ♥9 ♦9]`。现象上看像“队友手里还有大王却不信记忆模块”；更精确地说，是当前 `stage_2` 只消费了粗粒度 `teammate_cover_confidence`，没有回答**当上家敌方用最后一张单牌报尽时，尾位队友是否大概率仍有更大单张可接**。这会导致主动点火炸弹和 `mid_bomb_cutoff` 在“我方明明还有尾位承接窗口”时过早抢权。**首轮收敛口径**：① 在 `GUA-094 phase_relation` 新增 `teammate_rear_single_cover_confidence`，基于 MemoryTracker 的未出高张分布 + 队友/另一敌方剩张做概率估算；② 当 `greaterPos` 为上家敌方、该敌方已报尽单张、且 `teammate_rear_single_cover_confidence` 足够高时，`stage_2` 必须先 `PASS` 保留尾位队友承接窗口；③ 该停手同时拦住 `mid_sprint_fire_bomb` 与 `mid_bomb_cutoff` 两条炸弹路径，避免“主动点火”与“残局封口”抢在队友前面。 | `src/v/nn/features/rule_card_counter.py`、`src/v/nn/ultimate_win_rate_engine_v7.py`、`tests/test_gua094_phase_relation.py`、`tests/test_gua091_stage_mid_dispatch.py` | **登记 2026-07-01**。**停手条件**：① `phase_relation` 可稳定产出 `teammate_rear_single_cover_confidence`；② 构造态回归覆盖“上家敌方报尽单张、我方无同型、仅有炸弹、队友尾位大概率可接”时必须 `PASS`，不得再 `mid_sprint_fire_bomb`；③ 不影响既有 `GUA-091/094/102` 回归。**关联**：GUA-091、GUA-094、GUA-102、WF-12 |
| GUA-107 | **open** | **P0** | V-nn, v7, policy, endgame, memory | v7 | **残局 Q1：敌方极短残局单张通道要“领出先锁整牌，跟牌只准安全单反超队友”** | WF-12 继续分析同副 `20260630155538336842` 暴露出 Q1 在“单张通道”上的两处断层：**A** `62/79`，敌方仅剩 2 张时 `yf1_v7` 手牌已可组 `Straight/3=[3,4,5,6,7]` 直接锁死跟牌窗口，却仍在 **残局管线 Q1** 先领 `Single/4=[S4]`；**B** `63/79` / `78/79`，队友已领单、关键敌方在我后手时，`yf2_v7` 要么在**没有任何安全单**时拆 `77` 反超队友出 `Single/7`，要么在 `7/8/Q` 三个单里不选唯一安全的 `Q`，仍选最小 `7`，最终让报单敌人 `Single/J` 走掉。根因不是组牌引擎没产出，而是 Q1 缺了两层顺序：① **`enemy rest<=2` 且我方领牌**时，没有先看“整牌型能否直接锁死跟牌窗口”；② **队友领单且关键敌方在我后手**时，没有把“仅当前外部无压制的安全单才能反超队友，否则 PASS”做成硬约束，仍退回通用 `recommended=Single` + `recapture-first` 排序，导致拆对/选小单。 | `src/v/nn/endgame/endgame_decide.py`、`tests/test_gua078_endgame_tracker_decide_entry.py` | **登记 2026-07-01**。**停手条件**：① `enemy rest=2` 且我方领牌、有顺子/对子/三带二等整牌可直接锁死窗口时，Q1 必须优先整牌，不得再先领单；② 队友领单、关键敌方在我后手且剩 `1/2` 张时，若我方无安全单则 `PASS`，不得拆对/拆三反超队友；③ 若存在安全单，Q1 只能选安全单，不得再按 `recapture-first` 选更小伪控牌单；④ 不影响既有 GUA-078/100/103/104/105 回归。**关联**：GUA-078、GUA-100、GUA-105、WF-12 |
| GUA-108 | **open** | **P0** | V-nn, v7, grouping, architecture, scoring | v7 | **组牌引擎应枚举“拆低价值炸换顺子降手轮”候选，至少覆盖 `yf1` 这类 4 单可借一炸成顺的场景** | WF-12 + WF-05 联合复盘 `20260701151948500035`：`yf1_v7` 进入 `play` 后真实手牌为 `H2 C2 C2 D2 H3 D3 S4 H4 C4 D4 S5 C5 D5 S7 C7 D8 C9 DT HJ CJ CJ DJ HQ SK SK CK HA`，当前 `grouping_engine` 只枚举出 **4炸 + 三带二 + 对子 + 4单** 的方案：`2222 / 4444 / 555A / JJJJ / KKK+33 / 77 / D8 C9 DT HQ`，`check_grouping_engine.py` 给出的 best plan 分数为 **0.4667**。但基于同一套牌，存在更优的“拆低价值炸换顺子降手轮”候选：`D8 C9 DT HJ HQ` 组成顺子，保留 `2222 / 4444 / 555A / KKK+33 / 77`，余 `CJ CJ DJ` 为散牌；按当前引擎同一套评分函数重算，总分 **0.4987 > 0.4696**，且手轮 **9 < 10**。这说明当前问题不是“出牌器没选到更好方案”，而是 **grouping_engine 根本没把这类候选枚举出来**。按 [`V7-架构演进与新增规则准入治理.md`](V7-架构演进与新增规则准入治理.md) 5 问审查，本条属于**一类局面**（4 张连续散单可通过拆低价值炸补成顺子、以牺牲局部炸弹换更低轮次），默认落点应是 **`grouping_engine` 的候选枚举 / 评分层**，**禁止**直接写成“好牌就拆炸组顺”的动作补丁。**边界样本**：同副 `yf2_v7` 的 `5炸+1对+5单` 结构，按同一评分函数测试，顺子重组总分 **0.5004 < 0.5246**，说明本条不是“逢可成顺必拆炸”，而是要把该类候选**枚举出来并交给评分器比较**。 | `src/v/nn/features/grouping_engine.py`、`scripts/checks/check_grouping_engine.py`、`tests/test_gua108_grouping_straight_from_low_value_bomb.py` | **登记 2026-07-01**。**停手条件**：① `grouping_engine` 至少能枚举出“4 单连续 + 可借低价值炸一张/两张补成顺子”的候选，不再只给保炸方案；② 新增回归稳定覆盖 `yf1` 这类场景，要求顺子降轮次候选的总分高于当前保炸 baseline；③ 同时新增反例回归覆盖 `yf2` 这类 `5炸` 结构，要求引擎仍可判原始重炸方案更优，防止规则退化成“逢顺必拆炸”；④ 该修复的落点限于 **候选枚举 / 评分**，不得直接改 `actIndex` 或在 `stage_0_1/stage_2` 写牌例特判。**关联**：GUA-062、GUA-063、GUA-074、GUA-075、GUA-077、GUA-102、WF-05、WF-12 |
| GUA-109 | **open** | **P0** | V-nn, v7, grouping, architecture, scoring, midgame | v7 | **被动同型压制时，必须让“保未来结构”与“拆低价值结构换当前夺权”候选并存，再由统一评分决定是否牺牲 `KKK` / 低价值炸去形成更高顺子** | WF-12 继续复盘 `20260701151948500035`，暴露出一类比 GUA-108 更本质的矛盾：全局 `8/91`，对手 `@1` 出 `Straight/6=[D6,D7,S8,H9,CT]` 时，`yf1` 真实手牌仍持有 `C9,DT,HJ,CJ,CJ,DJ,HQ,SK,SK,CK` 等牌，**人类高频会选择拆 `J` 炸/拆 `K` 三张，组成 `Straight/9=[C9,DT,HJ,HQ,SK]` 去压 `6-10`**；但当前 V7 同时又在后续全局 `12/91` 的 `ThreeWithTwo/J` 场景里表现出另一面：`KKK+33` 并不是被牌规锁住，而是被当前“组牌核心结构优先 + 同型候选识别”链路**等效锁住**，导致系统更倾向保留 `KKK+33` 这类未来结构，而不是在当下为夺权牺牲它。这里的关键不再是单点“顺子候选漏枚举”或单点“KKK 没识别成同型”，而是**两套互斥未来计划没有被同时摆上台面比较**：方案 A 保 `KKK+33 / JJJJ`，当前对 `Straight/6` 选择 `PASS`；方案 B 拆低价值炸/拆低价值三张，当前出 `Straight/9` 抢回牌权，但牺牲后续 `ThreeWithTwo/K` 等结构。**新增锚点 2026-07-02**：WF-05/牌谱 `20260702195833037993` 进一步确认，问题不仅出现在“被动跟顺是否拆 `KKK/JJJJ`”，也出现在**组牌阶段内部的候选并存缺失**：当前 `grouping_engine` 会先在 `_detect_three_with_two()` 中把 `JJJ` 贪心配 `66`、把 `KKK` 贪心配 `22`，随后才进入 `_detect_straights()`，导致本来合法且牌理更顺的竞争方案 `KKK+22 + JJJ+QQ + Straight 6-10(C6,D7,H8,D9,ST)` 根本没有进入候选集与 `JJJ+66 + KKK+22 + QQ` 对比。也就是说，现状不只是“评分层没选中拆结构方案”，而是**pair 分配过早贪心，先把顺子关键张静默吃掉，候选并存都没建立起来**。按 [`V7-架构演进与新增规则准入治理.md`](V7-架构演进与新增规则准入治理.md) 5 问审查，本条不应再落成“看到 6-10 就特判出 9-K”之类动作补丁，而应落到 **候选层 + 统一评分层**：① 在被动同型压制，尤其是 `Straight/ThreeWithTwo/ThreePair` 这类结构牌型时，候选层必须允许“保未来结构”与“拆低价值结构换当前夺权”并存；② 评分层要结合 `role / enemy criticality / teammate_cover / natural_turn_count / single_residue / sprint_fire_ready / _belief + _phase_relation` 比较“当前夺权收益”与“未来结构损失”；③ 对组牌阶段共享关键张（如某个 `pair` 既可喂给 `trip` 又是 `straight` 关键张）的场景，也必须允许“保三带二”与“保顺子”两套计划并存，而不是在 `_detect_three_with_two()` 阶段先做不可逆贪心；④ 只有当当前夺权收益显著高于未来结构损失时，才允许牺牲 `KKK`、低价值炸或某个 pair/straight 关键张去形成更高顺子/三带二/连对。**与 GUA-108 边界**：GUA-108 解决的是“候选根本没枚举出来”；GUA-109 解决的是“即使候选都枚举出来，也不能再靠局部 core 保护先验把其中一边静默裁掉，必须交给统一评分比较”。本次 `20260702195833037993` 则进一步把边界前移到：**不能让 `three_with_two` 的 pair 贪心在候选层就提前杀死顺子竞争方案**。 | `src/v/nn/features/grouping_engine.py`、`src/v/nn/ultimate_win_rate_engine_v7.py`、`src/v/nn/features/rule_card_counter.py`、`tests/test_gua108_grouping_straight_from_low_value_bomb.py`、`tests/test_gua091_stage_mid_dispatch.py` 或新增 `tests/test_gua109_competing_future_plan_scoring.py` | **登记 2026-07-01；增补锚点 2026-07-02**。**停手条件**：① 至少在被动跟顺场景中，系统能同时产出“PASS 保 `KKK+33`”与“拆低价值结构出 `Straight/9` 抢权”两套候选，而不是只保留其一；② 新增回归稳定覆盖本副 `8/91` 这类“当前同型夺权 vs 未来 `ThreeWithTwo/K` 结构”冲突场景；③ 新增回归覆盖 `20260702195833037993` 这类“`pair` 既可组成 `ThreeWithTwo` 又是 `6-10` 顺子关键张”场景，要求候选层至少同时保留 `JJJ+66` 与 `JJJ+QQ + Straight 6-10` 两套竞争计划；④ 落点必须以 **候选并存 + 统一评分** 为主，不得直接写成具体牌例特判；⑤ `GUA-098` trace 中可观测到两套竞争计划及最终取舍理由。⑥ **2026-07-06 组牌子步（partial）**：THREE_PAIR_FIRST + STRAIGHT_BEFORE_TWT 竞争分支；334455（test_gua109_grouping_three_pair_first.py 3/3）与 6-10 顺子（test_gua109_grouping_straight_before_twt.py 3/3）。**待**：中局统一评分。 **关联**：GUA-075、GUA-091、GUA-094、GUA-098、GUA-102、GUA-108、WF-05、WF-12、[`V7-架构演进与新增规则准入治理.md`](V7-架构演进与新增规则准入治理.md) |
| GUA-110 | **open** | **P0** | V-nn, v7, policy, endgame, memory, sprint | v7 | **残局 Q0/Q1：两手冲刺优先整牌型；语义手数合并子结构** | 原锚点 `KKK+77+Q` 已落地。**2026-07-10 续作（WF-12 `06463698` 步49）**：① `_has_two_clean_hands` 按语义手数合并钢板/三连对/TWT 子组（Bomb+2×steel=2 手）；② Q0 自由领出用 `_is_my_q1_lead_turn` + `prefer_structure_first`，优先整组 TwoTrips/ThreePair/TWT，禁半组 Trips；③ 炸作回手。 | `endgame_decide.py`、`endgame_preprocessor.py`、`tests/test_gua078_*` | **登记 2026-07-01**；续作 2026-07-10。**关联**：GUA-116、WF-12 |
| GUA-111 | **open** | **P0** | V-nn, v7, policy, endgame, memory, same-type-lane | v7 | **残局 Q1：同型通道领出/续出时，必须评估“压后通道归属”；若我方无同型接力且敌方仍可能持有更大同型，不得机械续出次级同型** | WF-12 继续复盘 `20260701151948500035` 的 `80/91→83/91→85/91`。真实圈况是：`79/91` 对手 `@3` 出 `Pair/9`，`80/91` `yf1_v7` 用 `Pair/K=[SK,CK]` 压住；`81/91` 队友 `@2` `PASS`、`82/91` 对手 `@3` `PASS` 后，`83/91` `yf1_v7` 重新领出 `Pair/7=[S7,C7]`；`84/91` 队友继续 `PASS`，`85/91` 对手 `@3` 立刻用 `Pair/8=[H8,C8]` 抢回对子通道，随后还能继续走更大对子冲刺。问题不在 `Pair/K` 这一下是否可压，而在于 **Q1 在拿到对子牌权后，没有评估“我方对子通道还能否连续冲完”**：此时 `yf1` 剩牌是 `77 + Q`，队友仅剩两张且从牌路看不是对子接力者，而敌方 `@3` 明显仍在对子通道中；系统却仍把局面机械落成 `Q1 封锁敌方: type=Pair`，继续把 `Pair/7` 打出去，相当于把对子牌权重新送回给仍可能持有更大对子的一方。按 [`V7-架构演进与新增规则准入治理.md`](V7-架构演进与新增规则准入治理.md) 5 问审查，这条不应收敛成“对子 2 最大记忆”之类窄补丁，而应落成 **Q1 同型通道归属判断 + 记忆模块消费**：当我方领出或续出 Single/Pair/Trips 等同型通道时，必须结合 `MemoryTracker + _belief + _phase_relation` 评估“队友是否有同型接力”“敌方外部是否仍有更大同型”，若我方无接力而敌方仍有更大同型，则不得机械续出次级同型。 | `src/v/nn/endgame/endgame_decide.py`、`src/v/nn/endgame/endgame_preprocessor.py`、`src/v/nn/features/rule_card_counter.py`、`tests/test_gua078_endgame_tracker_decide_entry.py` | **登记 2026-07-01**。**停手条件**：① 构造态回归覆盖“我方 `KK/Q/77`，队友仅两张非对子，敌方在对子通道仍可能有更大对子”场景，Q1 在 `Pair/K` 拿权后不得再机械领 `Pair/7`；② 规则需泛化到同类 Single/Pair/Trips 通道，不得只写死对子；③ 记忆/算牌输出至少能参与判断“同型外部压制是否仍存在”与“队友是否有同型接力”；④ 不影响既有 GUA-105/107/110 回归。**关联**：GUA-078、GUA-094、GUA-100、GUA-105、GUA-107、GUA-110、WF-12、[`V7-架构演进与新增规则准入治理.md`](V7-架构演进与新增规则准入治理.md) |
| GUA-112 | **open** | **P0** | V-nn, v7, policy, endgame, sprint, safety | v7 | **残局 Q1：自方一手清牌优先，不得拆完整手牌** | WF-12 复盘 `20260701175340219689`（回放显示为 `2026070117540213098`）的 `77/96→80/96→83/96` 与 `94/96→96/96`，确认当前残局管线存在更底层的“清手候选优先级缺失”：`yf2_v7` 在 `80/96` 领出 `Straight/3=[S3,S4,S5,S6,C7]` 后仅剩 `HJ,DJ,HA`，平台候选里已明确存在 `Trips/J=[HJ,DJ,HA]` 一手清牌，但 `83/96` Q1 仍选 `Single/A=[HA]`，被对手 `Single/R=[HR]` 压制；同副后段 `yf1_v7` 在 `94/96` 明明持有三张 `6` 可一手 `Trips/6` 出完，却先拆成 `Pair/6=[S6,D6]`，再在 `96/96` 留 `Single/6`。这不是组牌引擎未产出，也不是牌规不允许，而是 **Q1 没有把“当前候选可一次性覆盖全部手牌”作为最高优先级的 finish-now 约束**。按 [`V7-架构演进与新增规则准入治理.md`](V7-架构演进与新增规则准入治理.md) 5 问审查，本条是可复用残局不变量：当轮到我方可行动且存在非 PASS 候选，其牌张 multiset 等于当前 `handCards` multiset 时，应优先一手清牌；不得拆完整手牌改成多手。 | `src/v/nn/endgame/endgame_decide.py`、`tests/test_gua078_endgame_tracker_decide_entry.py` | **登记 2026-07-01**。**准入结论**：通过 5 问；一类局面而非单副现象，首轮落点为 Q1 hard safety constraint，后续迁移到统一评分器的 `finish_now` 候选约束。**停手条件**：① 构造态回归覆盖 `HJ,DJ,HA` 必须选 `Trips/J`，不得先出 `Single/A`；② 构造态回归覆盖三张同点牌必须一手 `Trips` 出完，不得拆成 `Pair + Single`；③ 规则不得写死点数或花色，只比较候选牌张 multiset 与当前手牌 multiset；④ 不破坏队友已控牌让道、Q1 反压队友停手、GUA-103/104/105/107/110/111 既有回归。**关联**：GUA-078、GUA-100、GUA-105、GUA-107、GUA-110、GUA-111、WF-12、[`V7-架构演进与新增规则准入治理.md`](V7-架构演进与新增规则准入治理.md) |
| GUA-113 | **open** | **P0** | V-nn, v7, policy, endgame, safety, teammate | v7 | **残局 Q1：超弱/助攻在队友已控牌时，不得用非 bomb-like 结构反压队友** | WF-12 复盘 `20260702195833037993` 步 **66/84**：`yf2_v7` 步 64 出 `ThreeWithTwo/T` 控牌后，`yf1_v7`（role=**超弱**，9 张）在 **残局管线 Q1** 仍出 `ThreeWithTwo/K=[KKK+66]` 抢队友控牌；步 67–69 三家全 PASS，证明帮挡无必要，随后 yf1 剩 4 张散牌丢权。根因：`_q1_hold_teammate_max_control()` 因三带二 T 未达静态阈值 12 且记牌推断“敌或可压”而返回 `None`，Q1 把唯一非 PASS 候选当“封锁敌”打出；残局管线 bypass R07，且 R07 `numofmy≤10` 冲刺例外亦不会让道。**5 问准入结论**：① 一类局面；② 阶段 A 落 Q1 hard safety，迁移至 `intent=yield_to_teammate` + 评分器抢权成本；③ P0 反压队友；④ 构造态 + trace；⑤ 迁移出口已写明。**首轮收敛口径**（已实现 2026-06-01）：`_q1_hold_teammate_max_control()` 在 GUA-104 bomb-lane 之后，若 `game_state['_role']` 为 `超弱/助攻` 且平台有 PASS → 直接 PASS；`decide()` 注入 `_role` 供 Q1 消费。不改 R07 阈值、不改记牌推断。 | `src/v/nn/endgame/endgame_decide.py`、`src/v/nn/ultimate_win_rate_engine_v7.py`、`tests/test_gua078_endgame_tracker_decide_entry.py` | **登记 2026-06-01**。**停手条件**：① 构造态锚点 66/84：role=超弱、队友 T 三带二控牌 → Q1 必须 PASS；② 同构造态 role=主攻 仍可出 `ThreeWithTwo/K` 帮挡；③ 不影响 GUA-104/112 回归。**关联**：GUA-065、GUA-078、GUA-104、GUA-109、WF-12、[`V7-架构演进与新增规则准入治理.md`](V7-架构演进与新增规则准入治理.md) |
| GUA-114 | **open** | **P1** | V-nn, v7, policy, GUA-075 | v7 | **三带二 min 压带牌 orphan 规避：优先独立对子，避免从 ≥3 张同点抠对** | WF-12 复盘 `20260701175356193021` 步 **37/105**（配对副 `[36]-[2]`）：@1 出 `ThreeWithTwo/4` 后，`yf2_v7`（role=超弱，20 张，`curRank=Q`）在 **GUA-075 min 压** 出 `ThreeWithTwo/T=[ST,ST,DT,S3,H3]`，把 **现成对子 `H4 H4` 留着**，却从 **三张 3** 抠对作带牌，留下 **孤张 `D3`**。根因：`_build_three_with_two_press(..., strategy="min")` 内 `_find_available_pair()` 对带牌做 **全局最小 rank 排序**，未区分「独立对子（`card_mask` gsize=2 / 剩余恰 2 张）」与「从 ≥3 张同点抠 2 张」。**5 问准入结论**：① **一类局面**（被动三带二 min 压选带牌，不限步 37）；② **条件准入**：落 **GUA-075 builder 候选约束**（与 GUA-081 同级），**非** stage_2 特判；③ **非 P0 止血**（结构次优，非送头游/反压队友）；④ 验证闭环 = 构造态 pytest + WF-12 锚点 + 批跑 R-G080-4；⑤ **迁移出口** = 阶段 C 统一评分器 `natural_pair_kicker_bonus` / `split_triplet_orphan_penalty`，builder 约束后续降级。**首轮收敛口径**（已实现 2026-07-03）：`strategy=="min"` 时带牌池优先 **natural pair**（剩余恰 2 张同点，或 `card_mask` 两张均 gsize=2）；仅当无 natural pair 时才从 ≥3 张同点取 2 张；`strategy=="max"` 不变。 | `src/v/nn/ultimate_win_rate_engine_v7.py`（`_build_three_with_two_press` / `_find_available_pair`）、`tests/test_gua114_three_with_two_kicker_orphan.py` | **登记 2026-07-03**。**停手条件**：① 构造态：`333+44+TTT` 压 `TWT/4` 必须 `T+44` 非 `T+33`；② 反例：仅 `333+TTT` 无独立对子时仍可从三张取对；③ 不写死 3/4/10 等点数；④ 不影响 GUA-081 拆炸 skip 回归；⑤ 关单前净盘批跑 **R-G080-4 零退化**。**关联**：GUA-070、GUA-075、GUA-081、GUA-109、WF-12、[`V7-架构演进与新增规则准入治理.md`](V7-架构演进与新增规则准入治理.md) |
| GUA-115 | **open** | **P0** | V-nn, v7, policy, endgame, safety, baoshu | v7 | **残局 Q1：敌剩 4 张时火不打四（禁我方 Bomb/SF 压四张敌）** | WF-12 复盘 `20260703214948445203` 副 **28** 步 **51/75**（`[28]-[2]`）：步 **50** @3 领 `ThreeWithTwo/6`（5 张）→ @3 **剩 4 张**；步 **51** `yf1_v7`（role=**主攻**，13 张，`curRank=A`）**残局 Q1** 出 `Bomb/8` 压 TWT/6；步 **54** @3 **`Bomb/K`** 反压。常识 **火不打四/炸不压四**：敌剩 4 张极可能为四炸，不宜小炸换大炸。**根因**：① `_will_lose()` 已写「rem=4 → 炸不压四」，**Q1 未消费**；② `BAOSHU_RULE[4]` 过滤后 `block_with=['Bomb']`，**推动 Q1 选炸**（与 M1/V5「火不打四，观察或放给对家」相反）。**5 问准入结论**：① **一类局面**（主敌 `remaining==4` 被动跟压，不限本副）；② **阶段 A hard safety**：`endgame_decide._q1_block_enemy` + **`BAOSHU_RULE[4]` 对齐**；③ **P0 无收益炸损**；④ 构造态 pytest + WF-12 锚点 + R-G080-4；⑤ 迁移至评分器 `bomb_vs_four_card_enemy_cost` / intent `observe_four_card_enemy`。**首轮收敛口径**（已实现 2026-07-03）：主敌 `remaining==4` 且跟压敌方控牌时，Q1 `_q1_gua115_fire_no_bomb_four_pass` 仅 bomb-like 可压则 **PASS**；`BAOSHU_RULE[4]`：`Bomb`/`SF` 进 **`never_play`**、移出 `block_with`；例外：GUA-112 finish-now 优先 / `_will_lose` 真且 `has_two_clean_hands`。 | `src/v/nn/endgame/endgame_decide.py`、`src/v/nn/endgame/endgame_preprocessor.py`、`tests/test_gua115_fire_no_bomb_four.py` | **登记 2026-07-03**。**停手条件**：① 构造态：敌 rem=4、greater=TWT、有炸+PASS → PASS；② 锚点 `48445203` 51/75；③ R-G080-4 零退化；④ BAOSHU 与 Q1 一致。**关联**：GUA-078/112/113、M1 `endgame_strategy`、WF-12、[`V7-架构演进与新增规则准入治理.md`](V7-架构演进与新增规则准入治理.md) |
| GUA-117 | **open** 🟡 | **P0** | V-nn, v7, policy, assist, guard, teammate | v7 | **助攻/超弱出牌：Layer2 领出 + Layer0 B1–B6 guard + §6-7 pytest（方案 A 关单）** | 设计真源 [`助攻出牌-阶段划分设计口径.md`](助攻出牌-阶段划分设计口径.md)。**已落地**：Layer2 领出 + Q1 + 路由；**117-2a–2g** `guards/assist_layer0_guard.py` 接入 `filter_action_list`；**117-7a–7f** pytest bundle **86/86** ✅。**§6 #1–#7 齐套**，待正式关单登记。**非领出** → 075/091/Q1 + Guard。 | `stage_assist_feed.py`、`assist_prefer_table.py`、`guards/assist_layer0_guard.py`、`v7_guards.py`、`ultimate_win_rate_engine_v7.py`、`tests/test_gua117_*.py` | **登记 2026-07-05**；**实现 2026-07-06**。见 [[GUA-117-completion]]。**v1 后序**：GUA-118–121。**关联**：GUA-065/069/091/113、GUA-094 |
| GUA-116 | **open** 🟡 | **P1** | V-nn, v7, policy, main_attack, lead | v7 | **主攻领出整牌型：P1–4 + P3b/c/d** | P1–4 已落地。**2026-07-10**：① 整组可领 is_core；② **P3b TwoTrips / P3c ThreePair / P3d 天然 Trips**（炸回手）；③ 自由领出 filter 禁半组钢板 Trips。整牌型=单/对/顺/TWT/三张/钢板/三连对。 | `stage_main_attack_lead.py`、`ultimate_win_rate_engine_v7.py`、`tests/test_gua116_*` | **登记 2026-07-06**；续作 2026-07-10。[[GUA-116-completion]]。**关联**：GUA-110 |
| GUA-118 | **open** | **P2** | V-nn, v7, policy, assist, teammate | v7 | **助攻跟队友领牌：§三 L81「顺最小散牌」（117-P2）** | 设计 [`助攻出牌-阶段划分设计口径.md`](助攻出牌-阶段划分设计口径.md) §4.3.3 **L81**：领牌方=队友时，要领为 **顺最小散牌**（单 5–9 / 小对 33–77），其余 PASS。**现码**：stage_0/1/2 `_recommend_play` / `_stage_mid_dispatch` / Q1 GUA-113 等路径对助攻/超弱 **统一 PASS**，v1 117-2g 让权仍 PASS 即可。**落点**：**不新函数**；在 `_recommend_play()` **队友领牌分支**（及 091 让道分支若仍 PASS-only）扩展选牌：有点数序最小合法 Single/Pair 时出，否则 PASS。**不挡 GUA-117 v1 关单**。 | `src/v/nn/ultimate_win_rate_engine_v7.py`（`_recommend_play` 队友分支）、`tests/test_gua118_assist_l81_follow.py`（新建） | **登记 2026-07-05**。**停手条件**：① 构造态：role=助攻、队友控单/对、手有 5–9 单或 3–7 小对 → 出最小散牌非 PASS；② 无合法小散牌 → PASS；③ 不破坏 GUA-113/117 guard 回归。**关联**：GUA-117（117-2g）、GUA-065/091/113、[`助攻出牌要领.txt`](../archive/skill/助攻出牌要领.txt) |
| GUA-119 | **open** | **P1** | V-nn, v7, policy, assist, inference | v7 | **助攻 stage_2 真 2.1 同款送牌（117-P1-094）** | **Q3 拍板**：v1 `stage_assist_feed` 在 **无 GUA-094** 时 **S1-1 回落**；真 **2.1 同款**（常单/常对/三带/短顺等）待 **`GUA-094` `teammate_frequent_type`（或等价字段）** 产出后，替换 `_feed_mid_match_fallback` / 硬编码回落。**依赖**：GUA-094 关单或至少稳定输出队友高频牌型。**不挡 GUA-117 v1**。 | `src/v/nn/stage_assist_feed.py`、`src/v/nn/features/rule_card_counter.py`、`tests/test_gua119_assist_feed_21_match.py`（新建） | **登记 2026-07-05**。**停手条件**：① `_phase_relation` 含队友高频牌型信号；② 构造态：stage_2 助攻领出按同款优先（非仅 S1-1）；③ pytest + 117 领出回归不退化。**关联**：**GUA-094**（前置）、GUA-117、GUA-091、[`助攻出牌-阶段划分设计口径.md`](助攻出牌-阶段划分设计口径.md) §2.1 |
| GUA-120 | **open** | **P1** | V-nn, v7, policy, assist, grouping | v7 | **助攻约束 3 / B4：V5 `group_filter` 硬约束（117-P1-gf）** | **Q2 拍板**：v1 **117-2d = `v7_guards` 软禁**（`_action_breaks_core` / `is_core`）；**P1+** 在 V5+ **`group_filter` 层**对 role=助攻/超弱 做 **拆完整配套三带/顺/连对** 的硬约束（须结合队友高频牌型 / GUA-094）。**不挡 GUA-117 v1**。**建议顺序**：GUA-094 → GUA-119 → 本项。 | `src/v/nn/guards/v7_guards.py`、V5+ group_filter（待路径）、`tests/test_gua120_assist_group_filter.py`（新建） | **登记 2026-07-05**。**停手条件**：① 与 117-2d 软禁行为一致或更严，且有独立 pytest；② 批跑 R-G080-4 零退化。**关联**：GUA-117（117-2d）、GUA-069、GUA-094/119 |
| GUA-121 | **open** | **P1** | V-nn, v7, policy, assist, guard, midgame | v7 | **助攻 P1 续包：2.2 炸送同款、B7/B8、R09 批跑、sprint_fire 开放（117-P1-misc）** | 设计 §5 **P1 仍开放**打包：**① 2.2** 对手封队友 → 最小炸拿权 → **出刚被封同款**（091 `assist_bomb_then_same_shape`）；**② B7** 红桃逢人配不预留送队友；**③ B8** 禁抢头游耗大牌断队友链；**④ P1-R09** rest=5 时 R09 vs Q2 prefer 实战优先级（净盘批跑验证）；**⑤ L85** 助攻开放 `sprint_fire_ready` / `mid_sprint_fire_bomb`（现仅主攻）。**不挡 GUA-117 v1**；可分子 PR 交付。 | `src/v/nn/ultimate_win_rate_engine_v7.py`、`src/v/nn/guards/v7_guards.py`、`src/v/nn/stage_assist_feed.py` | **登记 2026-07-05**。**停手条件**：见 [`issues/GUA-121-completion.md`](issues/GUA-121-completion.md)（子条 **121-a … 121-e**）。**关联**：GUA-117、GUA-091/102、GUA-094、[`助攻出牌-阶段划分设计口径.md`](助攻出牌-阶段划分设计口径.md) |
| GUA-122 | **open** | **P0** | V-nn, v7, policy, endgame | v7 | **残局 Q1：压级牌单张时，有非逢人配同级牌则不得裸出 H{curRank}** | WF-12 复盘 `20260706084441445615` 副38 步 **39/77**（配对 yf2 `84441454613`）：`curRank=Q`，@1 出 `Single/K` 后，Q1 在 `HQ` 与 `CQ` 同级可压候选中选了 **♥Q 逢人配**，白白损耗 wild，削弱 `Bomb/7` 等后续结构。根因：`_sort_by_recapture_first` 把级牌单张排在 A 单张前合理，但同级 `Single/{curRank}` 内未区分 **H{curRank}（逢人配）** 与非红桃级牌，列表序 tie-break 先出 wild。**5 问准入结论**：① 一类局面（任意 `curRank`）；② 阶段 A 落 Q1 `_sort_q1_prefer_non_wild_level_singles` hard constraint；③ P0 损逢人配；④ 构造态 pytest + WF-12 锚点；⑤ 迁移至评分器 `wild_card_preservation_cost`。**收敛口径**：仅当候选中存在非 wild 的 `Single/{curRank}` 时，deprioritize `H{curRank}`；仅 wild 可出时仍允许；非级牌点数单张不受影响。 | `src/v/nn/endgame/endgame_decide.py`、`tests/test_gua122_wild_level_single_press.py` | **登记 2026-07-06**。**停手条件**：① 参数化 `curRank∈{5,8,Q,K}` 排序回归；② WF-12 锚点步39 必须选非 wild 级牌；③ 仅 wild 级牌单张时行为不变；④ 不影响 GUA-103 bomb 排序回归。**关联**：GUA-011、GUA-103、GUA-121（B7）、WF-12 |
| GUA-123 | **closed** | **P0** | V-nn, v7, policy, endgame, midgame, safety | v7 | **敌方炸弹冲刺：须最小足够反炸；core-filter 不得静默禁压** | …（同上登记）… **2026-07-06 落地**：`_q1_counter_enemy_bomb` + `_recommend_counter_bomb_in_action_list` + core-filter/GUA-075 反炸豁免；`action_list_item_to_feed_recommendation` 优先平台声明牌型；pytest `test_gua123_counter_enemy_bomb.py` 9/9。**2026-07-06 净盘批跑 WF-04**：R-G080-4 零退化；本批无步76 同类锚点（pytest + 旧锚点构造态已验）。`closed_in`: v7-gua123-batch-wf04 | 同上 | **登记 2026-07-06**；完成定义 [[GUA-123-completion]] |
| GUA-124 | **open** | **observation** | V-nn, v7, platform | v7 | **平台 actionList 未枚举可压同花顺（逢人配竞争）** | WF-12 锚点步 76：`actionList` 仅 PASS + `Bomb/J`（含 `H9`），组牌可见方块 5–9 SF 可压 `Bomb/6` 但未下发。**非客户端过滤**；`actIndex` 须对应平台列表，不可臆造出牌。**诊断**：`find_latent_bomb_like_beaters_not_in_action_list` + decide 路径 `GUA-124` 警告日志。 | `src/v/nn/endgame/endgame_decide.py`、`ultimate_win_rate_engine_v7.py` | **登记 2026-07-06**。**关联**：GUA-123、WF-12 |
| GUA-125 | **open** | **P0** | V-nn, v7, policy, endgame, sprint, safety | v7 | **残局 Q1：跟压敌方整牌时须 sprint-preserving / 同型 min 压，不得用更大炸替代可保两手冲刺的三带二** | WF-12 复盘 `20260706222548831117` 副10 步 **51/89**（配对 yf2 `48831193`）：@1 出 `ThreeWithTwo/3`（剩 ~8），yf2（14 张，`curRank=A`，role=主攻）**残局 Q1** 出 `Bomb/J×6`；手牌 `777+888+6J+22`，应 **777+22 或 888+22** min 压，保留 **6J+另一档三带** 两手冲刺。根因：① Q1 ④ `recommended` 对 rem=8 不含 TWT → ⑥ 通用排序选炸；② `_select_two_turn_sprint_structure` 未在被动跟 TWT 调用；③ `_has_recapture` 硬编码 `cur_rank="2"` 抬高 bomb。**5 问准入**：一类局面；落点 `_q1_block_enemy` ④→⑥ 间 sprint/same-type 通道 + 排序修 curRank；P0；pytest + 锚点。**不做**：GUA-115 silent 扩 rem=5–8 作 P0。**P1**：队友深牌让道（125-P1-a）、仅 bomb 可压 PASS（125-P1-b）。 | `src/v/nn/endgame/endgame_decide.py`、`ultimate_win_rate_engine_v7.py`（可选共用 matcher）、`tests/test_gua125_q1_sprint_preserving_press.py` | **登记 2026-07-07**。完成定义 [[GUA-125-completion]]。**停手条件**：见 completion §5。**关联**：GUA-075/103/110/115/122/123、WF-12 |
| GUA-131 | **open** | **P0** | V-nn, v7, policy, endgame, sprint, guard | v7 | **C1 队友协作冲刺 + bomb family 判别接口：yf2 圈 1 必须跟 TWT 形成冲刺能力，等 yf1 拦截 @1 finish** | GUA-125 §0.5.1 C1 决策树落地：① `_is_bomb_family(action)` 判别 bomb 家族（含 Bomb / StraightFlush / 王炸，§4.1）；② `_c1_decision` 队友视角决策（评估 yf1 拦截能力 + 选 yf2 圈 1 动作）；③ `_has_bigger_twt(hand, finish)` 同型互压判定；④ `_sprint_capability_after_twt(hand, action)` 校验剩 2 手 = 冲刺能力。根因：① `_q1_block_enemy` ⑤ `block_with` 命中 bomb-like 前无 sprint-preserving 通道；② `_select_two_turn_sprint_structure` 只看自己 sprint（仅剩两手触发），不评估 yf1 拦截能力；③ `_is_bomb_like_action` 不含 SF/王炸。**5 问准入**：① 一类局面（C1 finish 是更大 TWT 杂牌）；② 阶段 A 落 `_q1_block_enemy` ④ 与 ⑤ 之间新增 `_c1_decision` 通道；③ P0 损冲刺能力；④ pytest + 锚点回归；⑤ 复用 GUA-123 反炸判定。**不做**：GUA-065 队友保护强化（V7 R07-R09 已落地）；`_estimate_teammate_hand` 留 GUA-132；C2-C6 决策树各独立 GUA。 | `src/v/nn/endgame/endgame_decide.py`、`tests/test_gua131_c1_decision.py` | **登记 2026-07-07**。完成定义 [[GUA-131-completion]]。**停手条件**：① pytest 全绿（含 _is_bomb_family 单元 + _c1_decision 集成）；② WF-12 锚点步 51/89 改为跟 TWT；③ 同 seed 同 12 局回归 vn 改善 +1pp。**关联**：GUA-065/125/123/110/111、WF-12 |
| GUA-132 | **open** | **P0** | V-nn, v7, policy, endgame, sprint, guard | v7 | **C2 同花顺 finish 闭合路径：bomb family SF 跨型压 TWT，yf2 跟 TWT/PASS 必败，唯一闭合依赖 yf1 bomb family 接力** | GUA-125 §0.5.2 C2 行：@1 finish = SF（bomb family §4.1）跨型可压杂牌 TWT → yf2 圈 1 跟 TWT 必败（@1 SF 跨型压回 → @1 头游）。根因：① §4.1 总序 bomb family 跨型优势；② yf2 跟 TWT 触发 @1 SF 跨型压回；③ yf1 必须在 @1 SF 出手前拦截（出牌顺序限制）。**5 问准入**：① 一类局面（C2 finish SF）；② 阶段 A 落 `_q1_block_enemy` ④ 与 ⑤ 之间新增 `_c2_decision` 通道；③ P0 损头游；④ pytest + 锚点；⑤ 复用 GUA-131 `_is_bomb_family`。**不做**：完整锚点验证（待 WF-12 后续回放）；C3/C5/C6 高闭合情形留 GUA-134+。 | `src/v/nn/endgame/endgame_decide.py`、`tests/test_gua132_c2_sf_decision.py` | **登记 2026-07-07**。完成定义 [[GUA-132-completion]]。**停手条件**：① pytest 全绿；② WF-12 锚点验证 yf1 拦截闭合；③ 同 seed 同 12 局回归 vn 改善。**关联**：GUA-125/131、WF-12 |
| GUA-133 | **open** | **P0** | V-nn, v7, policy, endgame, sprint, guard | v7 | **C4 5 星炸 finish 闭合路径：yf2 圈 1 必须出 6 张炸压 5 张炸（同型张数优势 §4.1）** | GUA-125 §0.5.2 C4 行：@1 finish = 5 星炸（bomb family §4.1）→ yf2 圈 1 跟 TWT 必败（@1 5 炸同型压回）。**唯一闭合**：yf2 圈 1 出 JJJJJJ（6 张炸）→ 6 张压 5 张（同型张数优势 §4.1）→ @1 PASS → yf2 圈 2 领出 → 圈 2 三连对 + 圈 3 对 → yf2 头游（**自闭合，高闭合率**）。根因：① §4.1 同型张数优势；② 圈 1 跟 TWT 触发 @1 5 炸同型压回；③ yf2 自闭合不依赖 yf1。**5 问准入**：① 一类局面（C4 finish 5 炸）；② 阶段 A 落 `_q1_block_enemy` ⑤ `block_with` 命中 bomb-like 时校验「6 张炸压 5 炸」；③ P0 损头游；④ pytest + 锚点；⑤ 闭合条件 @3 无 6+ 张炸/同花顺/王炸/同型更大三连对。**不做**：完整锚点验证（待 WF-12 后续回放）；与 C1/C2 决策树对比分析。 | `src/v/nn/endgame/endgame_decide.py`、`tests/test_gua133_c4_5bomb_decision.py` | **登记 2026-07-07**。完成定义 [[GUA-133-completion]]。**停手条件**：① pytest 全绿；② WF-12 锚点验证 yf2 6J 闭合；③ 同 seed 同 12 局回归 vn 改善。**关联**：GUA-125/131/132、WF-12 |

| GUA-134 | **open** | **P0** | V-nn, v7, policy, endgame, sprint, guard | v7 | **C3/C5/C6 高闭合率 yf2 自闭合（圈 1 跟 TWT → @1 必 PASS → 圈 2 6J + 圈 3 三连对 + 圈 4 对 = 头游）** | GUA-125 §0.5.2 C3/C5/C6 行：@1 finish ∈ {顺子 / 更小 TWT / 5 张散}，杂牌通道不能跨型 / 同型压 yf2 跟的 TWT（§4.4）→ @1 圈 1 末必 PASS → yf2 圈 2 必领出 6J。**闭合路径**：圈 1 yf2 跟 min TWT → 圈 2 出 6J → 圈 3 出 777+888 三连对 → 圈 4 出 22 对 → yf2 头游（**自闭合三手清空，高闭合率**）。根因：① §4.4 杂牌同型不能跨型；② yf2 圈 1 跟 TWT 触发 @1 PASS；③ yf2 自闭合不依赖 yf1。**5 问准入**：① 一类局面（C3/C5/C6 finish 杂牌 / 更小 TWT / 散牌）；② 阶段 A 落 `_q1_block_enemy` ④ 与 ⑤ 之间新增 `_q1_c3_c5_c6_dispatch`（与 GUA-131/132/133 并联）；③ P0 损头游；④ pytest + 锚点；⑤ 与 C1/C2/C4 dispatch 互斥（finish_type 区分）。**不做**：圈 2 领出 6J 后 @3 / yf1 拦截能力评估（C1 路径 A 的镜像，留 GUA-135）；完整锚点验证（待 WF-12 后续回放）。 | `src/v/nn/endgame/endgame_decide.py`、`tests/test_gua134_c3_c5_c6.py` | **登记 2026-07-07**。完成定义 [[GUA-134-completion]]。**停手条件**：① pytest 全绿（16 cases）；② WF-12 锚点验证 yf2 跟 TWT 夺权；③ 同 seed 同 12 局回归 vn 改善。**关联**：GUA-125/131/132/133、WF-12 |
| GUA-135 | **open** | **P0** | V-nn, v7, policy, endgame, sprint, guard | v7 | **双进优先级 + 冲刺能力/Solo 让道** | **2026-07-10 止血**：teammate_sprint PASS 仅 remaining==0。**续作同日**：① `_has_sprint_capability` 支持多层炸剥 + 钢板/三连对一手；② `should_sprint` OR 冲刺能力（双炸+结构可开 Q0）；③ 队友已头游 cover=0，禁 `mid_hold_for_teammate`。锚点 `06467699` 步63。 | `endgame_decide.py`、`endgame_preprocessor.py`、`rule_card_counter.py`、`ultimate_win_rate_engine_v7.py` | **登记 2026-07-08**；续作 2026-07-10。[[GUA-135-completion]] |
| GUA-136 | **open** | **P0** | V-nn, v7, policy, endgame, sprint, guard | v7 | **玩家剩牌估算增强（记忆模块 + 圈序出牌历史）→ @3 冲刺能力精确评估 + yf1 接力能力精确评估** | GUA-135 §4.1 双进优先级判定的数据源升级：当前 `_estimate_player_remaining` 只看 `enemy_ctx.remaining`，yf1/@3 冲刺能力评估永远保守 False → 「阻止 @3 拿第二」分支永不命中。**增强**：① `_estimate_player_remaining` 优先 `MemoryTracker.get_hand_count`；② `_estimate_player_hand_cards` 基于 `card_state` 推断具体手牌；③ `_estimate_player_sprint_capability` 应用 `_has_sprint_capability` 判定。根因：① GUA-135 落地时未集成 MemoryTracker；② `tracker.get_seat_cards_estimate` 不存在导致 at3_sprint 兜底 False。**5 问准入**：① 一类局面（双进优先级判定中 yf1/@3 sprint 评估）；② 阶段 A 落 `endgame_decide.py` GUA-135 方法旁；③ P0 损 @3 拦截准确性；④ pytest + 锚点；⑤ 不修改 MemoryTracker 本身。**不做**：排除法推断算法增强（留 GUA-137）；推理性能优化（留 GUA-138）；贡牌/抗贡特殊处理（留 GUA-139）。 | `src/v/nn/endgame/endgame_decide.py`、`tests/test_gua136_player_remaining_enhance.py` | **登记 2026-07-08**。完成定义 [[GUA-136-completion]]。**停手条件**：① pytest 全绿；② GUA-135 sprint_race 触发后 yf1/@3 sprint 评估准确；③ 回归 GUA-131~135 无破坏。**关联**：GUA-052/125/131/132/133/134/135、WF-12 |
| GUA-137 | **open** | **P0** | V-nn, v7, policy, endgame, sprint, guard | v7 | **玩家整手结构推断增强（grouping_engine）→ yf1/@3 冲刺能力精确 + 整牌型评估** | GUA-136 `_estimate_player_sprint_capability` 只看「≥4 张同点 + 剩 ≤ 5 张」粗粒度判定，误判率高：① 6J+单+单（3 圈）误判为 sprint；② 三带二+单（2 圈但无 bomb）误判为 sprint。**增强**：① `_estimate_player_grouping_plan` 基于 `enumerate_groupings` 推断整手结构；② `_estimate_player_num_rounds` 推断手数；③ `_estimate_player_sprint_capability` 升级 v2（num_rounds ≤ 2 AND has_bomb_family）。根因：① GUA-136 未集成 grouping_engine；② sprint 定义（GUA-125 §0.0）要求「出+下一手接力清空」= 2 圈闭合，3 圈闭合不算 sprint。**5 问准入**：① 一类局面（双进优先级判定中 sprint_race / yf2_self_sprint）；② 阶段 A 落 `endgame_decide.py` GUA-136 方法旁；③ P0 损 sprint 判定准确性；④ pytest + 锚点；⑤ 不修改 grouping_engine 本身。**不做**：grouping_engine 性能优化（留 GUA-138）；缓存策略（留 GUA-138）；排除法推断具体牌型（留 GUA-139）；贡牌/抗贡重建（留 GUA-140）。 | `src/v/nn/endgame/endgame_decide.py`、`tests/test_gua137_grouping_enhance.py` | **登记 2026-07-08**。完成定义 [[GUA-137-completion]]。**停手条件**：① pytest 全绿；② sprint 判定准确（6J+单+单 判 False，三带二+单 判 False）；③ 回归 GUA-131~136 无破坏。**关联**：GUA-052/061/063/125/131/132/133/134/135/136、WF-12 |
| GUA-138 | **open** | **P1** | V-nn, v7, perf, endgame, sprint | v7 | **grouping_engine 推理性能优化（LRU 缓存 + 增量计算）** | GUA-137 `_estimate_player_grouping_plan` 每次 decide 都跑 `enumerate_groupings`：26 张手牌 ~4 ms；同局多次 decide 冗余计算 90%+。**优化**：① `_GroupingPlanCache` 类（LRU 64 容量 + 深拷贝）缓存 `(hand, cur_rank) → GroupingPlan`；② `_estimate_player_grouping_plan` 增强使用缓存；③ 缓存失效（cur_rank 变化清空）。根因：① grouping_engine.enumerate_groupings 无内置缓存（GUA-061 已 frozen）；② GUA-137 调用频率高（每 decide 2 次）。**5 问准入**：① 一类问题（推理时延）；② 阶段 A 落 `endgame_decide.py` GUA-137 方法旁；③ P1（非 P0：GUA-137 已可用但偏慢）；④ pytest benchmark + 回归；⑤ 不修改 grouping_engine 内部。**不做**：grouping_engine 内部缓存（已 frozen）；增量计算（留 GUA-139）；跨局持久化；排除法推断（留 GUA-139）。 | `src/v/nn/endgame/endgame_decide.py`、`tests/test_gua138_grouping_cache.py` | **登记 2026-07-08**。完成定义 [[GUA-138-completion]]。**停手条件**：① pytest 全绿；② cache hit 速度 > 50x；③ 回归 GUA-131~137 无破坏。**关联**：GUA-052/061/135/136/137、WF-12 |
| GUA-141 | **closed** | **P0** | V-nn, v7, policy, endgame | v7 | **Q1 `_sort_by_recapture_first`：张数多优先 → 牌力大优先** | WF-12 `041616032842` 步45→46：跟压 `Single/A` 时手中有 SB/HR/级牌 J，Q1 因「张数多优先」先出 `Bomb/4`。**改**：回收优先不变，第二键改为 `-_max_card_value`；`_has_recapture`/`_sort_by_recapture_first` 传入 `curRank`；GUA-122 级牌单张提升与牌力排序对齐。GUA-103 bomb-like 最小足够成本仍由 `_sort_q1_block_candidates` 后段负责。 | `endgame_decide.py`、`tests/test_gua141_q1_sort_card_strength.py` | **closed_in** 2026-07-10；pytest 141+122+103+100 **15/15** ✅ |
| GUA-142 | **closed** | **P0** | V-nn, v7, policy, endgame | v7 | **敌剩 6：取消强制 Trips/禁 Pair；自由领出整组 ThreePair/TwoTrips 保 SF/炸冲刺路径** | WF-12 `041616032842` 步50：`rem=6` 强制 `Trips/9`，禁 `Pair/5`，压过 `ThreePair 8899TT`。**Phase A**：`endgame_rule[6]` 对齐整结构推荐且 `banned=[]`；Q1 自由领出钩子优先整组三连对/钢板（出完后剩 SF/炸 + ≤2 手尾）。**不做**：记忆推断敌 6 张；不改 GUA-135 炸冲刺定义。 | `endgame_preprocessor.py`、`endgame_decide.py`、`tests/test_gua142_*` | **closed_in** 2026-07-10；[[GUA-142-completion]] |

## 交叉引用

| ID | 相关文档 |
|----|----------|
| GUA-022 | [`AGENT_HUB.md`](AGENT_HUB.md) — 多 Agent / Kanban 编排 |
| GUA-023 | [`AGENT_HUB.md`](AGENT_HUB.md) — OpenCode ACP 桥接不兼容 |
| GUA-029 | [`01_bomb_techniques.md`](../knowledge/skills/02_main_attack/01_bomb_techniques.md)、[`M3_DIAGNOSIS.md`](M3_DIAGNOSIS.md) BUG5；完成定义 [[GUA-029-completion]] |
| GUA-030 | [`PRINCIPLES_MAPPING.md`](PRINCIPLES_MAPPING.md)；完成定义 [[GUA-030-completion]] |
| GUA-031 | [`PRINCIPLES_MAPPING.md`](PRINCIPLES_MAPPING.md) §8.4、§十七–§二十二；完成定义 [[GUA-031-completion]] |
| GUA-032 | [`PRINCIPLES_MAPPING.md`](PRINCIPLES_MAPPING.md) §14–§15、§18–§22；完成定义 [[GUA-032-completion]] |
| GUA-033 | [`platform-data-interpretation.md`](../knowledge/platform-data-interpretation.md) **§2**；完成定义 [[GUA-033-completion]] |
| GUA-026 | [`10_three_with_two_skills.md`](../knowledge/skills/04_common_skills/10_three_with_two_skills.md) §二十 |
| GUA-034 | [`GUA-034-方案评审.md`](GUA-034-方案评审.md)；完成定义 [[GUA-034-completion]] |
| GUA-035 | GUA-034 END-M02+；完成定义 [[GUA-035-completion]] |
| GUA-036 | batch7 round38 复盘；完成定义 [[GUA-036-completion]] |
| GUA-044 | [`platform-data-interpretation.md`](../knowledge/platform-data-interpretation.md)；完成定义 [[GUA-044-completion]] |
| GUA-045 | [`V7-实施方案.md`](V7-实施方案.md) §0–§2；完成定义 [[GUA-045-completion]] |
| GUA-063 | 组牌→出牌衔接：is_core=0.0 顺子/三张保护 + 贡前手牌；参见 `ITERATIONS.md` 底部活跃行。GUA-067 联动 |
| GUA-064 | BC argmax collapse 日志确证；参见 `ITERATIONS.md` v7-bc-argmax-collapse-confirmed 行；`v7-win-rate-history.md` L43 |
| GUA-067 | 贡牌未调整 initial_hand；`adjust_initial_hand_for_tribute_back` 修复；联动 GUA-063 |
| GUA-065 | 导入 M3 队友识别与保护到 V7 Guard；`_is_teammate_greater` → V7 R07-R09；联动 GUA-031/034/036 |
| GUA-068 | V7-R11 全局抑制牌检查+节流；`_count_remaining_suppressors` 通过 MemoryTracker 查已出牌（HR/SB/级牌/高点数）；联动 GUA-052/GUA-045 |
| GUA-069 | 超弱/助攻角色不应拆核心牌型；移除 `_group_consistency_filter` 角色全放行 BUG + `_score_power` 钢板记分；yf2 回放 20260619082227220155 复现；联动 GUA-062/GUA-063 |
| GUA-070 | R12 拆对子出单禁制 + ThreeWithTwo对子is_core标记 + 小钢板减分移除；回放诊断发现三带二吞噬对子导致 is_core=0 静默放行 + curRank=3时单2是最小单不应拆对子出 + 钢板不管大小都是加分项；联动 GUA-062/GUA-063/GUA-069 |
| GUA-071 | 行牌启发式选择替代 NN argmax；纯 Guard 驱动决策管线；参见 [[组牌-NN衔接设计]] §九；联动 GUA-064/065 |
| GUA-072 | GUA-052 MemoryTracker + RuleCardCounter；**子项C** 贡牌/抗贡算王见 `04_calculation_skills` §二.1、`06_game_flow`；消费：残局 `_seat_may_hold_*`、R11 抑制牌、GUA-094/098 trace |
| GUA-073 | 知识库接入映射表：15 skills 文件 → V7 代码管线逐条标注；参见 `knowledge-integration-mapping.md` |
| GUA-074 | 组牌方案数从 6-7 缩减到 3 套；`grouping_engine.py` `enumerate_groupings()` 返回 `plans[:3]` |
| GUA-075 | V7 决策架构改造：排除法→推荐法双路径；参见 `V7-决策架构改造-从排除法到推荐法.md`；联动 GUA-063/064/071/114 |
| GUA-076 | 组牌方案完整性BUG：AssertionError→warning+剔除，closed ✅；联动 GUA-062/GUA-063/GUA-074 |
| GUA-077 | 精确拆牌规划：组牌引擎缺全局多轮规划维度；联动 GUA-062/GUA-075 |
| GUA-078 | 残局智能体独立管线：EndgamePreprocessor + EndgameDecider + check_endgame_agent.py；联动 GUA-075/GUA-052/GUA-063/GUA-065 |
| GUA-079 | 回放诊断：单牌倒置+拆炸凑压+残局静默三层根因；联动 GUA-071/GUA-078/GUA-069/GUA-070/GUA-072/GUA-075。回放原文见 `replay_word.md` |
| GUA-080 | 组牌 GUA-072 拆炸时序 + grouping_scanner 降级观测；完成定义 [[GUA-080-completion]]；联动 GUA-054/061/062/072/079 |
| GUA-081 | 三带二推荐拆炸 fallback + 贡还 `all_players_hands` 同步；回放 `20260628091707150272` 步7；完成定义 [[GUA-081-completion]]；分析流程 **WF-12**；联动 GUA-075/067/080 |
| GUA-082 | R12 主路径+heuristic 回退不拆对；锚点步23/步64；pytest `test_gua075_r12_main_path.py` + `test_gua082_r12_heuristic_fallback.py`；联动 GUA-070/075/081 |
| GUA-083 | `_ensure_valid` 失败误 PASS → return None 回退 + 录牌 `actionList_sample`；回放步53；pytest `test_gua083_*`/`test_v7_action_list_context.py`；联动 GUA-075/081/082、WF-12 |
| GUA-084 | 五星炸保护（n≥5 保4核+限量peel）+ SF 分支 BOMB_FIRST + 三带二禁吃炸对；回放 `20260628091704590941`；完成定义 [[GUA-084-completion]]；**GUA-080 冻结的结构例外**；联动 GUA-072/063、PB-001 |
| GUA-090 | `stage_0_1` 开局入口 `_stage_open_plan()`；开局从“最小合法牌”切到“意图驱动”领出；联动 GUA-075/089/101 |
| GUA-091 | `stage_2` 中局入口 `_stage_mid_dispatch()`；统一消费 `_belief + _phase_relation + role + greaterAction`；联动 GUA-075/089/094/097/098/101、`v7-kpi-vs-m3-vs-lalala-2026-06-29.md` §7.13.8 |
| GUA-094 | `rule_card_counter.py::infer_phase_relation()` + `game_state['_phase_relation']`；Layer 2 只出标签/概率，不直接改 actIndex；**GUA-119** 消费队友高频牌型；联动 GUA-072/089/091/097/098/101、`v7-kpi-vs-m3-vs-lalala-2026-06-29.md` §7.9.2 |
| GUA-096 | [`iterations/v7-gua096-post-batch-history-log.md`](iterations/v7-gua096-post-batch-history-log.md) · `§七.11` 预测 5 · `§七.9` Layer 3 运维基础 1 · `v4v5v6-lessons-2026-06.md` §0 |
| GUA-097 | [`iterations/v7-gua097-ip-ablation-runner.md`](iterations/v7-gua097-ip-ablation-runner.md) · [`iterations/v7-gua097-ablation-log.md`](iterations/v7-gua097-ablation-log.md) · `§七.6` 预测 2 · `§七.11.3` 反推 1 · `v4v5v6-lessons-2026-06.md` §4.1/§6.2 |
| GUA-098 | [`iterations/v7-gua098-decision-trace.md`](iterations/v7-gua098-decision-trace.md) · `src/v/nn/tracing/decision_trace.py` · `§七.9` Layer 3 运维基础 3 · `§七.11.3` 反推 2 · `v4v5v6-lessons-2026-06.md` §8.1 |
| GUA-099 | WF-12 回放 `20260630082350363704` 步69/79；`src/v/nn/endgame/endgame_decide.py` Q1 兜底排序；`src/v/nn/guards/v7_guards.py` 平台格式牌型识别；联动 GUA-078/079/083 |
| GUA-100 | WF-12 回放 `20260630082350346668/63704` 步76/79；`src/v/nn/endgame/endgame_preprocessor.py` `endgame_rule/BAOSHU_RULE` 收敛；`src/v/nn/endgame/endgame_decide.py` 保留剩 5 张单张残局回炸策略；联动 GUA-078/099 |
| GUA-101 | 开局/中局专用大脑最小落地总控；子顺序固定为 `GUA-090 → GUA-094 → GUA-091`；联动 GUA-075/089/097/098、`v7-kpi-vs-m3-vs-lalala-2026-06-29.md` §7.13 |
| GUA-102 | 前中段“整牌冲刺点火”意图；当 `group_type_map` 完备度极高且主攻/多炸/自然轮次短/残单少时，允许从“最小跟单”切到“主动开炸抢节奏”；区别于 `GUA-068` 的被动改炸；联动 GUA-090/091/094/098/101、WF-12 |
| GUA-103 | WF-12 回放 `20260630155538336842` 步39/79→40/79 与 `20260701175356865029` 步49/79→50/79；`src/v/nn/endgame/endgame_decide.py` Q1 候选排序；bomb-like 封锁候选内部改为最小足够成本优先，禁止无收益升炸 / 多耗逢人配；联动 GUA-078/099/100 |
| GUA-104 | WF-12 回放 `20260630155538336842` 步40/79→42/79；`src/v/nn/endgame/endgame_decide.py` 队友控牌停手逻辑；队友已控牌时，Q1 不得再用同花顺/更大炸反压队友，且平台声明型 `5炸` 不得再因 guard 误判 `Free` 漏掉停手；联动 GUA-078/099/103 |
| GUA-105 | WF-12 回放 `20260630155538336842` 步46/79→47/79；`src/v/nn/endgame/endgame_decide.py` `enemy rest=1` 领出特判；先整牌锁敌，再看安全单，若无安全单也不得再机械领 `K`；联动 GUA-078/100/103/104 |
| GUA-106 | WF-12 回放 `20260630155538336842` 步47/79→48/79；`src/v/nn/features/rule_card_counter.py` 新增尾位队友接单概率，`src/v/nn/ultimate_win_rate_engine_v7.py` 用于拦住 `mid_sprint_fire_bomb` / `mid_bomb_cutoff` 抢先炸；联动 GUA-091/094/102 |
| GUA-107 | WF-12 回放 `20260630155538336842` 步62/79→63/79→78/79；`src/v/nn/endgame/endgame_decide.py` 新增 `enemy rest<=2` 领出整牌锁敌特判 + “队友领单时仅安全单可反超，否则 PASS”；联动 GUA-078/100/105 |
| GUA-108 | WF-12 + WF-05 对 `20260701151948500035` 的组牌复盘；`src/v/nn/features/grouping_engine.py` 应枚举“拆低价值炸换顺子降手轮”候选，并让评分器与保炸方案比较，而非继续只给保炸方案；联动 GUA-062/063/074/075/077/102、[`V7-架构演进与新增规则准入治理.md`](V7-架构演进与新增规则准入治理.md) |
| GUA-109 | WF-12 对 `20260701151948500035` 的 `8/91` / `12/91` 竞争未来计划复盘 + WF-05 对 `20260702195833037993` 的组牌增补；被动同型压制时，必须让“保未来结构”与“拆低价值结构换当前夺权”候选并存，再交统一评分层比较；`three_with_two` 的 pair 分配也不得过早贪心到把顺子竞争方案静默吃掉；联动 GUA-075/091/094/098/102/108、[`V7-架构演进与新增规则准入治理.md`](V7-架构演进与新增规则准入治理.md) |
| GUA-110 | WF-12 对 `20260701151948500035` 的 `75/91` 复盘；残局 Q1 在“两手冲刺”局面下应优先整牌先手冲刺，不得再拆成单张领出；联动 GUA-078/100/105/107/109、[`V7-架构演进与新增规则准入治理.md`](V7-架构演进与新增规则准入治理.md) |
| GUA-111 | WF-12 对 `20260701151948500035` 的 `80/91→83/91→85/91` 复盘；残局 Q1 在同型通道领出/续出时必须评估“压后通道归属”，若我方无接力且敌方仍可能有更大同型，不得机械续出次级同型；联动 GUA-078/094/100/105/107/110、[`V7-架构演进与新增规则准入治理.md`](V7-架构演进与新增规则准入治理.md) |
| GUA-113 | WF-12 对 `20260702195833037993` 步 66/84 复盘；残局 Q1 在 role=超弱/助攻 且队友已控牌时必须 PASS，不得用三带二等结构反压队友；`decide()` 注入 `_role`；联动 GUA-065/078/104/109、[`V7-架构演进与新增规则准入治理.md`](V7-架构演进与新增规则准入治理.md) |
| GUA-114 | WF-12 对 `20260701175356193021` 步 37/105 复盘；GUA-075 `_build_three_with_two_press(min)` 带牌优先独立对子、避免从 ≥3 张抠对留孤张；联动 GUA-070/075/081/109、[`V7-架构演进与新增规则准入治理.md`](V7-架构演进与新增规则准入治理.md) |
| GUA-115 | WF-12 对 `20260703214948445203` 步 51/75 复盘；残局 Q1 敌 rem=4 时火不打四（禁我方 Bomb 压四张敌）；`BAOSHU_RULE[4]` 与 `_will_lose` 对齐；联动 GUA-078/112/113、M1 `endgame_strategy`、[`V7-架构演进与新增规则准入治理.md`](V7-架构演进与新增规则准入治理.md) |
| GUA-117 | 助攻/超弱；Layer0 **assist_layer0_guard** + pytest 117-7 ✅；方案 A 齐套待关单；[[GUA-117-completion]] |
| GUA-116 | 主攻 P1–4 `stage_main_attack_lead.py`；[[GUA-116-completion]] |
| GUA-118 | §三 L81 顺最小散牌；`_recommend_play` 队友分支；不新函数；[[GUA-117-completion]] **117-P2** |
| GUA-119 | 助攻 stage_2 真 2.1；依赖 **GUA-094**；[[GUA-117-completion]] **117-P1-094** |
| GUA-120 | 助攻 B4 P1+ `group_filter` 硬约束；[[GUA-117-completion]] **117-P1-gf** |
| GUA-121 | 助攻 P1 续包 2.2/B7/B8/R09/sprint_fire；[[GUA-121-completion]] **121-a–121-e** |
| GUA-112 | WF-12 `20260701175340219689` / `84440863663` 步106；`endgame_decide.py` finish-now 豁免 `apply_banned_filter` + `_get_q1_protected_types`；`tests/test_gua112_finish_now_banned_exempt.py` |
| GUA-122 | WF-12 `84441445615` 步39/77；`endgame_decide.py` Q1 压 `Single/{curRank}` 时优先非 wild 级牌，禁止有替代时裸出 `H{curRank}`；联动 GUA-103/121 |
| GUA-125 | WF-12 `20260706222548831117` 步51/89；Q1 跟压整牌 sprint-preserving + 同型 min 压；禁 6J 换 777+22；`_has_recapture` 真 curRank；[[GUA-125-completion]]；联动 GUA-075/110/115/122/123 |
| GUA-131 | GUA-125 §0.5.1 C1 决策树落地；`_is_bomb_family` / `_c1_decision` / `_has_bigger_twt` / `_sprint_capability_after_twt`；[[GUA-131-completion]]；联动 GUA-065/125/123 |
| GUA-132 | GUA-125 §0.5.2 C2 行；SF（bomb family §4.1）跨型压 TWT；yf2 跟 TWT 必败；yf1 bomb family 接力；[[GUA-132-completion]]；联动 GUA-131 |
| GUA-133 | GUA-125 §0.5.2 C4 行；5 星炸（bomb family §4.1）；yf2 6 张炸自闭合（同型张数优势）；[[GUA-133-completion]]；联动 GUA-131/132 |
| GUA-134 | GUA-125 §0.5.2 C3/C5/C6 行；杂牌 finish 不能跨型 / 同型压 TWT §4.4；yf2 跟 TWT 夺权 → 圈 2 6J + 圈 3 三连对 + 圈 4 对 = 自闭合；[[GUA-134-completion]]；联动 GUA-131/132/133 |
| GUA-135 | GUA-125 §0.5.2 C1-C6 队整体策略层；双进优先级判定（C2/C4 接受 @1 头游 + C3/C5/C6 yf2 自闭合后队策略）；`_q1_double_second_priority` + `_has_sprint_capability` + `_estimate_player_remaining`；[[GUA-135-completion]]；联动 GUA-131/132/133/134 |
| GUA-136 | GUA-135 §4.1 数据源升级；`_estimate_player_remaining` 增强（MemoryTracker 优先）+ `_estimate_player_hand_cards` 新增 + `_estimate_player_sprint_capability` 新增；[[GUA-136-completion]]；联动 GUA-052/131/132/133/134/135 |
| GUA-137 | GUA-136 sprint 判定升级；`_estimate_player_grouping_plan` + `_estimate_player_num_rounds` 新增；sprint = num_rounds ≤ 2 AND has_bomb_family；[[GUA-137-completion]]；联动 GUA-052/061/135/136 |
| GUA-138 | GUA-137 性能优化；`_GroupingPlanCache` LRU 64 + 深拷贝 + invalidate；[[GUA-138-completion]]；联动 GUA-061/137 |
| GUA-141 | Q1 `_sort_by_recapture_first` 牌力大优先；联动 GUA-103/122；WF-12 `041616032842` |
| GUA-142 | 敌剩6整结构领出保冲刺；[[GUA-142-completion]]；联动 GUA-100/116/135 |

---

## 模板（新增行时复制）

| ID | 状态 | 严重级别 | 标签 | 版本 | 简述 | 现象 / 复现要点 | 涉及模块 | 备注 |
|----|------|----------|------|------|------|-----------------|----------|------|
| GUA-xxx | open / closed | P0–P3 | rules, observation, policy | m1/v4/v5/v6/训练/docs | | | | `closed_in` / `duplicate of`；若需详细完成定义 → 新建 `issues/GUA-xxx-completion.md` |

## 来自「比赛汇总」的说明（非缺陷）

`docs/掼蛋AI相关比赛汇总.md` 主要为**南邮平台与赛事索引**，不记载本仓库逻辑缺陷；参赛与平台 JSON/WebSocket 对齐请在迭代中当作**环境约束**验收（不必单列 GUA，除非出现协议不符）。

