# Handoff: V7 3局净盘批跑 + WF-12 锚点筛选

| 字段 | 内容 |
|------|------|
| 日期 | 2026-06-30 |
| 分支 | `v7-dev` @ **4205b99** |
| 状态 | **进行中**（已完成净盘 3 局批跑 + 初筛 3 个高价值 WF-12 锚点；下一步应直接进单步决策链路） |
| 真源 | [`batch_executor/latest_victory_num.json`](../../batch_executor/latest_victory_num.json) / [`logs/v7_vs_lalala_20260630_155417.log`](../../../logs/v7_vs_lalala_20260630_155417.log) / [`game_records_v7/`](../../../game_records_v7/) / [`scripts/analysis/analyze_v7_rounds.py`](../../../scripts/analysis/analyze_v7_rounds.py) / [`scripts/tools/analyze_v7_round_levels.py`](../../../scripts/tools/analyze_v7_round_levels.py) |

## 背景（2 句）

本会话已按净盘口径执行 `python scripts/launchers/v7/run_v7_vs_lalala_games.py --games 3`，并按 WF-04 完成 L1/L3/L4 对账。结论不是“残局漏压导致 0/3”，而是 **V7 能零星赢副但无法把结构优势转成头游/双上**，因此下一步最值钱的是从这 22 副里挑 2~3 个“明明手很整却没转成赢局”的锚点，直接走 WF-12。

## 本批结果（WF-04 口径）

- `batch_games=3`，`completed_games=3/3`
- `victoryNum=[0,3,0,3]`，同队校验通过；V7 所在 `0+2` 队，故 **局胜 0/3**
- `game_records_v7` 新增 `44 JSON`，按 V7 双席录牌口径 = **22 副**
- `analyze_v7_rounds.py --all`：V7 **4/22 副胜（18.2%）**
- 批后残局异常扫描：**0 条**

## 已完成

- [x] V7 净盘（`game_records_v7/*.json`、`v7_vs_lalala_scores.json`、`logs/*`、`latest_victory_num.json` 等）
- [x] 批跑 3 局，日志见 [`logs/v7_vs_lalala_20260630_155417.log`](../../../logs/v7_vs_lalala_20260630_155417.log)
- [x] WF-04 解读：局级 / 副级 / victoryNum 口径已定
- [x] 从 22 副中初筛 3 个 **“结构优势高但没转成头游/双上”** 的 WF-12 候选

## 3 个优先锚点（按价值排序）

### 1. Round 17：四炸开局却只拿到 3/4 名

- 牌谱：
  - [`20260630155538336842 [yf2_v7]-[opponent_1_3]-[17]-[2].json`](../../../game_records_v7/20260630155538336842%20[yf2_v7]-[opponent_1_3]-[17]-[2].json)
- 为什么值钱：
  - 首个 `play` 决策的 `group_type_map` = `4 bomb + 2 pair + 1 three_with_two`
  - 这是本批最强的“整牌完备度”样本之一，但最终 `order=[1,3,0,2]`，V7 两席只拿到 **3/4 名**
  - 非残局问题，明显更像 **前中段节奏争夺 / 主动开炸时机 / 让道判断** 问题
- 建议先看步：
  - **`4/79`**：`yf2_v7 PASS`，当时 `greaterPos=1`，对手刚出 `Single/3`
- 初步怀疑：
  - `GUA-075` / `stage_0_1` 仍在按保守跟单/让道思路走，`GUA-102` 的“整牌冲刺点火”可能没有在这个对手控单场景真正吃到

### 2. Round 19：三炸+同花顺开局，最终只拿到 2 名

- 牌谱：
  - [`20260630155539618276 [yf2_v7]-[opponent_1_3]-[19]-[2].json`](../../../game_records_v7/20260630155539618276%20[yf2_v7]-[opponent_1_3]-[19]-[2].json)
- 为什么值钱：
  - 首个 `play` 决策的 `group_type_map` = `2 bomb + 1 straight_flush + 2 three_with_two`
  - 这种牌型密度理论上应至少强冲头游，但最终 `order=[3,2,0,1]`，V7 最好只到 **第 2 名**
  - 适合验证“不是不会赢副，而是不会把优势转成头游”
- 建议先看步：
  - **`2/115`**：`yf2_v7` 领出 `Single/5`
  - 后续自家前 4 次动作分别在 `2/115, 6/115, 10/115, 14/115`，连续是 `Single/5 → Single/7 → Single/R → Single/8`
- 初步怀疑：
  - 开局仍然被“最小单张试探”主导，`GUA-090` / `GUA-102` 还没学会在超高结构密度下主动切节奏

### 3. Round 12：三炸+三连对+三带二结构，最后只拿到 3 名

- 牌谱：
  - [`20260630155534979956 [yf1_v7]-[opponent_1_3]-[12]-[2].json`](../../../game_records_v7/20260630155534979956%20[yf1_v7]-[opponent_1_3]-[12]-[2].json)
- 为什么值钱：
  - 首个 `play` 决策的 `group_type_map` = `3 bomb + 1 pair + 1 three_with_two + 1 three_pair`
  - 这是非常典型的“牌很整，但最后没能转成头游/双上”的样本
  - 最终 `order=[1,3,0,2]`，且 `restCards` 显示 V7 两席都还有大量结构牌未出尽
- 建议先看步：
  - **`3/88`**：`yf1_v7` 领出 `Single/7`
  - 然后串看自家 `7/88 PASS`、`11/88 Bomb/6`、`15/88 Single/3`
- 初步怀疑：
  - 先小单试探后节奏被对手接管，直到更后面才被动开炸；这副很适合验证“主动点火”和“中局统一调度”谁在掉线

## 为什么先看这 3 副，不先看别的

- 它们都不是“残局漏压”主类，因为本批残局扫描为 0 条
- 它们都满足至少两条：
  - 多炸 / 同花顺 / 三带二 / 三连对等结构牌型密度高
  - 首个 `play` 决策就露出“最小单张 / PASS”而不是主动控节奏
  - 最终结果不是头游，也没转成双上
- 因而最适合回答当前主问题：
  - **GUA-102 是否真正生效？**
  - **开局/中局大脑为什么还在被最小跟单牵着走？**

## 下一步唯一动作

按 **WF-12** 依次深挖上面 3 个锚点，顺序固定为：

1. `20260630155538336842 ... round17 ...` 的 **`4/79`**
2. `20260630155539618276 ... round19 ...` 的 **`2/115`**
3. `20260630155534979956 ... round12 ...` 的 **`3/88`**

对每个锚点都必须输出：

- 圈况：步号、`greaterPos/greaterAction`、角色、手牌数
- 实际出牌：`type + actIndex`
- 命中决策层：`GUA-075 / stage_0_1 / stage_2 / GUA-102 / R11 / heuristic`
- 根因标签：至少一个 `R-D01~R-D08`
- 建议：是否要补 `pytest` / 是否新开 GUA / 是否要继续净盘批跑复验

## 命令速查

```powershell
# 局级 + 副级汇总（已跑过，可复核）
python scripts/analysis/analyze_v7_rounds.py --all
python scripts/tools/analyze_v7_round_levels.py

# 直接 grep 本批 V7 客户端日志
rg "GUA-075|GUA-090|GUA-091|GUA-102|残局管线|actIndex=|推荐被组牌保护拦截" logs/yf1_v7_20260630_155449.log logs/yf2_v7_20260630_155455.log
```

## 备注

- `SCRIPT_INDEX.md` 的“分析批跑结果”一行仍写的是 `scripts/analysis/analyze_v7_rounds.py`；本批实际副级脚本在 `scripts/tools/analyze_v7_round_levels.py`
- 本 handoff 不要求先写 ITERATIONS；先把 2~3 个 WF-12 链路做实，再决定是否开新 GUA
