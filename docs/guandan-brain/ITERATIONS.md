# 迭代日志（Iteration Log）

> 每条迭代只回答四件事：（1）本轮要解决哪几条 `GUA-xxx`（最多 2 条）；（2）改了哪些文件/参数（可链 commit）；（3）评测：`EVAL.md` 中哪些 case fail→pass，哪些 pass→fail（回归）；（4）下轮**唯一** priority。

## 模板（在表格下方追加新行）

| 日期 | 迭代名/分支 | 目标 GUA | 改动摘要（文件/commit） | 评测结果摘要 | 下轮 priority |
|------|-------------|----------|-------------------------|--------------|---------------|
| YYYY-MM-DD | | GUA-xxx, … | | pass/fail；回归： | |

---

## 记录

| 日期 | 迭代名/分支 | 目标 GUA | 改动摘要（文件/commit） | 评测结果摘要 | 下轮 priority |
|------|-------------|----------|-------------------------|--------------|---------------|
| 2026-04-21 | M1 对照与文档 / m-dev | GUA-001, GUA-020 | 重写 `docs/掼蛋AI相关比赛汇总.md`（UTF-8 v2.0）；新增 `scenarios/M1_yf1_vs_yf2_comparison.md`；更新 ISSUES/EVAL/scenarios/README | GUA-001：文档可读性 **pass**。GUA-020（数据来自 `game_records/`，可解析 **2** 个 `game_id`）：`20260113110809115923`、`20260421122404427820`；示例路径 `game_records/20260421122404427820 [yf1_m1]-[opponent_1_3]-[23]-[None].json` 与 `... [yf2_m1]-...`。两队记录中均为 `victoryNum: [0,3,0,3]`（0+2 队未胜）。**合并两局**：yf1_m1 共 **47** 条「我的决策」、PASS **26**（PASS 率 **≈55.3%**）；yf2_m1 共 **48** 条、PASS **31**（**≈64.6%**）。**近似问题 PASS**（`actionList_size>1` 仍 PASS）：yf1 合计 **3**、yf2 合计 **8**。**分局**：`20260113` 局 yf1 PASS 率 **47.6%** vs yf2 **68.2%**（yf2 该局近似问题 PASS **6**）；`20260421` 局双方 **26** 条决策、PASS 率均为 **61.5%**，近似问题 PASS **3 vs 2**（**该局二者接近**）。**回归**：无代码变更，无 EVAL case pass→fail。 | **下轮唯一 priority**：再积累对局（目标仍建议 **≥10 个可解析 `game_id`**）或用脚本批量扫 `game_records/*yf*_m1*.json`；若持续显示 yf2 偏高，优先 **diff `yf1_m1.py`/`yf2_m1.py` 调用链与 `player_id`**，再决定是否收紧共用 `src/decision/`。 |
| 2026-04-21 | M1 GUA-020 扩样复测 / m-dev | GUA-020 | 无代码变更；仅新增 `game_records/` 下对局文件 | **GUA-020（扩样）**：`game_records/` 中可配对 **`game_id` 共 8 个**（`yf1_m1`+`yf2_m1` 各 8 份）：`20260113110809115923`，`20260421122404427820`，`20260421122833986325`，`20260421123017792144`，`20260421123140409339`，`20260421123337946652`，`20260421123509820608`，`20260421123641090777`；路径示例 `game_records/<game_id> [yf1_m1]-[opponent_1_3]-[...].json`。各局 `victoryNum` 均为 **`[0,3,0,3]`**（本批样本内 0+2 队未胜）。**合并 8 局**：yf1_m1 **188** 条「我的决策」、PASS **104**（**55.32%**）；yf2_m1 **184** 条、PASS **103**（**55.98%**）——**PASS 率差约 0.66%**，**整体已无明显「yf2 更差」**。**近似问题 PASS**（`actionList_size>1` 仍 PASS）：yf1 **7**、yf2 **10**（yf2 仍略多，幅度小）。**回归**：无代码变更。 | **下轮唯一 priority**：若台账以 **≥10 个 `game_id`** 为关单条件，再补 **2** 局成对记录；否则可将 **GUA-020** 标 **closed** 并备注「大样本下 yf1/yf2 PASS 率接近，优先排查**胜负与阶段策略**或**共用层**，而非单独分叉 `yf2_m1` 路由」。 |
| 2026-04-21 | M1 GUA-020 达标样 / m-dev | GUA-020 | 无代码变更；追加 `game_records/` 成对记录 | **GUA-020（≥10 个 `game_id`）**：可配对 **`game_id` 共 10 个**（`yf1_m1`+`yf2_m1` 各 10 份）：在 8 个 id 基础上新增 `20260421123919905488`，`20260421124132048517`；路径示例不变。各局 `victoryNum` 仍为 **`[0,3,0,3]`**。**合并 10 局**：yf1_m1 **230** 条决策、PASS **126**（**54.78%**）；yf2_m1 **228** 条、PASS **127**（**55.70%**）——**PASS 率差约 0.92%**，**与扩样结论一致：无明显一方系统性更差**。**近似问题 PASS**：yf1 **7**、yf2 **10**。**回归**：无代码变更。**GUA-020**：样本量已达对照文档「≥10 局」口径（按 `game_id` 计）。 | **下轮唯一 priority**：在 **ISSUES** 将 **GUA-020** 标 **closed**（`closed_in` 本日 + 本摘要）；工程上**不必**为 yf2 单开路由；后续转 **提升对 lalala 胜率**、**分阶段/问题 PASS** 细拆或新开 GUA。 |
| 2026-04-21 | M1 减少问题 PASS / m-dev | **GUA-021** | **改动**：（本次无代码变更；仅追加 `game_records/`。若已改 `src/decision/` 请在本列补 commit/文件）。 | **GUA-021 跑局后（`game_records/`）**：成对 **`game_id` 共 12 个**（在 GUA-020 的 10 个基础上新增 **`20260421125139953810`**、**`20260421125448787594`**）。**合并 12 局**：yf1_m1 **272** 条决策、PASS **146**（**53.68%**），近似问题 PASS **7**；yf2_m1 **270** 条、PASS **150**（**55.56%**），近似问题 PASS **13**；**合计近似问题 PASS = 20**。对照 **GUA-020 基线（10 局）** yf1+yf2 合计 **17**，**全量合并未下降（+3，约 +17.6%）**，**未满足**「较基线下降」的完成定义。**仅新增 2 局**内近似问题 PASS：yf1 **0**、yf2 **3**（其中 `20260421125448787594` 的 yf2 侧 **3**）。`victoryNum` 仍为 **`[0,3,0,3]`**（12 局皆然）。**结论**：在**无共用层改动**前提下，本轮为扩样；**GUA-021 保持 open**。 | **下轮**：先 **改 `src/decision/`**（或声明基线重标）再跑 ≥5 对局复测；或转 **队胜率** 新开/改 GUA。 |
| 2026-04-21 | M1 GUA-021 共用层收紧 PASS / m-dev | **GUA-021** | **代码**：`src/decision/stage_router.py`（`BasePhaseHandler._action_list_has_non_pass`）；`src/decision/phase_handlers.py`（MidEarly/MidLate/Endgame 被动：队友分支仅当无非 PASS 才 PASS；`MidEarlyPassiveHandler._handle_single_passive` 策略分支改 `_default_passive_action`；`_handle_pair_passive` 队友分支对齐）；`src/decision/rule_based_decision_engine_m1.py`（手牌校验失败时 `_first_non_pass_index` 兜底，避免绕过 StageRouter 仍落 PASS）；`src/decision/intelligent_router.py`（`_is_passive_play` 与 StageRouter 对齐，避免误判被动）。 | **改后复测（`game_records/`）**：成对 **`game_id` 共 15 个**（新增 **`20260421130739321564`**、**`20260421130925429680`**、**`20260421131040387649`**）。**全量 15 局合并**（含改前旧局）：近似问题 PASS yf1 **7**、yf2 **13**、**合计 20**（旧局仍拉高合计）。**仅上述 3 个新 `game_id` 合并**（假定为本轮改代码后新跑）：yf1 **68** 条决策、近似问题 PASS **0**；yf2 **69** 条、近似问题 PASS **0**——**新局合计 0**。**`victoryNum`**：15 局均为 **`[0,3,0,3]`**。**结论**：新采样下 **GUA-021 指标达标**；**ISSUES** 将 **GUA-021** 标 **closed**（见备注：全量合并仍混历史，后续评测可只统计「改后 `game_id`」或清空旧记录再测）。 | **队胜率**（YiFei 对 lalala）；或仅保留改后 `game_records` 再跑一轮全量合并以验证 **合计近似问题 PASS** 是否整体下降。 |
| 2026-04-21 | M1 GUA-022/014 共用层落地 / m-dev 主 | **GUA-022**，**GUA-014**（联动，≤2 条） | **代码**：`src/decision/strategy_engine.py`（对手冲关时减弱队友让牌、提高队友保护触发阈值）；`src/decision/enhanced_priority_system.py`（钢板/三连对相对三张、加重主动拆三张惩罚）；`tests/test_decision_gua022_gua014.py`。**提交**：以本机 `git` 为准；提交后在本列补 **commit hash**。 | **自动化**：`pytest tests/test_decision_gua022_gua014.py` — **5 passed**。**本轮离线批跑（2026-04-21）**：按 `LOCAL_EVAL_CHECKLIST` 跑 `--target-games 10`（M1 四客户端，离线服 `D:/GDAI/server/windows/guandan_offline_v1006.exe`），`execution_state.json`：`target_games=10`、`completed_games=10`、`last_update=2026-04-21T14:42:04.453987`；日志：`logs/batch_executor_20260421_144051.log`（收尾为「已完成 10 / 系统正常退出」）。**新增成对 `game_id`（10）**：`20260421144152547644`、`20260421144152344839`、`20260421144152147671`、`20260421144151967958`、`20260421144151561222`、`20260421144151343636`、`20260421144151128593`、`20260421144150694829`、`20260421144150426323`、`20260421144150154557`。**指标（合并 10 对）**：yf1_m1 决策 **217**、PASS **108**（**49.77%**）、近似问题 PASS **0**；yf2_m1 决策 **225**、PASS **124**（**55.11%**）、近似问题 PASS **9**；近似问题 PASS 合计 **9**。**胜负**：0+2 队胜场局数 **0/10**（未出现 YiFei 队胜场）；可读到 `victoryNum` 的样本显示 **`[0,3,0,3]`**，其余新局 `victoryNum` 字段为空（记录侧需持续观察）。**结论**：批跑稳定性达标，但 **GUA-022 未达关单条件**（胜场仍 0）。 | **下轮**：继续在 `src/decision/` 强化队级进攻/压制并复跑 ≥10 对；若 `victoryNum` 持续缺失，先并行排查记录链路后再判胜负收敛。 |
| 2026-04-21 | M1 GUA-022/014 第二轮验收 / m-dev 主 | **GUA-022**，**GUA-014**（联动） | **代码（仅 `src/decision/`）**：进一步收紧 `strategy_engine.py` 队友保护逻辑（对手冲关时禁止无脑 full protect，改最小代价压制；同型比较牌值后再判可压；最小压制优先非炸弹/少张/小牌），并补充 `tests/test_decision_gua022_gua014.py`（新增 2 条用例，合计 7 passed）。 | **离线评测（M1+lalala，成对 `game_id`=10）**：`20260421144817345107`、`20260421144817078604`、`20260421144816856490`、`20260421144816389468`、`20260421144816179884`、`20260421144815811521`、`20260421144815584011`、`20260421144815025735`、`20260421144814502522`、`20260421144813338421`。`victoryNum`：0+2 胜场局数 **0/10**（未达关单）；其中仅 1 局有值 `[0,3,0,3]`，其余 **9/10** 为空（记录链路风险仍在）。PASS/近似问题 PASS：yf1 **244/129=52.87%**、近似 **3**；yf2 **230/109=47.39%**、近似 **3**；合计近似 **6**。**证据**：日志 `logs/batch_executor_20260421_144719.log`；`execution_state.json`：`target_games=10`、`completed_games=10`、`last_update=2026-04-21T14:48:32.296071`。 | **下轮唯一 priority**：先并行修复 `victoryNum` 落盘稳定性（记录链路）后，再继续 GUA-022 策略收敛并复跑 ≥10 对。 |
| 2026-04-21 | M1 victoryNum 链路修复后复跑 / m-dev 主 | **GUA-022**（主），**GUA-014**（联动） | **P0 记录链路修复**：`src/communication/yf1_m1.py`、`src/communication/yf2_m1.py`（新增 pending 结果回填机制：`episodeOver` 先落盘的空 `result` 在 `gameResult` 到达后批量补写 `victoryNum`；并统一提取结果字段，避免仅更新“最近一条”）。决策层延续：`src/decision/strategy_engine.py`。 | **离线复跑（M1+lalala，成对 `game_id`=10）**：`20260421145353437443`、`20260421145353186751`、`20260421145352483649`、`20260421145352262301`、`20260421145351828706`、`20260421145351493580`、`20260421145351251524`、`20260421145350860474`、`20260421145350628221`、`20260421145348392128`。**victoryNum 非空率 100%（10/10）**，均为 `[0,3,0,3]`，故 0+2 胜场局数 **0/10**（GUA-022 仍 open）。PASS/近似问题 PASS：yf1 决策 **220**、PASS **108**（**49.09%**）、近似 **1**；yf2 决策 **228**、PASS **114**（**50.00%**）、近似 **0**；合计近似 **1**。**证据**：日志 `logs/batch_executor_20260421_145255.log`；`execution_state.json`：`target_games=10`、`completed_games=10`、`last_update=2026-04-21T14:54:03.205494`。 | **下轮唯一 priority**：在 victoryNum 链路稳定前提下继续迭代 GUA-022 取胜策略（仍仅改共用层）并复跑 ≥10 对。 |
2026-05-24 | Phase 2 实际验证与优化 / m-dev | GUA-022 | choose_bomb()优化完成(100%测试通过率)，开始实际对局验证和context补全 | 测试：choose_bomb优化验证通过，context补全待执行 | Phase 2: 任务1实际对局验证(优先)，任务2 context补全，任务3 GUA-014联动
| 2026-05-25 | Phase 2 context 补全 / m-dev | **GUA-022**（主），GUA-014（联动） | **代码**：`game_recorder.sync_pass_counters`；`yf1_m1.py`/`yf2_m1.py`（pass_num/my_pass_num 跟踪并注入 act 消息）；`stage_router._build_context`（numofnext/numofpre/numoffri/numofgreaterPos/pass_num/my_pass_num）；`strategy_engine._should_full_protect`（numofnext≤4 时不无脑 PASS）；重建 `tests/test_decision_gua022_gua014.py` | **自动化**：`pytest tests/test_decision_gua022_gua014.py` — **7 passed**。**对局**：未跑（须本机按 `LOCAL_EVAL_CHECKLIST.md`）。**GUA-022**：仍 open，关单条件队胜率 >50% 未验证 | **下轮唯一 priority**：人类本机跑 ≥10 对 M1 vs lalala，回填胜率；若仍 0% 继续共用层策略 + PHASE2-003 |
| 2026-05-25 | Phase2 context 本机批跑 / m-dev | **GUA-022**（主） | **代码**：`batch_executor/executor.py`（成对计数补 game_round 口径）；验证 commit `abfa698` + 上述 batch 修复 | **离线批跑（10 对，~73s）**：`execution_state.json` `completed_games=10/10`；日志 `logs/batch_executor_20260525_224215.log`。成对 `game_id`（10）：`20260525224310201640`…`20260525224316193031`。**victoryNum 非空 10/10**，均为 `[0,3,0,3]` → **M1 队胜 0/10（队胜率 0%）**。**PASS**：yf1 **234** 决策、PASS **141**（**60.26%**）、近似 **0**；yf2 **230**、PASS **129**（**56.09%**）、近似 **0**。**GUA-022 仍 open**（未达 >50%） | **下轮 priority**：PHASE2-003 拆牌优先级 |
| 2026-05-25 | PHASE2-003 拆牌优先级 / m-dev | **GUA-014**（主），**GUA-022**（联动） | **代码**：`stage_router.py`（`_rank_counts`、`_single_splits_trips_or_bomb`、`_action_breaks_scanned_complex`）；`enhanced_priority_system.py`（rank 计数拆牌惩罚、开局主动权重、Trips 降权）；`phase_handlers.py`（OpeningActive 过滤拆三张/复杂牌型、fallback 优先 TwoTrips；OpeningPassive 级牌/王先于拆对；MidEarly rank 拆牌检测）；`tests/test_decision_gua022_gua014.py`（+3 用例） | **自动化**：`pytest` — **10 passed**。**批跑**：`logs/batch_executor_20260525_230037.log`，队胜率 **0%**；yf1 PASS **51.10%**、yf2 近似 PASS **9** | **下轮 priority**：PHASE2-004 队级进攻 |
| 2026-05-25 | PHASE2-004 队级进攻 / m-dev | **GUA-022**（主），GUA-014（联动） | **代码**：`strategy_engine.py`（`TeamOffensiveStrategy`、`find_minimal_beat_action`；保护仅队友控牌、有非 PASS 不强制 PASS）；`stage_router.py`（`team_role`/`should_seize_control`/`_apply_team_strategies`）；`phase_handlers.py`（进攻→保护顺序）；`enhanced_priority_system.py`（`team_offensive` 因素）；`tests/test_decision_gua022_gua014.py`（**15 passed**） | **批跑（10 对，~60s）**：`logs/batch_executor_20260525_231600.log`；`game_id`（10）：`20260525231636779807`…`20260525231643850208`。**队胜率 0/10**（`[0,3,0,3]`）。**PASS**：yf1 **231**/117（**50.65%**）、近似 **0**；yf2 **251**/133（**52.99%**）、近似 **8**（较 PHASE2-003 后 9 略降，**未达 0**）。**GUA-022 仍 open** | **下轮 priority**：PHASE2-005 清 yf2 近似 PASS |
| 2026-05-26 | PHASE2-005 清 yf2 近似 PASS / m-dev | **GUA-022**（主） | **代码**：`stage_router.py`（`_is_passive_play` 接风判主动、`_coerce_non_pass_if_available`、`_avoid_unjustified_pass` 放宽）；`intelligent_router.py`（缓存命中后仍 coerce；`_is_passive_play` 与 StageRouter 对齐）；`phase_handlers.py`（接风 `_first_non_pass_index`、队友单张不提前 PASS）；`rule_based_decision_engine_m1.py`（decide 末段 coerce）；`tests/test_decision_gua022_gua014.py`（**19 passed**，含智能路由缓存回归） | **根因**：`IntelligentStageRouter` 按 `(my_remain, phase, is_passive, threat)` 缓存 route 索引，命中历史 **PASS** 时绕过 StageRouter 末段兜底。**批跑（10 对，~55s）**：`logs/batch_executor_20260526_000638.log`；`game_id`（10）：`20260526000725840558`…`20260526000729572922`。**队胜率 0/10**。**PASS**：yf1 **250**/136（**54.40%**）、近似 **10**；yf2 **233**/105（**45.06%**）、近似 **1**（PHASE2-004 后 **8→1**；余 1 为队友控牌 `greaterPos=0` 让牌）。**GUA-022 仍 open** | **下轮 priority**：yf1 近似 PASS（当前 10）清零；或残局炸弹冲刺提升胜率 |
| 2026-05-26 | Hermes 验收 PHASE2-001/003/004/005 / m-dev | **GUA-022**（主），**GUA-014**（联动） | **验收**：`pytest tests/test_decision_gua022_gua014.py` **19/19 passed**；审查 `git diff` 确认所有改动完整。`enhanced_priority_system.py` rank 拆牌惩罚/Trips 降权、`phase_handlers.py` OpeningActive/Passive 过滤拆结构、`TeamOffensiveStrategy` 队级进攻、智能路由缓存 coerce、接风判主动、多层 `_coerce_non_pass_if_available` 兜底。**批跑结果**：yf2 近似 PASS **8→1** ✅，yf1 近似 PASS 仍 **10** ⚠️，队胜率 **0/10** ❌。**4 任务全部已验收归档（TASKS.md）**。代码未提交（待定）。 | **全部验收通过** ✅。GUA-022 仍 open（队胜率 0%）。GUA-014 优化已落地。 | **下轮唯一 priority**：清零 yf1 近似 PASS（当前 10）；或残局炸弹冲刺策略提升 M1 胜率 |
| 2026-05-29 | m3 目录迁移 Phase 1 / m-dev | —（infra） | **代码**：`src/contracts/`（`IDecisionProvider` 草案 v0.1）；`src/m/`（platform、m1/m2/m3 re-export + `M3DecisionProvider`）；`src/v/learn/`、`src/v/nn/`；`tests/test_m3_contracts_layout.py`。**文档**：治理方案、MATRIX、handoff、guandan-brain README | **自动化**：`pytest tests/test_m3_contracts_layout.py` — **5 passed**。**行为**：无决策逻辑变更（re-export only） | **下轮 priority**：GUA-022 队胜率；或 m3 Phase 2 物理迁入 |
