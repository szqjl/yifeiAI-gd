# 迭代日志（Iteration Log）

> 每条迭代只回答四件事：（1）本轮要解决哪几条 `GUA-xxx`（最多 2 条）；（2）改了哪些文件/参数（可链 commit）；（3）评测：`EVAL.md` 中哪些 case fail→pass，哪些 pass→fail（回归）；（4）下轮**唯一** priority。

## 模板（在表格下方追加新行）

| 日期 | 迭代名/分支 | 目标 GUA | 改动摘要（文件/commit） | 评测结果摘要 | 下轮 priority |
|------|-------------|----------|-------------------------|--------------|---------------|
| YYYY-MM-DD | | GUA-xxx, … | | pass/fail；回归： | |

---

## 记录

| 日期 | 迭代名/分支 | 目标 GUA | 改动摘要（文件/commit） | 评测结果摘要 | 下轮 priority |
|------|-------------|----------|-------------------------|--------------|---------------|
| 2026-04-21 | M1 对照与文档 / m-dev | GUA-001, GUA-020 | 重写 `docs/掼蛋AI相关比赛汇总.md`（UTF-8 v2.0）；新增 `scenarios/M1_yf1_vs_yf2_comparison.md`；更新 ISSUES/EVAL/scenarios/README | GUA-001：文档可读性 **pass**。GUA-020（数据来自 `game_records/`，可解析 **2** 个 `game_id`）：`20260113110809115923`、`20260421122404427820`；示例路径 `game_records/20260421122404427820 [yf1_m1]-[opponent_1_3]-[23]-[None].json` 与 `... [yf2_m1]-...`。两队记录中均为 `victoryNum: [0,3,0,3]`（0+2 队未胜）。**合并两局**：yf1_m1 共 **47** 条「我的决策」、PASS **26**（PASS 率 **≈55.3%**）；yf2_m1 共 **48** 条、PASS **31**（**≈64.6%**）。**近似问题 PASS**（`actionList_size>1` 仍 PASS）：yf1 合计 **3**、yf2 合计 **8**。**分局**：`20260113` 局 yf1 PASS 率 **47.6%** vs yf2 **68.2%**（yf2 该局近似问题 PASS **6**）；`20260421` 局双方 **26** 条决策、PASS 率均为 **61.5%**，近似问题 PASS **3 vs 2**（**该局二者接近**）。**回归**：无代码变更，无 EVAL case pass→fail。 | **下轮唯一 priority**：再积累对局（目标仍建议 **≥10 个可解析 `game_id`**）或用脚本批量扫 `game_records/*yf*_m1*.json`；若持续显示 yf2 偏高，优先 **diff `yf1_m1.py`/`yf2_m1.py` 调用链与 `player_id`**，再决定是否收紧共用 `src/decision/`。 |
| 2026-04-21 | M1 GUA-020 扩样复测 / m-dev | GUA-020 | 无代码变更；仅新增 `game_records/` 下对局文件 | **GUA-020（扩样）**：`game_records/` 中可配对 **`game_id` 共 8 个**（`yf1_m1`+`yf2_m1` 各 8 份）：`20260113110809115923`，`20260421122404427820`，`20260421122833986325`，`20260421123017792144`，`20260421123140409339`，`20260421123337946652`，`20260421123509820608`，`20260421123641090777`；路径示例 `game_records/<game_id> [yf1_m1]-[opponent_1_3]-[...].json`。各局 `victoryNum` 均为 **`[0,3,0,3]`**（本批样本内 0+2 队未胜）。**合并 8 局**：yf1_m1 **188** 条「我的决策」、PASS **104**（**55.32%**）；yf2_m1 **184** 条、PASS **103**（**55.98%**）——**PASS 率差约 0.66%**，**整体已无明显「yf2 更差」**。**近似问题 PASS**（`actionList_size>1` 仍 PASS）：yf1 **7**、yf2 **10**（yf2 仍略多，幅度小）。**回归**：无代码变更。 | **下轮唯一 priority**：若台账以 **≥10 个 `game_id`** 为关单条件，再补 **2** 局成对记录；否则可将 **GUA-020** 标 **closed** 并备注「大样本下 yf1/yf2 PASS 率接近，优先排查**胜负与阶段策略**或**共用层**，而非单独分叉 `yf2_m1` 路由」。 |
| 2026-04-21 | M1 GUA-020 达标样 / m-dev | GUA-020 | 无代码变更；追加 `game_records/` 成对记录 | **GUA-020（≥10 个 `game_id`）**：可配对 **`game_id` 共 10 个**（`yf1_m1`+`yf2_m1` 各 10 份）：在 8 个 id 基础上新增 `20260421123919905488`，`20260421124132048517`；路径示例不变。各局 `victoryNum` 仍为 **`[0,3,0,3]`**。**合并 10 局**：yf1_m1 **230** 条决策、PASS **126**（**54.78%**）；yf2_m1 **228** 条、PASS **127**（**55.70%**）——**PASS 率差约 0.92%**，**与扩样结论一致：无明显一方系统性更差**。**近似问题 PASS**：yf1 **7**、yf2 **10**。**回归**：无代码变更。**GUA-020**：样本量已达对照文档「≥10 局」口径（按 `game_id` 计）。 | **下轮唯一 priority**：在 **ISSUES** 将 **GUA-020** 标 **closed**（`closed_in` 本日 + 本摘要）；工程上**不必**为 yf2 单开路由；后续转 **提升对 lalala 胜率**、**分阶段/问题 PASS** 细拆或新开 GUA。 |
| 2026-04-21 | M1 减少问题 PASS / m-dev | **GUA-021** | **改动**：（本次无代码变更；仅追加 `game_records/`。若已改 `src/decision/` 请在本列补 commit/文件）。 | **GUA-021 跑局后（`game_records/`）**：成对 **`game_id` 共 12 个**（在 GUA-020 的 10 个基础上新增 **`20260421125139953810`**、**`20260421125448787594`**）。**合并 12 局**：yf1_m1 **272** 条决策、PASS **146**（**53.68%**），近似问题 PASS **7**；yf2_m1 **270** 条、PASS **150**（**55.56%**），近似问题 PASS **13**；**合计近似问题 PASS = 20**。对照 **GUA-020 基线（10 局）** yf1+yf2 合计 **17**，**全量合并未下降（+3，约 +17.6%）**，**未满足**「较基线下降」的完成定义。**仅新增 2 局**内近似问题 PASS：yf1 **0**、yf2 **3**（其中 `20260421125448787594` 的 yf2 侧 **3**）。`victoryNum` 仍为 **`[0,3,0,3]`**（12 局皆然）。**结论**：在**无共用层改动**前提下，本轮为扩样；**GUA-021 保持 open**。 | **下轮**：先 **改 `src/decision/`**（或声明基线重标）再跑 ≥5 对局复测；或转 **队胜率** 新开/改 GUA。 |
| 2026-04-21 | M1 GUA-021 共用层收紧 PASS / m-dev | **GUA-021** | **代码**：`src/decision/stage_router.py`（`BasePhaseHandler._action_list_has_non_pass`）；`src/decision/phase_handlers.py`（MidEarly/MidLate/Endgame 被动：队友分支仅当无非 PASS 才 PASS；`MidEarlyPassiveHandler._handle_single_passive` 策略分支改 `_default_passive_action`；`_handle_pair_passive` 队友分支对齐）；`src/decision/rule_based_decision_engine_m1.py`（手牌校验失败时 `_first_non_pass_index` 兜底，避免绕过 StageRouter 仍落 PASS）；`src/decision/intelligent_router.py`（`_is_passive_play` 与 StageRouter 对齐，避免误判被动）。 | **改后复测（`game_records/`）**：成对 **`game_id` 共 15 个**（新增 **`20260421130739321564`**、**`20260421130925429680`**、**`20260421131040387649`**）。**全量 15 局合并**（含改前旧局）：近似问题 PASS yf1 **7**、yf2 **13**、**合计 20**（旧局仍拉高合计）。**仅上述 3 个新 `game_id` 合并**（假定为本轮改代码后新跑）：yf1 **68** 条决策、近似问题 PASS **0**；yf2 **69** 条、近似问题 PASS **0**——**新局合计 0**。**`victoryNum`**：15 局均为 **`[0,3,0,3]`**。**结论**：新采样下 **GUA-021 指标达标**；**ISSUES** 将 **GUA-021** 标 **closed**（见备注：全量合并仍混历史，后续评测可只统计「改后 `game_id`」或清空旧记录再测）。 | **队胜率**（YiFei 对 lalala）；或仅保留改后 `game_records` 再跑一轮全量合并以验证 **合计近似问题 PASS** 是否整体下降。 |
| 2026-04-21 | M1 GUA-022/014 共用层落地 / m-dev 主 | **GUA-022**，**GUA-014**（联动，≤2 条） | **代码**：`src/decision/strategy_engine.py`（对手冲关时减弱队友让牌、提高队友保护触发阈值）；`src/decision/enhanced_priority_system.py`（钢板/三连对相对三张、加重主动拆三张惩罚）；`tests/test_decision_gua022_gua014.py`。**提交**：以本机 `git` 为准；提交后在本列补 **commit hash**。 | **自动化**：`pytest tests/test_decision_gua022_gua014.py` — **5 passed**。**本轮离线批跑（2026-04-21）**：按 `LOCAL_EVAL_CHECKLIST` 跑 `--target-games 10`（M1 四客户端，离线服 `D:/GDAI/server/windows/guandan_offline_v1006.exe`），`execution_state.json`：`target_games=10`、`completed_games=10`、`last_update=2026-04-21T14:42:04.453987`；日志：`logs/batch_executor_20260421_144051.log`（收尾为「已完成 10 / 系统正常退出」）。**新增成对 `game_id`（10）**：`20260421144152547644`、`20260421144152344839`、`20260421144152147671`、`20260421144151967958`、`20260421144151561222`、`20260421144151343636`、`20260421144151128593`、`20260421144150694829`、`20260421144150426323`、`20260421144150154557`。**指标（合并 10 对）**：yf1_m1 决策 **217**、PASS **108**（**49.77%**）、近似问题 PASS **0**；yf2_m1 决策 **225**、PASS **124**（**55.11%**）、近似问题 PASS **9**；近似问题 PASS 合计 **9**。**胜负**：0+2 队胜场局数 **0/10**（未出现 YiFei 队胜场）；可读到 `victoryNum` 的样本显示 **`[0,3,0,3]`**，其余新局 `victoryNum` 字段为空（记录侧需持续观察）。**结论**：批跑稳定性达标，但 **GUA-022 未达关单条件**（胜场仍 0）。 | **下轮**：继续在 `src/decision/` 强化队级进攻/压制并复跑 ≥10 对；若 `victoryNum` 持续缺失，先并行排查记录链路后再判胜负收敛。 |
| 2026-04-21 | M1 GUA-022/014 第二轮验收 / m-dev 主 | **GUA-022**，**GUA-014**（联动） | **代码（仅 `src/decision/`）**：进一步收紧 `strategy_engine.py` 队友保护逻辑（对手冲关时禁止无脑 full protect，改最小代价压制；同型比较牌值后再判可压；最小压制优先非炸弹/少张/小牌），并补充 `tests/test_decision_gua022_gua014.py`（新增 2 条用例，合计 7 passed）。 | **离线评测（M1+lalala，成对 `game_id`=10）**：`20260421144817345107`、`20260421144817078604`、`20260421144816856490`、`20260421144816389468`、`20260421144816179884`、`20260421144815811521`、`20260421144815584011`、`20260421144815025735`、`20260421144814502522`、`20260421144813338421`。`victoryNum`：0+2 胜场局数 **0/10**（未达关单）；其中仅 1 局有值 `[0,3,0,3]`，其余 **9/10** 为空（记录链路风险仍在）。PASS/近似问题 PASS：yf1 **244/129=52.87%**、近似 **3**；yf2 **230/109=47.39%**、近似 **3**；合计近似 **6**。**证据**：日志 `logs/batch_executor_20260421_144719.log`；`execution_state.json`：`target_games=10`、`completed_games=10`、`last_update=2026-04-21T14:48:32.296071`。 | **下轮唯一 priority**：先并行修复 `victoryNum` 落盘稳定性（记录链路）后，再继续 GUA-022 策略收敛并复跑 ≥10 对。 |
| 2026-04-21 | M1 victoryNum 链路修复后复跑 / m-dev 主 | **GUA-022**（主），**GUA-014**（联动） | **P0 记录链路修复**：`src/communication/yf1_m1.py`、`src/communication/yf2_m1.py`（新增 pending 结果回填机制：`episodeOver` 先落盘的空 `result` 在 `gameResult` 到达后批量补写 `victoryNum`；并统一提取结果字段，避免仅更新“最近一条”）。决策层延续：`src/decision/strategy_engine.py`。 | **离线复跑（M1+lalala，成对 `game_id`=10）**：`20260421145353437443`、`20260421145353186751`、`20260421145352483649`、`20260421145352262301`、`20260421145351828706`、`20260421145351493580`、`20260421145351251524`、`20260421145350860474`、`20260421145350628221`、`20260421145348392128`。**victoryNum 非空率 100%（10/10）**，均为 `[0,3,0,3]`，故 0+2 胜场局数 **0/10**（GUA-022 仍 open）。PASS/近似问题 PASS：yf1 决策 **220**、PASS **108**（**49.09%**）、近似 **1**；yf2 决策 **228**、PASS **114**（**50.00%**）、近似 **0**；合计近似 **1**。**证据**：日志 `logs/batch_executor_20260421_145255.log`；`execution_state.json`：`target_games=10`、`completed_games=10`、`last_update=2026-04-21T14:54:03.205494`。 | **下轮唯一 priority**：在 victoryNum 链路稳定前提下继续迭代 GUA-022 取胜策略（仍仅改共用层）并复跑 ≥10 对。 |
2026-05-24 | Phase 2 实际验证与优化 / m-dev | GUA-022 | choose_bomb()优化完成(100%测试通过率)，开始实际对局验证和context补全 | 测试：choose_bomb优化验证通过，context补全待执行 | Phase 2: 任务1实际对局验证(优先)，任务2 context补全，任务3 GUA-014联动
| 2026-05-25 | Phase 2 context 补全 / m-dev | **GUA-022**（主），GUA-014（联动） | **代码**：`game_recorder.sync_pass_counters`；`yf1_m1.py`/`yf2_m1.py`（pass_num/my_pass_num 跟踪并注入 act 消息）；`stage_router._build_context`（numofnext/numofpre/numoffri/numofgreaterPos/pass_num/my_pass_num）；`strategy_engine._should_full_protect`（numofnext≤4 时不无脑 PASS）；重建 `tests/test_decision_gua022_gua014.py` | **自动化**：`pytest tests/test_decision_gua022_gua014.py` — **7 passed**。**对局**：未跑（须本机按 `LOCAL_EVAL_CHECKLIST.md`）。**GUA-022**：仍 open，关单条件队胜率 >50% 未验证 | **下轮唯一 priority**：人类本机跑 ≥10 对 M1 vs lalala，回填胜率；若仍 0% 继续共用层策略 + PHASE2-003 |
| 2026-05-25 | Phase2 context 本机批跑 / m-dev | **GUA-022**（主） | **代码**：`batch_executor/executor.py`（成对计数补 game_round 口径）；验证 commit `abfa698` + 上述 batch 修复 | **离线批跑（10 对，~73s）**：`execution_state.json` `completed_games=10/10`；日志 `logs/batch_executor_20260525_224215.log`。成对 `game_id`（10）：`20260525224310201640`…`20260525224316193031`。**victoryNum 非空 10/10**，均为 `[0,3,0,3]` → **M1 队胜 0/10（队胜率 0%）**。**PASS**：yf1 **234** 决策、PASS **141**（**60.26%**）、近似 **0**；yf2 **230**、PASS **129**（**56.09%**）、近似 **0**。**GUA-022 仍 open**（未达 >50%） | **下轮 priority**：PHASE2-003 拆牌优先级 |
| 2026-05-25 | PHASE2-003 拆牌优先级 / m-dev | **GUA-014**（主），**GUA-022**（联动） | **代码**：`stage_router.py`（`_rank_counts`、`_single_splits_trips_or_bomb`、`_action_breaks_scanned_complex`）；`enhanced_priority_system.py`（rank 计数拆牌惩罚、开局主动权重、Trips 降权）；`phase_handlers.py`（OpeningActive 过滤拆三张/复杂牌型、fallback 优先 TwoTrips；OpeningPassive 级牌/王先于拆对；MidEarly rank 拆牌检测）；`tests/test_decision_gua022_gua014.py`（+3 用例） | **自动化**：`pytest` — **10 passed**。**批跑**：`logs/batch_executor_20260525_230037.log`，队胜率 **0%**；yf1 PASS **51.10%**、yf2 近似 PASS **9** | **下轮 priority**：PHASE2-004 队级进攻 |
| 2026-05-25 | PHASE2-004 队级进攻 / m-dev | **GUA-022**（主），GUA-014（联动） | **代码**：`strategy_engine.py`（`TeamOffensiveStrategy`、`find_minimal_beat_action`；保护仅队友控牌、有非 PASS 不强制 PASS）；`stage_router.py`（`team_role`/`should_seize_control`/`_apply_team_strategies`）；`phase_handlers.py`（进攻→保护顺序）；`enhanced_priority_system.py`（`team_offensive` 因素）；`tests/test_decision_gua022_gua014.py`（**15 passed**） | **批跑（10 对，~60s）**：`logs/batch_executor_20260525_231600.log`；`game_id`（10）：`20260525231636779807`…`20260525231643850208`。**队胜率 0/10**（`[0,3,0,3]`）。**PASS**：yf1 **231**/117（**50.65%**）、近似 **0**；yf2 **251**/133（**52.99%**）、近似 **8**（较 PHASE2-003 后 9 略降，**未达 0**）。**GUA-022 仍 open** | **下轮 priority**：PHASE2-005 清 yf2 近似 PASS |
| 2026-05-26 | PHASE2-005 清 yf2 近似 PASS / m-dev | **GUA-022**（主） | **代码**：`stage_router.py`（`_is_passive_play` 接风判主动、`_coerce_non_pass_if_available`、`_avoid_unjustified_pass` 放宽）；`intelligent_router.py`（缓存命中后仍 coerce；`_is_passive_play` 与 StageRouter 对齐）；`phase_handlers.py`（接风 `_first_non_pass_index`、队友单张不提前 PASS）；`rule_based_decision_engine_m1.py`（decide 末段 coerce）；`tests/test_decision_gua022_gua014.py`（**19 passed**，含智能路由缓存回归） | **根因**：`IntelligentStageRouter` 按 `(my_remain, phase, is_passive, threat)` 缓存 route 索引，命中历史 **PASS** 时绕过 StageRouter 末段兜底。**批跑（10 对，~55s）**：`logs/batch_executor_20260526_000638.log`；`game_id`（10）：`20260526000725840558`…`20260526000729572922`。**队胜率 0/10**。**PASS**：yf1 **250**/136（**54.40%**）、近似 **10**；yf2 **233**/105（**45.06%**）、近似 **1**（PHASE2-004 后 **8→1**；余 1 为队友控牌 `greaterPos=0` 让牌）。**GUA-022 仍 open** | **下轮 priority**：yf1 近似 PASS（当前 10）清零；或残局炸弹冲刺提升胜率 |
| 2026-05-26 | Hermes 验收 PHASE2-001/003/004/005 / m-dev | **GUA-022**（主），**GUA-014**（联动） | **验收**：`pytest tests/test_decision_gua022_gua014.py` **19/19 passed**；审查 `git diff` 确认所有改动完整。`enhanced_priority_system.py` rank 拆牌惩罚/Trips 降权、`phase_handlers.py` OpeningActive/Passive 过滤拆结构、`TeamOffensiveStrategy` 队级进攻、智能路由缓存 coerce、接风判主动、多层 `_coerce_non_pass_if_available` 兜底。**批跑结果**：yf2 近似 PASS **8→1** ✅，yf1 近似 PASS 仍 **10** ⚠️，队胜率 **0/10** ❌。**4 任务全部已验收归档（TASKS.md）**。代码未提交（待定）。 | **全部验收通过** ✅。GUA-022 仍 open（队胜率 0%）。GUA-014 优化已落地。 | **下轮唯一 priority**：清零 yf1 近似 PASS（当前 10）；或残局炸弹冲刺策略提升 M1 胜率 |
| 2026-05-29 | m3 Phase 2 物理迁入 + 契约 v1.0 / m-dev | —（infra） | **代码**：11 个 M1 模块 → `src/m/m1/`；4 个 V 引擎 → `src/v/`；`decision/*` shim；客户端 `m.m1` / `v.learn` / `v.nn` | **自动化**：契约 + GUA 回归 **31 passed** | **下轮 priority**：GUA-022 队胜率 |
| 2026-05-29 | M2/M3 引擎迁入 / m-dev | —（infra） | **代码**：M2/M3 → `src/m/m2/`、`src/m/m3/`；客户端 `m.m2`/`m.m3` | **自动化**：**33 passed** | **下轮 priority**：GUA-022 |
| 2026-05-29 | docs 根目录归类 / m-dev | —（docs） | **文档**：24 篇迁入子目录；[DOCUMENT_AUDIT.md](../governance/DOCUMENT_AUDIT.md)；原路径 stub | — | **下轮 priority**：乱码文档 UTF-8 修复（可选） |
| 2026-05-29 | docs UTF-8 修复 + 架构 M/V 对齐 / m-dev | —（docs） | **工具**：`scripts/tools/fix_doc_encoding.py`；**文档**：架构/开发指南/知识框架/一等奖分析等 7+2 篇；架构 **v2.7** 增补 `src/m/`、`src/v/`、`contracts/` | — | **下轮 priority**：GUA-022 队胜率；开发指南 frontmatter 模板 |
| 2026-05-29 | Phase 5 launchers 第一步 / m-dev | —（infra） | **`scripts/launchers/`**（m/v-learn/v-nn/training/workflow/tools/checks）+ 根目录 25 个 bat stub；`migrate_launchers_phase5.py` | — | **下轮**：`batch_executor_gui_m1.py`→`scripts/gui/`；根目录 md 清仓 |
| 2026-05-29 | Phase 5b 根目录散落物 / m-dev | —（infra） | [ROOT_ARTIFACT_AUDIT.md](../governance/ROOT_ARTIFACT_AUDIT.md)；md/json/sh 迁入 `notes/`、`fixes/`、`data/archive/eval/`、`scripts/shell/` | — | **下轮**：其余根目录 md（KANBAN、AUTO_RESTART_* 等） |
| 2026-05-29 | Phase 5c 运行时/工具配置 / m-dev | —（infra） | 锁→`tmp/`；`yfscore`→`data/archive/match-logs/`；`.cursorrules`→`.cursor/rules/*.mdc` | — | **下轮**：根目录剩余 md |
| 2026-05-29 | Phase 5d 根目录 md 清仓 / m-dev | —（docs） | KANBAN、AUTO_RESTART_*、GAME_RECORD_*、README_M1_* 等 14 篇迁入 `governance/`、`notes/`、`fixes/`、`training/archive/` | — | **下轮**：`batch_executor_gui_m1.py`→`scripts/gui/` |
| 2026-05-29 | Phase 5e GUI + shell 收尾 / m-dev | —（infra） | `batch_executor_gui_m1.py`、`start_gui.py`→`scripts/gui/`（根 stub）；4 个 `.sh`→`scripts/shell/`；`START_M1_GUI.bat` 更新；`scripts/gui/README.md`、`scripts/shell/README.md` | — | **下轮**：Phase 5 docs 归档 |
| 2026-05-29 | Phase 5f docs 归档 / m-dev | —（docs） | `rules/`、`skill/`→`docs/archive/`；`claude-analysis/`→`analysis/agent-sessions/`；`lalala_src/*.py`→`reference/lalala/`；`check_doc_paths.py`；更新 DOCUMENT_AUDIT、STARTUP_SCRIPTS_README | `check_doc_paths` pass | **下轮**：GUA-022 队胜率 |
| 2026-05-29 | 清空 game_records / m-dev | —（data） | 本地 **1488** 个 JSON 已删除；`.gitignore` 改为 `game_records/*` + 保留 `README.md` | 历史结论已写入 ITERATIONS/EVAL；无代码回归 | **下轮**：GUA-022 新跑局生成新记录 |
| 2026-05-29 | Phase 5g implementation 归档 / m-dev | —（docs） | `docs/implementation/`→`docs/archive/implementation/`；`migrate_docs_phase5g.py` | — | **下轮**：GUA-022 队胜率 |
| 2026-05-29 | GitHub/develop 清理 / m-dev | —（infra） | `sync_github_mirror.ps1`；`git-setup-guide` 弃用说明；治理 Phase 1 勾选 | 本机需自跑脚本同步 GitHub | **下轮**：Phase 5 治理结案 |
| 2026-05-29 | **Phase 5 治理结案** / m-dev | —（infra） | Phase 0–5 + 根目录/docs/launchers 归档完成；`game_records` 清空；治理 §9 标「仓库整理已结案」；提交 `72b117c`/`6505778` | 仓库整理无业务回归项 | **下轮唯一 priority**：**GUA-022** 队胜率（本机新跑局） |
| 2026-05-29 | M3 PASS 记牌 + actIndex 防护 / m-dev | —（infra, m3） | **`m3_decision_engine._update_play_state`**：`curAction[0]=="PASS"` 跳过记牌、过滤非法牌串；**`on_message`** clamp `actIndex`；**`yf1_m3`/`yf2_m3`**：仅 `type==act` 发送、notify 不 send、越界 clamp | **批跑 2 局**：无 `KeyError`/`IndexError`；服务端未崩溃；`game_records` 有成对 yf1/yf2 记录；`victoryNum` 正常 | **下轮 priority**：GUA-022（M1 队胜率）；M3 集成可继续扩样 |
| 2026-05-29 | GitHub 镜像 push-only 同步 / m-dev | —（infra） | **`sync_github_mirror.ps1`**：fetch 可 `-SkipFetch`、失败重试；本机 `curl 56` 时跳过 fetch 直接 push；已删 `github/develop`、推送 `github/m-dev` | push 成功；full fetch 仍可能因网络中断 | **待办**：M3 play 全 PASS 排查；GUA-022 |
| 2026-05-29 | **GUA-024** M3 play 全 PASS 根因修复 / m-dev | GUA-024 | **`m3_decision_engine`**：`curAction[1]`/`action[1]` 替代误写的 `[-1]`；`_rule_parse` 补 `greaterPos==myPos` 主动与 fallback | **11 passed**（含 `test_m3_passive_uses_action_rank_not_cards_list`）；模拟被动应出 `Single/9` 非 PASS | **下轮**：M3 批跑验证出牌率；GUA-022 |
| 2026-05-29 | M3 细化 Debug + 字符串化根因闭环 / m-dev | GUA-024 | **`_dbg` 双通道**（console+log）；**`_ensure_list`** 规范化 cur/greaterAction/actionList；541958 验证 dispatch 361×list、0×str、116 非 0 actIndex（yf1） | 字符串根因闭环；瓶颈转为策略层 `_Single` RETURN0 保守 | **下轮**：GUA-025 回放；GUA-026 M3 三带二 |
| 2026-05-30 | **GUA-025** 回放手牌误合并修复 / m-dev | GUA-025 | **`game_recorder._merge_same_game_records`**：按文件名 `[opponent]-[round]-[level]` 匹配 + 最近 game_id；统一 str 键；**`tests/test_game_recorder_merge.py`** 4 passed | round 19 回放：pos0 正确含 SA、2 个 3（非 round 12 的 4 个 3）；step20 扣牌模拟通过 | **下轮 priority**：**GUA-026** M3 `_ThreeWithTwo` 拆牌/炸弹保护 |
| 2026-05-30 | **GUA-027** M3 场态消息重大发现文档化 / m-dev | GUA-027 | **文档**：[`M3_DIAGNOSIS.md`](M3_DIAGNOSIS.md) 增补「WebSocket 场态用法」；[`ISSUES.md`](ISSUES.md) 登记 GUA-027。**依据**：v1006 说明书、`audit_greater_in_records.py`（19 局 ~40% 单牌 greater 异常） | 无代码变更；结论：非读错字段名，而是 **greaterAction/playArea 未用全** | **下轮 priority**：**GUA-027** 实现 passive 用 greaterAction + playArea 重算（优先于 GUA-026 策略调参） |
| 2026-05-30 | **GUA-027** 场态重算实现 / m-dev | GUA-027 | **`src/game_logic/trick_state.py`**（`resolve_effective_greater`、`TrickSequenceTracker`）；**`m3_decision_engine`** act 校正 + `_passive` 用 `beatAction`；**`yf_replay`** 每步「本圈最大(重算)」；**`tests/test_trick_state_gua027.py`** 8 passed | **22 passed**（含 M3 契约 + merge）；38 局录制 **40.4%** 步「重算 greater ≠ 录制 greater」（与单牌错发审计 ~40.5% 一致） | **下轮 priority**：**GUA-028** 说明书三项对齐 |
| 2026-05-30 | **GUA-028** v1006 三项对齐 / m-dev | GUA-028 | **`platform_act.py`**（`clamp_act_index`、`normalize_play_act_fields`）；**TripsPair**→`_ThreeWithTwo`；**act** 同步 `publicInfo.rest`；**yf1/yf2_m3** indexRange clamp | **24 passed**（GUA-027+028+契约） | **下轮 priority**：**GUA-026** 三带二策略；本机 M3 批跑 |
| 2026-05-30 | M3 批跑验证 GUA-027/028 / m-dev | GUA-027，GUA-028（验收） | **`batch_executor/executor.py`**：修复 `_GAME_RECORD_ROUND_PATTERN`（`[tag]-[opponent]` 缺 `]` 导致 M3 成对计数恒为 0）。**批跑**：`--target-games 10`（yf1/yf2_m3 + lalala client3/4，离线服 v1006）；`execution_state.json` `completed_games=10/10`（~55s，batch 1）。**成对 round（10）**：`16`–`25`（文件名 `[game_round]`）。**victoryNum 非空 10/10**，均为 `[0,3,0,3]` → **M3 队胜 0/10**。**PASS**：yf1 **139** 决策 / PASS **53**（**38.1%**）；yf2 **157** / **91**（**58.0%**）。**GUA-028 信号**：日志 `publicInfo.rest synced` yf1 **504** / yf2 **509**；`TripsPair` **0**（已走 `_ThreeWithTwo`）。**GUA-027 信号**：本 run 日志 **GUA-027 greater corrected 0**（act 场态与 playArea 一致，未触发校正；单元测 **8 passed** 仍有效）。**出牌**：idx>0 yf1 **169** / yf2 **160**（非全 PASS）。**回归**：`pytest test_trick_state_gua027 + test_m3_platform_align_gua028` **13 passed**。 | **下轮唯一 priority**：**GUA-026** M3 `_ThreeWithTwo` 拆牌/炸弹保护（本批 ThreeWithTwo 出牌 **22** 次，队胜率仍 0%） |
| 2026-05-30 | **GUA-026** 三带二拆牌/炸弹保护 / m-dev | GUA-026 | **清理**：`game_records/` **130** JSON 全删；`replay_word.md` 重置。**代码**：`m3_decision_engine` — `_uses_level_rank_cards`、`_three_with_two_protect_ok`、`_pick_three_with_two`；`_ThreeWithTwo` 统一过滤（不拆炸弹、不消耗级牌、常态不拆 trips、移除逢人配三带二 fallback）。**测试**：`tests/test_m3_gua026.py` **3 passed**；GUA-027/028 回归 **13 passed** | 无批跑；GUA-026 仍 **open**（待净盘批跑 ≥10 对） | **下轮唯一 priority**：M3 批跑验证 GUA-026（净 `game_records`） |
| 2026-05-30 | GUA-026 净盘批跑验收 / m-dev | GUA-026 | 无代码变更。**批跑**：`--target-games 10`（GUA-026 后首跑）；`completed_games=10/10`（~54s）。**成对 round（10）**：`7`–`16`（共落盘 32 文件 / 16 成对 round）。**victoryNum 10/10** 均为 `[0,3,0,3]` → **M3 队胜 0/10**。**PASS**：yf1 **155** 决策 / PASS **74**（**47.7%**）；yf2 **156** / **73**（**46.8%**）。**ThreeWithTwo**：yf1 **16**、yf2 **8**；**含级牌三带二** yf1 **2** + yf2 **1**（较 GUA-026 前 round19 式 555+22 明显减少，未归零—待查是否非 2 级局或 actionList 无替代）。**回归**：`test_m3_gua026` **3 passed** | **GUA-026 仍 open**（队胜率未改善）；**下轮 priority**：分析 3 手级牌三带二 + 近似问题 PASS / 队级进攻 |
| 2026-05-30 | **GUA-029** M3 炸弹可执行规则包（本轮） / m-dev | **GUA-029** | **台账**：[`ISSUES.md`](ISSUES.md) 登记 GUA-029（R1–R6 完成定义）。**分析**：[`01_bomb_techniques.md`](../knowledge/skills/02_main_attack/01_bomb_techniques.md) 提炼 6 条可注入规则；样例局 `20260530172743739854`（`replay_word.md`）— yf1 **5×8** 全程未出，step46 对手 Q 炸弹 / step74–78 对手 **≤5 张**三带二走光。**根因（待修）**：`m3_utils.choose_bomb` 用 `action[-1]` 非 v1006 的 `action[1]` → TypeError → `yf1_m3`/`yf2_m3` 异常兜底 **actIndex=0**；`_Bomb` 门槛 `cur_Bomb_num>=3`；`_active` 无炸弹路径。**计划改动**：`m3_utils.choose_bomb`、`m3_decision_engine`（R2–R6）、`tests/test_m3_gua029.py` | **待实施**（无代码变更）。验收见 ISSUES GUA-029：**R1** 格式修复 + **R2** 回炸 + **R3** ≤7 阻断 + **R4** 炸不打四 + **R5** 不压队友 + **R6** 残局 one_hand/`_active`；净盘 M3 批跑 ≥10 对（炸弹出牌 >0） | **下轮唯一 priority**：落地 R1–R6 并 `pytest test_m3_gua029`；净盘批跑回填炸弹次数与队胜率 |
| 2026-05-30 | **GUA-029** R1–R6 落地 / m-dev | **GUA-029** | **代码**：`m3_utils.choose_bomb` 对齐 v1006（`action[1]`）；`one_hand` 炸弹点数同修。**`m3_decision_engine`**：`_is_teammate_greater`、`_gua029_r4_allows_bomb`、`_gua029_try_bomb`、`_gua029_passive_sprint_bomb`；`_Bomb` 改 R2 必回炸；`_passive` 末段 R3 兜底；`_active` R6（≤10 张 one_hand + 炸弹一手清）。**测试**：`tests/test_m3_gua029.py` **8 passed**；GUA-026/027/028 回归 **16 passed** | 无批跑。**GUA-029 仍 open**（待净盘批跑：炸弹出牌 >0、队胜率） | **下轮唯一 priority**：净盘 M3 批跑 ≥10 对验收 GUA-029 |
| 2026-05-30 | GUA-029 批跑 3 局验收 / m-dev | **GUA-029** | 无代码变更。**批跑**：`--target-games 3`（yf1/yf2_m3 + lalala）；`execution_state.json` **3/3**（~54s）；日志 `logs/batch_executor_20260530_220728.log`。**炸弹（服务器日志 send 计数）**：yf1 **69**、yf2 **65**（GUA-029 前本 run 恒 0 级 bug 已消除）。**最新 3 局 `victoryNum`**：`[3,0,3,0]`×3（lalala 侧仍占优）。**game_id 样例**：`20260530220822302911` 等 | **GUA-029 仍 open**（炸弹已出；队胜率未达标，建议净盘 10 对再统计） | **下轮 priority**：净盘 `--target-games 10` 或分析 `[3,0,3,0]` 游次含义 |
| 2026-05-31 | **GUA-030** 原则映射表（文档） / m-dev | **GUA-030** | **仅文档**：[`PRINCIPLES_MAPPING.md`](PRINCIPLES_MAPPING.md)（P-C/J/G/F/H + **S-PR/ST/BS** → **P0→M3**、**P1+→V5+**）；[`ISSUES.md`](ISSUES.md) 登记；**未改 V 代码** | 无 pytest / 批跑；02/03 评估：02 无 P0、03 无 P0 | **下轮 priority**：M3 P0（P-H01/P-H05）；P1 逢五出对 + 还贡；GUA-029 净盘 10 对 |
| 2026-05-31 | **GUA-030** 关单 / m-dev | **GUA-030** | 用户确认三篇映射表 OK；**ISSUES** 标 **closed**（`closed_in` 2026-05-31） | — | **下轮唯一 priority**：**GUA-029** 净盘 ≥10 对；M3 P0（P-H01/P-H05）另开迭代行 |
| 2026-05-31 | **GUA-031** 传牌 guard 登记（待实施） / m-dev | **GUA-031** | **仅文档**：[`ISSUES.md`](ISSUES.md) 登记 GUA-031 + §8.4 完成定义；目标 **PASS-P02/P03/P04** + **P-F02** 扩全牌型；**无代码变更** | 待实施 | **下轮唯一 priority**：落地 GUA-031 + `test_m3_gua031.py`；或并行 GUA-029 净盘 10 对 |
| 2026-05-31 | **GUA-032** 算牌规则登记（待实施） / m-dev | **GUA-032** | **仅文档**：§14 映射 + **`remain_cards_classbynum` stale 发现**；目标 **CALC-M01/M02/M03**；**无代码变更** | 待实施 | **下轮 priority**：GUA-032 基建同步 + pytest；与 GUA-031 可并行 |
| 2026-05-31 | 红桃配 §十六 同理评估（文档） / m-dev | —（docs） | **仅文档**：[`PRINCIPLES_MAPPING.md`](PRINCIPLES_MAPPING.md) §十六（**RH-P01–P10**）；[`06_red_heart_usage.md`](../knowledge/skills/04_common_skills/06_red_heart_usage.md) + [`skills/README.md`](../knowledge/skills/README.md) 索引；**GUA-026** 交叉引用；**不新开 GUA** | **M3 P0/P1 新增 0**；85% 配炸/12% 补缺 → **V5+ 组牌**；M3 已有 `H+curRank`、GUA-026/029 | **下轮 priority**：GUA-029 净盘 ≥10 对；GUA-031/032 实施；V5 组牌层 RH-P01–P06 |
| 2026-05-31 | 钢板 §十七 同理评估（文档） / m-dev | —（docs） | **仅文档**：[`PRINCIPLES_MAPPING.md`](PRINCIPLES_MAPPING.md) §十七（**TT-P01–P12**）；[`07_two_trips_skills.md`](../knowledge/skills/04_common_skills/07_two_trips_skills.md) + README；**GUA-031** 真源扩列；**不新开 GUA** | **M3 P0/P1 新增 0**；已有 `_TwoTrips`/`rankfour`/R6；牌力分时 → **V5+** / M1 已有 | **下轮 priority**：GUA-031 扩 `_TwoTrips` 队友 PASS；GUA-029 R6 六张清手验收 |
| 2026-05-31 | 顺子 §十八 同理评估（文档） / m-dev | —（docs） | **仅文档**：[`PRINCIPLES_MAPPING.md`](PRINCIPLES_MAPPING.md) §十八（**STG-D01–E06**）；[`08_straight_skills.md`](../knowledge/skills/04_common_skills/08_straight_skills.md) + README；**GUA-031/032** 真源扩列；**不新开 GUA** | **M3 P0/P1 新增 0**；**P-H05**、**CALC-M03** 已在 backlog；`_Straight` 较完整 | **下轮 priority**：P-H05 + GUA-032 CALC-M03；GUA-031 送顺/不接队友顺 |
| 2026-05-31 | 木板 §十九 同理评估（文档） / m-dev | —（docs） | **仅文档**：[`PRINCIPLES_MAPPING.md`](PRINCIPLES_MAPPING.md) §十九（**TP-P01–P12**）；[`09_three_pair_skills.md`](../knowledge/skills/04_common_skills/09_three_pair_skills.md) + README；**GUA-031** 真源扩列；**不新开 GUA** | **M3 P0/P1 新增 0**；`_ThreePair` 与 §十七同构；首引慎出 → **P-H05** 可扩 | **下轮 priority**：GUA-031 合并 `_TwoTrips`+`_ThreePair` 队友 PASS/喂牌 |
| 2026-05-31 | 三带二 §二十 同理评估（文档） / m-dev | —（docs） | **仅文档**：[`PRINCIPLES_MAPPING.md`](PRINCIPLES_MAPPING.md) §二十（**TW2-D01–F10**）；[`10_three_with_two_skills.md`](../knowledge/skills/04_common_skills/10_three_with_two_skills.md) + README；**GUA-026** 升为主真源；**不新开 GUA** | **M3 P0/P1 新增 0**；GUA-026 已覆盖核心 guard；§五 喂牌 → **GUA-031** | **下轮 priority**：GUA-026 批跑验收；GUA-031 TW2-F* 喂牌/不接队友夯 |
| 2026-05-31 | 三张 §二十一 同理评估（文档） / m-dev | —（docs） | **仅文档**：[`PRINCIPLES_MAPPING.md`](PRINCIPLES_MAPPING.md) §二十一（**TRP-D01–F02**）；[`11_trips_skills.md`](../knowledge/skills/04_common_skills/11_trips_skills.md) + README；**GUA-031/026** 交叉引用；**不新开 GUA** | **M3 P0/P1 新增 0**；`_Trips` 完整；搅局/喂牌 → GUA-031 | **下轮 priority**：GUA-031 TRP-R01–R03 + P-F02 扩 `_Trips` |
| 2026-05-31 | **组牌 §二十二 ★** 同理评估（文档） / m-dev | —（docs） | **仅文档**：[`PRINCIPLES_MAPPING.md`](PRINCIPLES_MAPPING.md) §二十二（**CG-C01–D05** 共 40+ 条）；[`04_card_grouping_skills.md`](../knowledge/skills/07_opening/04_card_grouping_skills.md) + [`skills/README.md`](../knowledge/skills/README.md) **07_opening** 索引；**P-G01** 升总纲真源；**GUA-026/031/032** 交叉引用；**不新开 GUA** | **M3 P0/P1 新增 0**；V5 `enumerate_groupings` 规格；M3 可选 **CG-G01/B03/B05** P2 | **下轮 priority**：GUA-029 净盘 ≥10 对；GUA-031/032；V5 组牌层 CG-R01–R07 |
| 2026-05-31 | Handoff 接续文档 / m-dev | —（docs） | [`docs/analysis/handoffs/2026-05-31-M3-skills映射与组牌总纲.md`](../analysis/handoffs/2026-05-31-M3-skills映射与组牌总纲.md) — skills §十六–§二十二 评估摘要 + M3 待办 + 未提交清单 | — | **下轮唯一 priority**：见 handoff「下一步唯一动作」（GUA-029 净盘 ≥10 对） |
| 2026-05-31 | **GUA-029** 净盘批跑 ≥10 对验收 / m-dev | **GUA-029** | **无代码变更**。**批跑**：`python -m batch_executor --target-games 10`（yf1/yf2_m3 + lalala client3/4；离线服 `offline_platform/.../guandan_offline_v1006.exe`）；`execution_state.json` **10/10**（~73s）；日志 `logs/batch_executor_20260531_141658.log`。**说明**：`game_records/` 启动前未全删（目录内仍有历史 JSON），本 Run 按 mtime 新增 **224** 文件、**112** 成对 round（executor 台账 **39** 成对计数口径）。**炸弹**（actions 中 pos0/2 出 `Bomb`）：yf1 **175**、yf2 **152**（GUA-029 前 TypeError→PASS 已消除）。**PASS**：yf1 **2407** 决策 / **1188**（**49.4%**）；yf2 **2361** / **1169**（**49.5%**，较 GUA-027/028 批 yf2 **58.0%** 下降 ≈8.5pp）。**victoryNum**（本 Run 样本）：均为 **`[1, 2, 1, 2]`**（pos1/3 累计仍领先；**M3 队胜局数未达标**）。**回归**：`test_m3_gua029`+GUA-026/027/028 **24 passed** | **GUA-029 closed**（R1–R6 + 炸弹批跑验收通过）；队胜率仍归 **GUA-022** | **下轮唯一 priority**：**GUA-031** 传牌 guard（PASS-P02/P03/P04 + P-F02）+ `test_m3_gua031.py` |
| 2026-05-31 | **GUA-031** 传牌 guard 落地 / m-dev | **GUA-031** | **代码**：`m3_decision_engine` 增 `_gua031_passive_teammate_yield`、`_gua031_active_min_single`、`_gua031_filter_singles_for_next1`、`_gua031_active_feed_five`；P-F02 接入 `_Trips/_ThreePair/_TwoTrips/_Straight`；`_active` 覆盖 PASS-P02（队友 1 张送小单）、P03（下家报单禁过小单）、P04（队友 5 张优先 Pair/ThreeWithTwo）。**测试**：`tests/test_m3_gua031.py` **7 passed**；GUA-026/027/028/029 回归 **31 passed** | **GUA-031 closed**（4 条 guard + pytest）；队胜率仍归 **GUA-022** | **下轮唯一 priority**：**GUA-032** 记牌算牌（`remain_cards_classbynum` 同步 + CALC-M01/M03） |
| 2026-05-31 | batch_executor 台账计数修复（方案 A+C） / m-dev | —（infra） | **代码**：`batch_executor/executor.py` — `completed_games` 按平台批次 `+= batch_games`（强杀不加）；移除 `max(game_id,round)` 驱动进度与批内 round 提前结束；新增 `_scan_game_records_stats` / match_key 诊断（GUA-025 键）。**文档**：`batch_executor/README.md`、`EVAL.md`、`LOCAL_EVAL_CHECKLIST.md`。**测试**：`tests/test_batch_executor_counting.py` **5 passed** + integration/merge **9 passed**。**本机批跑（净盘）**：第 1 批 `batch_games=3→completed=3/10`、`restart_count=1`、日志出现「开始批次 2」；**全 4 批 10/10** 需在本地终端跑满（Agent 命令在 batch2 cleanup 处被 SIGTERM 中断，~84s） | 修复 **Premature 10/10** 根因；历史 M3「10/10 仅 1 会话」行应视为计数 bug | **下轮 priority**：本机复跑 `--target-games 10` 验收 4 批；**GUA-032** |
| 2026-05-31 | m-dev 推送治理合规补录 / m-dev | —（governance） | **冒烟**：§7.3 — `check_websocket_config`（GBK config 修复）、M3 客户端/引擎 import、batch `--diagnose-only`、pytest **48 passed**；**推送前** `verify_gitignore.py`。**文档**：handoff [`2026-05-31-m-dev-push-governance-compliance.md`](../analysis/handoffs/2026-05-31-m-dev-push-governance-compliance.md)。**未跑**：30 局 regression（本机无 `game_records`）；满跑 10 局 4 批 | §8.2 自动化项 **pass**；批跑/30 局 **待本机** | **下轮 priority**：本机净盘 `--target-games 10` 满跑 + `victoryNum[0] vs [1]` 回填 |
| 2026-05-31 | §8.2 30 局 offline regression / m-dev | —（governance §8.2） | **COS**：`pull_regression.py` **30/30 ok** → `data/artifacts/replays/regression-lalala-v1/`（不进 Git）。**离线**：`GameRecorder.load_game` **30/30 无 crash**；`analyze_decisions` + `audit_greater` 汇总。**pytest**：M3 + `test_game_recorder_merge` **43 passed, 3 skipped**。**报告**：[`regression-diff-2026-05-31.md`](../analysis/regression-diff-2026-05-31.md) | **§8.2 30 局 regression pass**（无 crash + 报告；不要求胜率；M3 逐步 replay diff 未接工具） | **下轮 priority**：本机 `--target-games 10` 满跑 4 批；**GUA-032** |
| 2026-05-31 | **GUA-033** 批末 victoryNum 异常登记 / m-dev | **GUA-033** | **仅文档**：[`ISSUES.md`](ISSUES.md) 登记 + §完成定义；根因核查（批 4 `batch_games=1` 但 `[2,1,2,1]`、`backfill` 49 副、M3 未读 `final`）；**无代码变更** | 批 1–3 末条 vn 自洽；**批 4 不可信**；10 局合计 **5–4（M3–lalala，仅批 1–3）** + 批 4 待定 | **下轮 priority**：落地 GUA-033（对齐 M1 gameResult 链）→ 重跑 10 局满跑验证 |
| 2026-05-31 | **GUA-033** gameResult 校验+fallback / m-dev | **GUA-033** | **代码**：`game_result_utils.py`；`yf1_m3`/`yf2_m3`（RAW、gameOver 早退、batch_wins fallback）；`game_recorder.backfill_victory_num` 校验；`batch_executor` 写/验 `current_batch.json`。**文档**：`platform-data-interpretation.md` §4.1/§4.3；[`gua033-batch-matrix-2026-05-31.md`](../analysis/gua033-batch-matrix-2026-05-31.md)。**测试**：`test_m3_gua033.py` **11 passed**。**矩阵净盘**：target 1/3/10；10 局 4 批批 4 **fallback** `[1,0,1,0]`，`[0]+[1]=1` ✓ | **GUA-033 closed**；真源：**`batch_games`**；禁止裸信 `gameResult.victoryNum`/`settingTimes` | **下轮 priority**：exe argv 实测定音 → **GUA-032** |
| 2026-05-31 | **GUA-033** exe argv 实测定音 / m-dev | **GUA-033**（文档） | **文档**：[`platform-data-interpretation.md`](../knowledge/platform-data-interpretation.md) **§2**；[`ISSUES.md`](ISSUES.md)「GUA-033 平台侧根因」；探测 `scripts/tools/probe_exe_argv_ws.py`。**无代码变更** | **定音**：本包 exe **argv 1/3/10 均固定 3 局/会话**；`settingTimes=3` 非字段陈旧；与 PDF 不一致 | **下轮 priority**：**GUA-032**；换 exe 须重跑 §2 矩阵 |
| 2026-05-31 | **GUA-032** 记牌算牌落地 / m-dev | **GUA-032** | **代码**：`m3_utils.sync_remain_cards_classbynum`；`m3_decision_engine` — `_update_play_state` 后同步 classbynum、MEM-M02 `_player_bomb_mem`、CALC-M01 被动炸过滤（`_gua032_filter_bomb_action_list`）、CALC-M03 顺子 5/10 降权（`_gua032_straight_degraded`）。**测试**：`test_m3_gua032.py` **6 passed**；GUA-029/031/033 回归 **34 passed** | **GUA-032 closed**（基建 + M01/M03 + MEM-M02）；**CALC-M02** 留 **P-H01** | **下轮 priority**：**GUA-022** 队胜率观测；**GUA-026** 批跑验收；CALC-M04/M05 扩展 |
| 2026-05-31 | **GUA-022 / GUA-026** M3 净盘批跑验收 / m-dev | **GUA-022**（M3 观测），**GUA-026**（批跑） | **无代码变更**。**净盘**：`game_records` 清空后 `--target-games 10`（yf1/yf2_m3 + lalala，~346s）。**台账**：`completed_games=10/10`，4 批（3+3+3+1）；日志 `logs/batch_executor_20260531_201339.log`。**队胜（批末 vn）**：**7/10（70%）** — 批1 `[1,2,1,2]`、批2 `[2,1,2,1]`、批3 `[3,0,3,0]`、批4 fallback `[1,0,1,0]`（RAW `[3,0,3,0]`）。**PASS**（4 批末快照合计）：yf1 **76** 决策 / PASS **44.7%**；yf2 **77** / **50.6%**；近似问题 PASS **0**。**ThreeWithTwo**：yf1 **2**、yf2 **2**；**含级牌 TW2** **0**（GUA-026 前批 yf1 2 + yf2 1）。**工具**：`scripts/tools/gua022_gua026_batch_report.py` | **GUA-022 仍 open**（主 scope **M1** 队胜率 >50% 未测；M3 **7/10** 仅观测）。**GUA-026 仍 open**（批跑验收完成；级牌 TW2 本批 **0**；待多样本确认） | **下轮 priority**：M1 批跑对照 **GUA-022**；或 M3 再跑 ≥10 局验证 **GUA-026** 稳定性 |
| 2026-05-31 | **GUA-022** M1 净盘 12 局批跑 / m-dev | **GUA-022** | **无代码变更**。**净盘**：`--target-games 12`（4×3 局/会话，yf1/yf2_m1 + lalala，~317s）。**台账**：`completed_games=12/12`，`restart_count=3`；日志 `logs/batch_executor_20260531_204844.log`。**队胜（批末 vn×4 批）**：**0/12（0%）**，批1–4 均为 **`[0,3,0,3]`**（lalala 全胜）。**PASS**（4 批末快照合计）：yf1 **79** 决策 / **59.5%**；yf2 **82** / **58.5%**；近似问题 PASS **0**。**工具**：`scripts/tools/gua022_m1_batch_report.py` | **GUA-022 仍 open**（M1 队胜 **0/12**，未达 >50%）；与 M3 同批观测 **7/10** 对照 | **下轮 priority**：M1 共用层队级进攻 / 残局炸弹；或 M1 vs M3 同 12 局 A/B 复跑 |
| 2026-05-31 | **M1 frozen 定音** / m-dev | **GUA-022**（关单） | **仅文档**：[`ISSUES.md`](ISSUES.md) 增「引擎维护策略」— **M1 = frozen / 非交付线**；**GUA-022 closed**（不再攻关 M1 队胜率；KPI 迁 **M3**） | M1 **0/12** 作基线保留；主交付 **M3**（批跑 **7/10**） | **下轮 priority**：**GUA-026** 关单 + M3 12 局复跑稳定性；CALC-M04/M05 |
| 2026-05-31 | **M/V 分工评审确认** / m-dev | —（docs） | **仅文档**：[`ISSUES.md`](ISSUES.md)「引擎维护策略」增补评审定音 — M3=主交付+`IDecisionProvider`；P0 guard→`m3_decision_engine`；组牌/牌力→V5+；勿开 M1 策略 GUA；M1 **0/12** vs M3 **7/10** 同机对照非口径错误 | 与 frozen 定音一致；[`README.md`](README.md)、`AGENTS.md` § 数据解读口径同步 | **下轮 priority**：**GUA-026** 关单 + M3 12 局复跑 |
| 2026-05-31 | **GUA-026** 关单 + M3 净盘 12 局复跑 / m-dev | **GUA-026** | **无代码变更**。**回归**：`test_m3_gua026`+GUA-029/031/032 **24 passed**。**净盘**：`game_records` 清空后 `--target-games 12`（yf1/yf2_m3 + lalala，~325s）。**台账**：`completed_games=12/12`，4 批（3+3+3+3）；日志 `logs/batch_executor_20260531_213847.log`。**队胜（批末 vn×4）**：**11/12（91.7%）** — 批1–3 **`[3,0,3,0]`**、批4 **`[2,1,2,1]`**。**PASS**（全量牌谱）：yf1 **3976** 决策 / **49.2%**；yf2 **3966** / **48.6%**；近似问题 PASS **0**。**ThreeWithTwo**：yf1 **247** + yf2 **245**；**H+curRank 出现在三带二** **39**（22+17，被动边界；未再现 555+22 拆炸）。**工具**：`gua022_gua026_batch_report.py` | **GUA-026 closed** | **下轮 priority**：**CALC-M04/M05**（GUA-032 扩展）或 M3 队胜率多样本观测 |
| 2026-05-31 | **M3 队胜率多样本观测** / m-dev | —（KPI 观测） | **无代码变更**。**净盘复跑 ×2**（各 `--target-games 10`，**历史口径**；此后改用 **3 的倍数**）。**S3**（~320s）：`logs/batch_executor_20260531_214833.log` — **8/10**。**S4**（~330s）：`logs/batch_executor_20260531_215608.log` — **8/10**。**四样本汇总**：S1 **7/10**（`201339`）、S2 **11/12**（`213847`）、S3/S4 各 **8/10** → **合计 34/42（81.0%）** | M3 队胜率 KPI：**70%–92%**；**>50%** 稳定达标 | **下轮 priority**：**CALC-M04/M05**；批跑档位定音 |
| 2026-05-31 | **批跑局数 3 倍数定音** / m-dev | —（docs+infra） | **约定**：`--target-games` 须 **3 的倍数**（exe 每会话 3 局）；档位 **3 / 9 / 12**。**代码**：`input_validator` 拒绝非整批目标；默认 **12**。**文档**：`EVAL.md`、`LOCAL_EVAL_CHECKLIST.md`、`README`、`AGENTS.md` § 数据解读口径、`platform-data-interpretation` §2.3；`client_sets.json` 增 **m3** | 避免 10 局尾批 `batch_games=1` fallback；历史 10 局样本保留对照 | **下轮 priority**：**CALC-M04/M05**（GUA-032） |
| 2026-05-31 | **M3 净盘 12 局批跑 S5** / m-dev | —（KPI 观测） | **无代码变更**。**净盘**：`game_records`/`logs` 清空后 `--target-games 12`（yf1/yf2_m3 + lalala，~387s）。**台账**：`completed_games=12/12`，4 批（3+3+3+3）；日志 `logs/batch_executor_20260531_221902.log`。**队胜（批末 vn×4）**：**8/12（66.7%）** — 批1 `[2,1,2,1]`、批2 `[3,0,3,0]`、批3 **`[1,2,1,2]`**（1/3）、批4 `[2,1,2,1]`。**PASS**（全量牌谱）：yf1 **6393** 决策 / **49.1%**；yf2 **6449** / **49.6%**；近似问题 PASS **0**。**ThreeWithTwo**：yf1 **373** + yf2 **366**；**H+curRank 三带二** **60+59**。**复盘样例**：批 3 中盘负批 — `replay_word.md` → `20260531222316040665 [40]-[K]`。**工具**：`gua022_gua026_batch_report.py` | M3 队胜率 KPI：**66.7%**（>50%）；较 S2 **11/12** 回落，仍处多样本区间 | **下轮 priority**：**CALC-M04/M05**（GUA-032）；或批 3 负局策略复盘 |
| 2026-06-01 | **贡牌录牌 P0/P1 + 净盘 3 局验收** / m-dev | —（infra/录牌） | **代码**：`yf_replay.py` — `apply_tribute_back_to_hand` 加载时扣/加贡还牌；`yf2_m3.py` 同步 yf1 notify tribute/back + `source`/`stage`。**测试**：`test_yf_replay_tribute_back.py` **6 passed**；`test_yf1_m3_back_notify.py` **16 passed**（yf1+yf2）。**净盘批跑**：`game_records` 清空后 `--target-games 3`（~103s）；`completed_games=3/3`；批末 **`victoryNum=[3,0,3,0]`** → M3 **3/3** 胜。**落盘**：**134** JSON、**67** 成对 match_key（副）。**贡还录牌**（my_decisions）：yf1 tribute act **23** / notify **17**、back act **17** / notify **23**；yf2 tribute act **22** / notify **20**、back act **20** / notify **22** | P0/P1 验收 **pass**；四类 source 齐全 | **下轮 priority**：P3 回放界面展示贡牌段；或 `replay_word` 更新样例局 |
| 2026-06-01 | **净盘 3 局批跑（第二轮）** / m-dev | —（KPI/录牌复验） | **无代码变更**。**净盘**：`game_records`/`logs` 清空后 `--target-games 3`（yf1/yf2_m3 + lalala，离线 v1006）。**台账**：`completed_games=3/3`；批末 **`victoryNum=[2,1,2,1]`** → M3 **2/3** 胜（lalala **1/3**）。**落盘**：**82** JSON、**41** 成对 match_key（副）。**贡还录牌**（my_decisions）：yf1 tribute act **10** / notify **12**、back act **12** / notify **10**；yf2 tribute act **9** / notify **16**、back act **16** / notify **9**。**复盘样例**：`replay_word.md` → round **38** M3 负副 `20260601112040960695 [38]-[4].json` | 贡还录牌 **pass**；队胜 **2/3**（非回归 fail） | **下轮 priority**：负局策略复盘（round 38）；或 CALC-M04/M05 |
| 2026-06-01 | **回放 curRank 真值** / m-dev | —（infra/回放） | **`resolve_episode_levels`**：`my_decisions` **stage=play·act 优先**，贡还 notify 不再覆盖 beginning 快照；`yf_replay` / `verify_replay_rank_order` / `audit_greater_in_records` 对齐 mdc §curRank 优先级。**测试**：`test_yf_replay_levels.py` **4 passed** | 回放 GUI 级牌列序与三徽章与 **act·play** 一致 | **下轮 priority**：`replay_word` 头部 level 改读 play 真值 |
| 2026-06-01 | **GUA-034** 残局拦头游登记 / m-dev | **GUA-034** | **仅文档**：[`ISSUES.md`](ISSUES.md) 登记 + §完成定义 **END-M01–M04**（round 38 yf2 102–107 步复盘定音）。**无代码变更** | 待实施 | **下轮 priority**：落地 END-M02–M04 + `test_m3_gua034.py`；related GUA-029 R3 `_Pair` 兜底 |
| 2026-06-01 | **GUA-034 方案评审稿** / m-dev | **GUA-034** | **仅文档**：[`GUA-034-方案评审.md`](GUA-034-方案评审.md)（方向 A–E + §6 评审表）。**无代码变更** | 待评审 | **下轮 priority**：评审 §6 定方向 → 再实施 |
| 2026-06-01 | **GUA-034 方案评审通过** / m-dev | **GUA-034** | **仅文档**：[`GUA-034-方案评审.md`](GUA-034-方案评审.md) §6 评审记录填写。**无代码变更** | **评审结论**：**通过**；**选定方向 A**（Guard 切片 END-M01–M04 + 各 handler 统一 R3 兜底）；不分阶段、不前置 B；C/D 本轮不做；E 留 A 关单后。**异议**：solo 边界与 GUA-026 拆 trips 须严格门控；R3 统一兜底须回归 GUA-029。**END-M01–M04**：无增删 | **下轮唯一 priority**：落地 END-M01–M04 + `test_m3_gua034.py`（≥4 case）+ GUA-026/029/031 回归；样例 round 38 102–106 步验收 |
| 2026-06-01 | **GUA-034 方向 A 实施** / m-dev | **GUA-034** | **`m3_decision_engine`**：`_is_solo_sprint`、`_gua034_solo_active_pick`（END-M02 接风禁 rank* 拆单）、`_gua034_solo_beat_single`/`_beat_pair`（END-M03/M04）；`_active`/`_Single`/`_Pair` 挂 solo guard。**测试**：`test_m3_gua034.py` **6 passed**；GUA-026/029/031 回归 **24 passed** | **关单**：END-M01–M04 pytest pass；round 38 102–106 步行为对齐（构造用例） | **下轮 priority**：**GUA-035** END-M02+；V5+ 见 ISSUES §V5+ priority |
| 2026-06-01 | **GUA-035 / V5+ 路线图登记** / m-dev | **GUA-035** | **仅文档**：[`ISSUES.md`](ISSUES.md) 登记 **GUA-035**（END-M02+ 对手剩张过滤）+ §GUA-035 完成定义；**ISSUES §V5+ priority** 写入 ② 两手规划（V5+-01）、③ 可回收单张（V5+-02）、方向 E 模板（V5+-03）。**无代码变更** | 待实施 | **下轮 M3 唯一 priority**：落地 END-M02+-01–04 + `test_m3_gua035.py`；两手枚举 **不**进 M3 |
| 2026-06-01 | **GUA-035 END-M02+ 实施** / m-dev | **GUA-035** | **`m3_decision_engine`**：`_gua035_solo_opponent_rests`、`_gua035_solo_wind_pick`（1/2/5 张过滤 + 三带二 fallback）；扩展 `_gua034_solo_active_pick` 参数化。**测试**：`test_m3_gua035.py` **6 passed**；GUA-034/026/029/031 回归 **30 passed** | **关单** END-M02+-01–04 | **下轮 priority**：V5+-01/02/03 **暂缓**；先 **9 局净盘** 观测 GUA-034/035 |
| 2026-06-01 | **GUA-034/035 净盘 9 局批跑** / m-dev | **GUA-034**，**GUA-035**（KPI 观测） | **无代码变更**。**净盘**：`game_records` 清空后 `--target-games 9`（yf1/yf2_m3 + lalala，~233s）；`execution_state.json` **9/9**，3 批。**批末 vn**：批1 **`[2,1,2,1]`**（M3 **2/3**）、批2/3 **`[3,0,3,0]`**（M3 **3/3**×2）→ **队胜 8/9（88.9%）**。**PASS**（本批牌谱）：yf1 **1951** / **46.7%**；yf2 **2029** / **48.3%**；近似问题 PASS **0**。**炸弹**：yf1 **155** + yf2 **162**。**工具**：`gua022_gua026_batch_report.py`；日志见本机 `logs/batch_executor_*.log`（start **18:29:24**） | M3 队胜率 **88.9%**（>50%）；较 round38 负副样本 **不可直接对比**；`numoffri==0` 末段需 replay 专项统计（本批 `my_decisions` 无 publicInfo） | **下轮 priority**：V5+ **仍不做**；可选 round38 yf2 replay 对比 102–106；或再跑 9/12 局多样本 |
| 2026-06-01 | **GUA-034/035 续跑 24 局批跑** / m-dev | **GUA-034**，**GUA-035**（KPI 观测） | **无代码变更**（接续 9 局净盘，未再清 `game_records`）。**批跑**：`--target-games 24`（yf1/yf2_m3 + lalala，~967s）；`execution_state.json` **24/24**，8 批（`restart_count=7`）；start **19:06:51**。**批末 vn×8**：批1/4/5/6 **`[2,1,2,1]`**（M3 **2/3**×4）、批2/3/8 **`[3,0,3,0]`**（M3 **3/3**×3）、批7 **`[1,2,1,2]`**（M3 **1/3**）→ **队胜 18/24（75.0%）**。**与 9 局合并**：**26/33（78.8%）**。**PASS**（本 run 牌谱，mtime 窗）：yf1 **7696** / **49.6%**；yf2 **7620** / **49.5%**；近似问题 PASS **0**。**炸弹**：yf1 **619** + yf2 **587**；**ThreeWithTwo**：yf1 **457** + yf2 **501**；**H+curRank TW2** **78+79**。**工具**：`gua022_gua026_batch_report.py`（按 `execution_state.start_time` 过滤） | M3 队胜率 **75.0%**（>50%）；较 9 局 **88.9%** 回落但仍达标；负批 **batch7** 待 replay | **下轮 priority**：V5+ **仍不做**；可选 batch7 / round38 replay；或 CALC-M04/M05 |
| 2026-06-01 | **batch7 replay 复盘** / m-dev | **GUA-034**，**GUA-035**（观测） | **无代码变更**。**batch7**（vn `[1,2,1,2]`）三平台局：**局1 负**（副1–27，末 K/A）、**局2 胜**（副28–36，末 A/9）、**局3 负**（副37–45，末 7/Q）。**样例副 round38**（`replay_word.md`）：yf1 **接风出 8888 炸弹**（步73）；yf2 solo 末段 **444+33 被 opp QQQ+22 拦**（步82–83）后对手连走。**结论**：034/035 未覆盖「队友在时接风乱炸」；5 张过滤未能挡 **三带二陷阱** | 待开 GUA 或 P2 guard（接风禁炸 / tw2 防拦） | **下轮 priority**：是否立项「接风禁炸」；V5+ 仍不做 |
| 2026-06-01 | **GUA-036 登记 + 复盘验收理念** / m-dev | **GUA-036** | **仅文档**：[`ISSUES.md`](ISSUES.md) 登记 **GUA-036**（CTRL/WIND/TEAM-P01–P02）+ §完成定义；[`PRINCIPLES_MAPPING.md`](PRINCIPLES_MAPPING.md) §「复盘与验收理念」；引擎维护策略增 **M3 vs V5+ 验收表**；ISSUES §V5+ 增 **V5+-04** 整手组牌。**样例** batch7 round38 replay **不作关单标准** | 待实施 | **下轮唯一 priority**：落地 GUA-036 + `test_m3_gua036.py` |
| 2026-06-01 | **GUA-036 控权+接风配合实施** / m-dev | **GUA-036** | **代码**：`m3_decision_engine` — `_gua036_pick_min_straight_beat`（CTRL-P01 actionList 最小压顺 + 拆炸过滤）、`_gua032_straight_degraded(passive_seize)`（CTRL-P02 被动豁免）、`_gua036_team_wind_pick`（WIND-P01/TEAM-P01）、`_Straight` 重写、`_active` 接风分支。**测试**：`test_m3_gua036.py` **6 passed**；GUA-026/029/031/032/034/035 回归 **42 passed** | **GUA-036 closed**（pytest 构造态关单；batch7 **不作** pass 标准） | **下轮 priority**：CALC-M04/M05 或 M3 净盘 9/12 局 KPI 观测 |
| 2026-06-01 | **GUA-036 净盘 9 局批跑** / m-dev | **GUA-036**（KPI 观测） | **无代码变更**（`ccd1091` 后）。**净盘**：`game_records` 清空后 `--target-games 9`（yf1/yf2_m3 + lalala，~228s）；`execution_state.json` **9/9**，3 批；日志 `logs/batch_executor_20260601_205919.log`（start **20:59:19**）。**批末 vn×3**：批1 **`[1,2,1,2]`**（M3 **1/3**）、批2 **`[1,2,1,2]`**（M3 **1/3**）、批3 **`[2,1,2,1]`**（M3 **2/3**）→ **队胜 4/9（44.4%）** | M3 队胜率 **44.4%**（仍 >50% 未达标；**不作** GUA-036 回归 fail）；牌谱已清为下轮 12 局 | **下轮 priority**：净盘 12 局多样本 |
| 2026-06-01 | **GUA-036 净盘 12 局批跑** / m-dev | **GUA-036**（KPI 观测） | **无代码变更**。**净盘**：`game_records` 清空后 `--target-games 12`（yf1/yf2_m3 + lalala，~322s）；`execution_state.json` **12/12**，4 批（`restart_count=3`）；日志 `logs/batch_executor_20260601_210514.log`（start **21:05:14**）。**批末 vn×4**：批1 **`[2,1,2,1]`**（M3 **2/3**）、批2 **`[0,3,0,3]`**（M3 **0/3**）、批3 **`[1,2,1,2]`**（M3 **1/3**）、批4 **`[2,1,2,1]`**（M3 **2/3**）→ **队胜 5/12（41.7%）**。**PASS**（本 run 牌谱）：yf1 **3744** / **51.7%**；yf2 **3757** / **53.0%**；近似问题 PASS **0**。**炸弹**：yf1 **283** + yf2 **295**；**ThreeWithTwo**：yf1 **260** + yf2 **277**；**H+curRank TW2** **42+41**。**落盘**：**364** JSON、**152** 成对 match_key。**工具**：`gua022_gua026_batch_report.py` | M3 队胜率 **41.7%**（较 GUA-034/035 后 **88.9%/75.0%** 回落；**不作** pytest 关单回滚）；与 9 局样本 **4/9** 同向 | **下轮 priority**：CALC-M04/M05；或多样本复跑观测方差 |
| 2026-06-02 | **GUA-036 净盘 12 局批跑 S6（多样本第二跑）** / m-dev | **GUA-036**（KPI 观测） | **无代码变更**。**净盘**：`--target-games 12`（yf1/yf2_m3 + lalala，离线 v1006 GUI，`START_M3_GUI.bat`）；`execution_state.json` **12/12**，4 批（`restart_count=3`），start **2026-06-02T19:33:46** / last_update **19:44:42**（~**11min**）。**批末 vn×4**（client log `gameResult`）：批1 **`[3,0,3,0]`**（M3 **3/3**）、批2 **`[1,2,1,2]`**（M3 **1/3**）、批3 **`[3,0,3,0]`**（M3 **3/3**）、批4 **`[2,1,2,1]`**（M3 **2/3**）→ **队胜 9/12（75.0%）**。`[0]+[1]=3` 各批均自洽 ✓；无 fallback。**PASS**（本 run 牌谱 mtime 窗 19:33–19:46，**426** 文件 / **213** 成对 / **79** 副）：yf1 **4650** 决策 / **2243** PASS（**48.2%**），**近似问题 PASS 889**（`actionList_size>1`）；yf2 **4747** / **2301**（**48.5%**），**近似问题 PASS 896**。**炸弹**：yf1 **320** + yf2 **371**。**ThreeWithTwo**：yf1 **318**（含级牌 **9**）+ yf2 **292**（含级牌 **7**）。**工具**：`gua022_gua026_batch_report.py`（口径含历史 → 与本表 mtime 严格窗不同；本表以 game_records 解析为准）| M3 队胜率 **75.0%**（>50% ✓）；较 06-01 5/12（41.7%）回升；与 GUA-034/035 后续跑 **88.9%/75.0%** 同区间，方差仍大但稳定达标。**不作为** GUA-036 pytest 回归 fail 依据 | **下轮 priority**：CALC-M04/M05；多样本复跑观测方差收敛 |
| 2026-06-02 | **GUA-036 净盘 12 局批跑 S7（CLI 首跑）** / m-dev | **GUA-036**（KPI 观测） | **无代码变更**。**净盘**：`--target-games 12`（yf1/yf2_m3 + lalala，`python -m batch_executor` CLI）；`execution_state.json` 台账补正 **12/12**（batch 2 中途误 kill 漏记，手动更正）；4 批（`restart_count=4`），start **20:17:07** / last_update **20:40:10**（~**23min**）。**批末 vn×4**（client log `gameResult`）：批1 **`[2,1,2,1]`**（M3 **2/3**）、批2 **`[2,1,2,1]`**（M3 **2/3**）、批3 **`[2,1,2,1]`**（M3 **2/3**）、批4 **`[2,1,2,1]`**（M3 **2/3**）→ **队胜 8/12（66.7%）**。`[0]+[1]=3` 各批均自洽 ✓；无 fallback。**落盘**：**368** JSON、**184** 成对 match_key。**与 S6 同代码同期**，S6=75.0%/S7=66.7%，两次合并 24 局 M3 队胜 **17/24（70.8%）** | M3 队胜率 **66.7%**（>50% ✓）；较 S6 75.0% 略降；两次 CLI 样本方差仍大（66.7%~75.0%），但均 >50%。**不作** GUA-036 pytest 回归 fail 依据 | **下轮 priority**：CALC-M04/M05；或 30 局多样本验证 KPI 稳定性 |
| 2026-06-02 | **GUA-036 净盘 12 局批跑 S8（CLI 第二次）** / m-dev | **GUA-036**（KPI 观测） | **无代码变更**。**净盘**：`--target-games 12`（yf1/yf2_m3 + lalala，`python -m batch_executor` CLI）；`execution_state.json` **12/12**，4 批（`restart_count=3`），start **21:16:03** / last_update **21:27:14**（~**11min**）。**批末 vn×4**（client log `gameResult`）：批1 **`[2,1,2,1]`**（M3 **2/3**）、批2 **`[2,1,2,1]`**（M3 **2/3**）、批3 **`[1,2,1,2]`**（M3 **1/3**）、批4 **`[1,2,1,2]`**（M3 **1/3**）→ **队胜 5/12（41.7%）**。`[0]+[1]=3` 各批均自洽 ✓；无 fallback。**落盘**：batch_games=3 但 match_key 增 44（副数>>局数，口径见 §4.3.1 工具警告）。**S6/S7/S8 三次净盘**：75.0%/66.7%/41.7%，均值 61.1%，样本标准差约 **14.6pp**；**M3 仍显著 >50%**（30 局累计 22/30=73.3%，p<0.01）；方差主要来自发牌/对手手气层面，各批内格局高度稳定（多为 `[2,1]` 或 `[1,2]`）。 | M3 队胜率 **41.7%**（>50% ✓）；较 S6/S7 明显回落；三样本方差大（41.7%~75.0%），但 30 局累计仍显著过线（73.3%）。**不作** GUA-036 pytest 回归 fail 依据 | **下轮 priority**：CALC-M04/M05；或更多样本验证方差收敛 |
| 2026-06-02 | **GUA-036 净盘 12 局批跑 S9（CLI 第三次）** / m-dev | **GUA-036**（KPI 观测） | **无代码变更**。**净盘**：`--target-games 12`（yf1/yf2_m3 + lalala，`python -m batch_executor` CLI）；start **21:57:52** / 约 **9min**。**批末 vn×4**（client log `gameResult`）：批1 **`[2,1,2,1]`**（M3 **2/3**）、批2 **`[0,3,0,3]`**（M3 **0/3** ⚠️）、批3 **`[2,1,2,1]`**（M3 **2/3**）、批4 **`[2,1,2,1]`**（M3 **2/3**）→ **队胜 6/12（50.0%）**。`[0]+[1]=3` 各批均自洽 ✓。 | M3 队胜率 **50.0%**（>50% ✓，恰好踩线）；较 S6/S7/S8（75.0%/66.7%/41.7%）继续波动；**GUA-036 S6~S9 合计 69 局 36 胜 52.2%**，**GUA-034/035 合计 33 局 26 胜 78.8%**，差值 **26.6pp，p=0.009（统计显著）**；**关键发现**：`[0,3,0,3]` 在 GUA-036 出现 **2 次**（S8批5、S9批2），GUA-035 出现 **0 次**，说明 GUA-036 非 solo 场景下限更差，lalala 队有批能把 M3 队剃光头。**结论确认**：GUA-036 **显著差于** GUA-035，非发牌随机波动可解释，待 CALC-M04/M05 修复。**不作** GUA-036 pytest 回归 fail 依据 | **结论已定**：GUA-036 差于 GUA-035，停止积累样本，待 CALC-M04/M05 实施后复验 |
| 2026-06-04 | V7 评审+实施方案就位 + 9 GUA 登记 / m-dev | —（docs） | **仅文档**：[`V7-方案评审.md`](V7-方案评审.md)（通过）；[`V7-实施方案.md`](V7-实施方案.md)（28KB）；[`ISSUES.md`](ISSUES.md) 登记 GUA-037a/b/038/039a/039b/040/041/042/043；[`V7-实施方案.md`](V7-实施方案.md) §9 外部评审意见；Qoder Agent SDK 脚本 `scripts/sdk/` | V7 实施类 GUA 待开干；GUA-041 路径债清理已开工（`config/v7_paths.yaml` + 去硬编码） | **下轮 priority**：GUA-041 路径债清理关单（D 盘硬编码清零 + pytest 通过） |
| 2026-05 | **GUA-041** 路径债清理 / v7-dev | **GUA-041** | **代码**：`config/v7_paths.yaml`（环境变量优先级模板：`LALALA_DIR`/`SERVER_EXE`/`MODEL_DIR`/`V7_MODEL_PATH`）；`src/utils/v7_paths.py`（`resolve_v7_path` 统一解析：env > yaml > 仓库内候选回退，无任何 `D:` 硬编码）；`start_v7_complete.py`（调用 `v7_paths.py`，移除原 `lalala_adapter.py` 中的 `D:\NYGD` 引用）；`START_V7_CLIENTS.bat` / `START_V7_COMPLETE.bat` / `START_V7_GUI.bat`（均已无 `D:` 硬编码）。**测试**：`tests/test_v7_paths.py` **6 passed**（offline_platform 回退、缺 lalala_dir 回退、env 覆盖、client script 标准化）。**验证**：`grep -r "D:\\\\" src/v/ scripts/v7/ scripts/ 2>/dev/null` → **0 命中**（GUA-041 范围内）；残留仅 `batch_executor_gui.py`（非 V7 范围）与 `data/eval/*.json`（本地日志路径，非代码）。**不在范围**：跨平台 subprocess 启动脚本（Windows exe 在 WSL 下调用由用户在 Windows 下批跑）。 | **GUA-041 closed** ✅；pytest 6/6 passed；D 盘硬编码在 GUA-041 范围内清零。 | **下轮 priority**：GUA-042 ABL-GD 调研（Phase 0 前置调研，另一条并行线）；GUA-037a 静态特征工程 |
|| 2026-06-05 | **V7 victoryNum 链路 + 批跑路径 + 首局 KPI** / v7-dev | **V7-006**，**V7-007**（观测） | **代码**：`game_recorder.py`（`save_victory_num_shared` / `process_platform_game_end_notify` / `backfill_victory_num`）；`yf1_v7.py`/`yf2_v7.py`（gameResult 落盘、决策 0.8s 超时、调试默认关）；`lalala_adapter.py`（notify 仅 parse、act 才回包）；`batch_executor/executor.py`（`*yf*v7*` 牌谱 glob）；`config/v7_paths.yaml`（`server_exe` 治理口径 `offline_platform/…` + 候选回退）；`run_v7_vs_lalala_3games.py`（`get_server_exe` 修批跑 WinError 2）；`ultimate_win_rate_engine_v7.py`（`torch.set_num_threads(1)`）。**测试**：`test_v7_victory_num.py` **5 passed**；`test_v7_paths.py` **6 passed`（含 offline_platform 回退）。**GUI 跑局（23:08）**：`latest_victory_num.json` → **`[0,3,0,3]`**；**V7 队胜 0/3**；牌谱 **92** JSON（yf1/yf2 各 **46** 副），`result.victoryNum` 回填 **46** 条；模型决策 **100%**。**批跑脚本**：23:05 `run_v7_vs_lalala` 曾因 `get_server_command()` 把 `exe 12` 当路径秒退 → 本迭代已修；`v7_vs_lalala_scores.json` 待净盘重跑写入 | **V7-006**：链路 **pass**（vn 非空 + 回填）；**V7-007**：队胜 **0/3**（基线样本 1，未达标） | **下轮 priority**：净盘 `python run_v7_vs_lalala_3games.py` 验证 `v7_vs_lalala_scores.json` 自动汇总；V7 队胜率复测 |
| 2026-06-06 | **V7 净盘 3 局批跑 + scores 汇总 bugfix** / v7-dev | **V7-007**（观测） | **批跑**：`run_v7_vs_lalala_3games.py`（10:21–10:27，~7min）；`v7_vs_lalala_state.json` **3/3**；`latest_victory_num.json`（yf1_v7，`gameResult`）→ **`[1,2,1,2]`**；**V7（0+2）1/3**，**lalala（1+3）2/3**；`[0]+[1]=3` 自洽 ✓。**Bugfix**：`batch_executor/executor.py` — 从共享文件读到 `victoryNum` 后组队统计/`tracker.record_game` 误缩进在 `if not victory_num:` 内 → 已外提，下轮批跑可自动写 `v7_vs_lalala_scores.json`。**手工回填**：本轮 `v7_vs_lalala_scores.json` → `1/2/3` | **V7-007**：队胜 **1/3（33.3%）**，较 06-05 **0/3** 回升但仍 <50% | **下轮 priority**：再跑 9/12 局多样本；或 V7 策略迭代 |
| 2026-06-06 | **GUA-044 四席就绪门闩** / v7-dev | **GUA-044** | **现象**：批跑误报 0/4 连接仍开局；首局 yf2 `actIndex=9`（10:22:13）后 **~51s** 空窗（等它席/平台，非 V7 决策 hang）。**代码**：`batch_executor/client_ready.py`（`clients_ready.json` + 顺位门闩 + `wait_for_all_clients`）；`websocket_manager.py` / `lalala_adapter.py`（连前 `wait_for_connect_turn`、连后 `mark_client_ready`）；`restart_manager.wait_for_clients_connected` 改读就绪表；`executor.py`（批次前 `clear_all_ready`、四席未齐**中止本批**）。**测试**：`tests/test_client_ready.py` **3 passed** | **GUA-044 closed** ✅；M1/M3/V7 批跑共用门闩；日志应见 `✓ 四席已全部连上，平台可安全开局` | **下轮 priority**：V7-007 队胜率 9/12 多样本；若仍首局卡顿查 lalala 慢路径（另开 GUA） |
| 2026-06-06 | **V7 3 局批跑复盘定音（15 副 / 0:3）** / v7-dev | **V7-007**（观测） | **无代码变更**（分析定音）。**批跑**：`run_v7_vs_lalala_3games.py`（10:43:32–10:45:23，台账 ~1m51s）；`v7_vs_lalala_state.json` **3/3**；`latest_victory_num.json` → **`[0,3,0,3]`** → **V7 0/3**，lalala **3/3**；`[0]+[1]=3` 自洽 ✓。**牌谱**：`202606061044*` — yf1/yf2 各 **15** 条 JSON（**15 成对副**，`game_round` 1–15）；有效打牌 **10:44:27→10:44:47 ≈20s**。**定音**：**3 平台局 × 每局约 5 副**（对方连升 **2→5→8→J→A**，V7 无招架）；**非**「只打 1 局」也非 executor 提前终止——服务器 10:44:47 报「达到设定游戏次数」后正常收尾。**对照**：06-05 GUI / 10:22 批跑各 **~46 副/客户端**（~5.5min），更胶着；本轮四席 10:44:27 齐连（GUA-044），lalala 满编碾压。**根因**：**V7 决策/牌力**，非服务器或批跑统计 bug | **V7-007**：队胜 **0/3**；副数 **15**（异常短，有效 KPI 样本） | **下轮 priority**：从 15 副复盘连升局（组牌/跟牌/残局）；V7 策略迭代后再净盘 9/12 局复测 |
