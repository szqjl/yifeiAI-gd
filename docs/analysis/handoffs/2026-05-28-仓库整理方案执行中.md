# Handoff: 仓库整理 / M-V 治理方案执行

| 字段 | 内容 |
|------|------|
| 日期 | 2026-05-28 |
| 分支 | **m-dev** @ `887531d`（Gitee **默认分支** 已设为 m-dev） |
| 状态 | **进行中**（Phase 1–3 大部分完成） |

## 背景

按 [M-V-Series-治理方案](../../governance/M-V-Series-治理方案.md) 做仓库治理；主开发在 **m-dev**，已 push Gitee。

## 已完成

- [x] 治理文档、MATRIX、COS 脚本、handoff 流程、manifest 模板
- [x] `git checkout m-dev && git pull`（已同步至 origin/m-dev）
- [x] 治理改动 **commit + push** `m-dev`（54bcb48）
- [x] `main` 打 tag：`archive/main-pre-governance-20260528`
- [x] `git fetch origin --prune` / `github --prune`
- [x] `.gitignore`：`training_logs/`、`trajectories/`、`config/cos.env`、`data/artifacts/`
- [x] [repo-cleanup-inventory.md](../../repo-cleanup-inventory.md) 根目录脚本清单
- [x] 分支 `m1-dev` → `m-dev`；Gitee 默认分支已改为 **m-dev**

## 未完成

- [ ] **config/cos.env**：复制 example 并填密钥；`pip install cos-python-sdk-v5`
- [ ] 试上传一局 replay，更新 manifest 的 sha256
- [ ] ~~GitHub 远程 develop 删除~~ → **暂不处理**（2026-05-28 决定；仅用 Gitee `origin`）
- [ ] **main vs origin/main** 策略拍板（选项 A/B，治理方案 §2.4）
- [ ] **Phase 4**：根目录脚本迁入 `scripts/`（按 inventory 分批 PR）

## 下一步唯一动作

配置 COS 并试跑上传：

```bash
cp config/cos.env.example config/cos.env
# 编辑 cos.env
pip install cos-python-sdk-v5
python scripts/cos/upload_regression.py <一局game_records路径> --id reg-001
```

## 不要重做

- Phase 1–3 文档与 m-dev 推送已完成
- 日常开发请在 **m-dev**，勿在 **main** 上提交
