# Handoff: 仓库整理 / M-V 治理方案执行

| 字段 | 内容 |
|------|------|
| 日期 | 2026-05-28 |
| 分支 | 当前在 **main**（建议回家后在 **m1-dev** 上继续） |
| 状态 | **进行中** |
| 对话 | Cursor 整理方案会话（无自动记忆，靠本文件接续） |

## 背景

按 [M-V-Series-治理方案](../../governance/M-V-Series-治理方案.md) 做仓库治理；主开发应在 `m1-dev`，M 对 lalala，V 暂未挂接。Artifact 统一 **腾讯云 COS**（见 [COS-接入指南](../../governance/COS-接入指南.md)）。

## 已完成

- [x] 文档：`docs/governance/M-V-Series-治理方案.md`、`docs/versions/MATRIX.md`
- [x] COS：`config/cos.env.example`、`scripts/cos/*.py`、`docs/governance/COS-接入指南.md`
- [x] Handoff 流程：`docs/governance/分析接续-handoff.md`
- [x] `data/manifests/regression-lalala-v1.json` 模板
- [x] Git：删除本地 `develop`；`credential.helper=manager`；`git fetch origin` 成功
- [x] README 增加治理 / COS 链接
- [x] `.gitignore`：`config/cos.env`、`data/artifacts/`

## 未完成（回家后在另一台电脑做）

- [ ] `git checkout m1-dev && git pull origin m1-dev`（本地 m1-dev 曾落后 origin 约 12 commit）
- [ ] 复制 `config/cos.env.example` → `config/cos.env`，填腾讯云密钥与桶名
- [ ] `pip install cos-python-sdk-v5`，试跑一局 `upload_regression.py` + 更新 manifest
- [ ] 治理方案 **Phase 1**：本地 `main` 打归档 tag；确认不在 `main` 日常提交
- [ ] 治理方案 **Phase 2/3**：根目录脚本清单、`.gitignore` 收紧（见治理方案 §9）
- [ ] GitHub 远程 `develop` 删除（若尚未做）→ `git fetch github --prune`
- [ ] 选定 `main` 与 `origin/main` 处理策略（治理方案 §2.4 选项 A 或 B，团队拍板）

## 关键结论（已采纳，勿推翻）

- 默认开发分支：**m1-dev**；COS 为**唯一** artifact 网盘
- 回归集：**30 局**；V-default-smoke：**OFF**
- 改 M 的 PR：**不**默认跑 v5 冒烟
- 换机接续：本目录 handoff + Git push；大文件 COS

## 数据与产物位置

| 类型 | 路径 |
|------|------|
| 治理总纲 | `docs/governance/M-V-Series-治理方案.md` |
| 状态矩阵 | `docs/versions/MATRIX.md` |
| COS 操作 | `docs/governance/COS-接入指南.md` |
| 本 handoff | `docs/analysis/handoffs/2026-05-28-仓库整理方案执行中.md` |
| 本地未提交 | 运行 `git status` 查看（回家前先 commit+push 文档与脚本） |

## 下一步唯一动作

```bash
cd D:/YiFeiAI-GD
git status
git add docs/ data/manifests/ scripts/cos/ config/cos.env.example requirements.txt .gitignore README.md
git commit -m "docs: M/V 治理方案、COS 脚本与 handoff 流程"
git push origin HEAD   # 若在 main；或先 checkout m1-dev 再 push m1-dev
git checkout m1-dev && git pull origin m1-dev
```

然后配置 `config/cos.env` 并试上传一局 replay。

## 不要重做

- 不必重写治理文档（除非发现错误）
- 不必再讨论「COS vs 迅雷双轨」（已定为 COS 唯一）
- 新 Agent 勿从零「分析仓库乱不乱」——直接按 governance §9 Phase 执行
