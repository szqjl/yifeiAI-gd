# Handoff: 仓库整理 / M-V 治理方案执行

| 字段 | 内容 |
|------|------|
| 日期 | 2026-05-28 |
| 分支 | **m-dev** @ Phase 4 第六批完成 |
| 状态 | **进行中**（Phase 4 第六批完成；`yf_replay.py` 暂留根目录） |

## 背景

按 [M-V-Series-治理方案](../../governance/M-V-Series-治理方案.md) 治理仓库。主开发 **M 系列 × lalala**，V 未挂接。Artifact **仅腾讯云 COS**（见 [COS-接入指南](../../governance/COS-接入指南.md)）。

## 已完成

### Git / 分支

- [x] 治理文档、`docs/versions/MATRIX.md`、handoff 流程
- [x] 默认开发分支 **`m-dev`**（由 `m1-dev` 重命名）；`origin/m1-dev` 已删除
- [x] Gitee 仓库默认分支已设为 **m-dev**
- [x] `main` 归档 tag：`archive/main-pre-governance-20260528`（勿在 `main` 日常提交）
- [x] `git fetch origin --prune`；**GitHub 远程整理暂不处理**
- [x] `.gitignore`：`training_logs/`、`trajectories/`、`config/cos.env`、`data/artifacts/`
- [x] [repo-cleanup-inventory.md](../../repo-cleanup-inventory.md)（根目录约 64 个 `.py` 待 Phase 4 迁移）

### Phase 4 第一批（check / diagnose → scripts/checks/）

- [x] 迁移 15 个脚本：`check_websocket_config`、`check_workflow_*`、`check_cuda` 等 + 3 个 `diagnose_*`
- [x] 更新 `CHECK_RECORD_CONSISTENCY.bat`、`START_M1_WORKFLOW_FULL.bat`、README 与训练文档中的路径
- [x] `check_websocket_config.py` 改用 `REPO_ROOT` 定位 `config.yaml`

### Phase 4 第二批（剩余 check → scripts/checks/）

- [x] 迁移 8 个脚本：`check_model_loading`、`check_model_optimizations`、`check_training_progress` 等
- [x] `check_model_loading.py` 改用 `REPO_ROOT` 加入 `sys.path`

### Phase 4 第三批（verify / analyze / train → scripts/）

- [x] `scripts/verify/`：6 个 `verify_*.py`（P0 验证等）
- [x] `scripts/analysis/`：6 个 `analyze_*.py`
- [x] `scripts/training/`：3 个 `train_stage*.py`（stage5/6 超优化与游戏导向）
- [x] verify/train 脚本改用 `REPO_ROOT` 定位仓库根目录

### Phase 4 第四批（train 剩余 + 工具 → scripts/）

- [x] `scripts/training/` +4：`train_stage7_online_rl`、`train_stage8_full_rl`、`train_strategy_tasks`、`monitor_training`
- [x] `scripts/tools/` +6：`batch_update_json_from_rep`、`batch_update_szqjl_only`、`clean_git_history`、`clean_large_files`、`convert_rep_to_xml`、`download_models`
- [x] 更新 `START_STRATEGY_TASKS_TRAINING.bat`、用法文档与 `check_training_progress` 提示

### Phase 4 第五批（workflow / V7 / test → scripts/ & tests/）

- [x] `scripts/workflow/`（3）：`auto_restart_workflow`、`workflow_current_status`、`monitor_workflow_progress`
- [x] `scripts/v7/`（2）：`start_v7_complete`、`start_v7_gui`
- [x] `tests/`（10）：P0/M1/V7 相关测试脚本（纳入 Git 跟踪）
- [x] `.gitignore`：`/test_*.py` 仅忽略根目录，允许 `tests/` 入库
- [x] 更新 `START_AUTO_RESTART_WORKFLOW.bat`、`START_V7_*.bat`、`运行M1测试.cmd` 等

### Phase 4 第六批（杂项 → scripts/ & tests/，`yf_replay.py` 除外）

- [x] `tests/` +5：剩余根目录 `test_*.py`
- [x] `scripts/training/` +4：`view_training_*`、`run_stage6_training_gui`、`stage6_training_gui`
- [x] `scripts/tools/` +5：`delete_non_replay_records`、`feishu_*`、`replay`、`fix_model_compatibility`
- [x] `scripts/verify/` +1：`p0_verification_auto`（改用 `REPO_ROOT`）
- [ ] **`yf_replay.py` 暂留根目录**（其他 Agent 编辑中，第七批再迁）

### COS（本机已通）

- [x] `config/cos.env`（勿提交）：`ap-shanghai`，桶 `hermes-1377564594`，前缀 **`yifei-gd/`**
- [x] 官方 SDK `cos-python-sdk-v5`；`python scripts/cos/verify_cos.py` 通过
- [x] **30 局** regression → `yifei-gd/replays/regression-lalala-v1/`
- [x] `data/manifests/regression-lalala-v1.json`（reg-001～030）；`pull_regression` **30/30** sha256 通过
- [x] 脚本：`cos_client.py`、`verify_cos.py`、`upload_regression.py`、`batch_upload_regression.py`、`sync_pull_all.py`、`upload_szqjl_batch.py`（后者待用）
- [ ] ~~szqjl 10 局~~：本机 `game_records` 无文件名含 szqjl 的 JSON，**已取消**

### 约定（勿推翻）

- V-default-smoke：**OFF**；改 M 的 PR **不**默认跑 v5 冒烟
- 回归集固定 **30 局**；换机：`git pull` + `python scripts/cos/pull_regression.py`

## 未完成

- [ ] **main vs origin/main** 策略（治理方案 §2.4 选项 A/B，团队拍板）
- [ ] **Phase 4 第七批**：`batch_executor.py`、`client_std_*`、`run_t9_direct` 等；`yf_replay.py` 待其他 Agent 完成后迁入 `scripts/tools/`
- [ ] 桶内 **3 个误传** yf2 对象（首次 batch glob 错误，可选在控制台删除）

## 数据位置

| 类型 | 路径 |
|------|------|
| 治理总纲 | `docs/governance/M-V-Series-治理方案.md` |
| COS 操作 | `docs/governance/COS-接入指南.md` |
| 接续说明 | `docs/governance/分析接续-handoff.md` |
| Regression manifest | `data/manifests/regression-lalala-v1.json` |
| COS 对象 | `cos://hermes-1377564594/yifei-gd/replays/regression-lalala-v1/` |
| 本地拉取镜像 | `data/artifacts/replays/regression-lalala-v1/`（gitignore） |
| 全量 game_records | **仅本机** `game_records/`（未整库上传 COS） |

## 下一步唯一动作

**Phase 4 第七批**：迁移 `batch_executor.py`、`client_std_*`、`run_t9_direct` 等；`yf_replay.py` 编辑完成后 → `scripts/tools/`。

## 换机 / 新 Agent 快速命令

```bash
git clone https://gitee.com/Philsz/yifei-ai-gd.git
cd yifei-ai-gd && git checkout m-dev && git pull
cp config/cos.env.example config/cos.env   # 填入密钥（每台机一次）
pip install cos-python-sdk-v5
python scripts/cos/verify_cos.py
python scripts/cos/pull_regression.py
```

对新 Agent：

```text
请读 docs/analysis/handoffs/2026-05-28-仓库整理方案执行中.md，
按「下一步唯一动作」继续，不要重做「已完成」项。
```

## 不要重做

- 不重写治理文档；不恢复 `m1-dev` 分支名
- 不讨论 COS vs 迅雷双轨；不默认 v5 冒烟
- 不必再上传 30 局 regression（已完成）
- GitHub 维护 **暂不处理**
