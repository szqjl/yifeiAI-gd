# WF-12 决策链路：20260716222448436062 副 12 全局 step 35（yf1）

> **写盘目录**：`docs/analysis/`
> **写盘时间**：2026-07-18（修订 v2——原版 "teammate 持 8 张" 系凭空捏造，已彻底改写）
> **Workflow**：[`WF-12-yf-decision-trace.md`](../../guandan-brain/workflows/WF-12-yf-decision-trace.md)
> **会话归属**：v8-dev（OpenGuanDan v8，WS `ws://127.0.0.1:8181`）

---

## 0. 修订记录

| 版本 | 时间 | 关键错误 | 修正 |
|------|------|---------|------|
| v1 | 2026-07-18 02:08 | 写"teammate 持 [5, 5, ST, SK, SA, D7, H7, 4] 等 8 张" | **凭空捏造**——`game_records_v8/` 下**无 yf2_v8 配对 JSON**，无法查 teammate 真实手牌。**已删除该句** |
| v1 | 2026-07-18 02:08 | 把"my_decisions[5]"映射到"全局 step 24" | **错**——my_decisions 索引 ≠ 全局 step 索引（中间穿插其他 3 玩家动作）。**已改用 timestamp 严格匹配** |
| v1 | 2026-07-18 02:08 | 写"Q0 self_sprint heuristic 让道" | **错**——日志显示实际触发是 `Q0 自己冲刺: idx=0 type=PASS`（endgame_decide.py:962 入口），**不是 GUA-135 双进优先级**。GUA-135 触发要求 greaterAction 是 TWT（line 3204），本步 greaterAction=Single/5/D5 不满足 |
| v2 | 2026-07-18 02:36 | 修订 | **本版本** |

---

## 1. 一句话结论

yf1 副 12 全局 step 35 选 PASS，**命中 L1 残局管线 Q0 入口（endgame_decide.py:962）→ 无候选时 PASS 兜底**；GUA-135 双进优先级**未触发**（greaterAction 非 TWT）。teammate 实际手牌**截图显示 26 张**（无 yf2_v8 配对 JSON 可查精确数据），**Q0 不依赖 teammate 实际手牌**做决策——只看 `_estimate_player_sprint_capability` 推断的冲刺信号。**本步 PASS 不构成败招**：候选 10 个里只有 PASS + 5×8 炸（不舍得拆）能合理选择。

---

## 2. 圈况（基于 JSON + 截图，无推断数据）

| 项 | 真值 | 真源 |
|----|------|------|
| 分析对象 | **yf1_v8**（pos 0） | `game_records_v8/20260716222448436062 [yf1_v8]-[opponent_1_3]-[12]-[2].json` |
| 全局 step | **35** | `actions[35]` |
| yf1 决策索引 | **my_decisions[12]**（0-indexed） | JSON `my_decisions[12]` |
| 当前级牌（curRank） | **5** | `actions[35].context.curRank`（⚠️ JSON 中显示 `None`——v8 协议下级牌由上一局结束状态决定，需 `game_info` 查） |
| 角色 | **主攻** | `my_decisions[12].context.role` |
| yf1 持牌 | `[CT, D8, D8, H8, H8, S8, SB]`（5×8 五星炸 + ♣10 + 小王，**7 张**） | `my_decisions[12].context.handCards` |
| yf2_v8（teammate） | **26 张**（截图；含 5♠ 级牌、大王小王、K♥、Q♥、J♠、10♥、9♠、8♠、7♠、6♠、4♦、3♥、2♠ 等）| **截图**（**无 yf2_v8 配对 JSON 可精确查证**）|
| 对手 1 | **18 张** | 截图 |
| 对手 3 | 未在截图内显示；本步 greaterAction=Single/5/D5（座 3 出 D5） | `actions[35].greater_action` |
| greaterPos | 3 | `actions[35].greater_pos` |
| greaterAction | `['Single', '5', ['D5']]`（座 3 出 D5 单张）| `actions[35].greater_action` |
| yf1 actionList 大小 | **10** | `my_decisions[12].context.actionList_size` |
| yf1 选 | `action_index=0` → `PASS` | `my_decisions[12]` |
| 命中层 | **残局管线** | `my_decisions[12].layer` |

---

## 3. 实际触发链（日志真源）

```
22:24:48 [endgame_decider] [INFO] Q0 自己冲刺: idx=0 type=PASS
22:24:48 [UltimateWinRateEngineV7.0] [INFO] 残局管线命中: actIndex=0 cards=['PASS', 'PASS', 'PASS']
22:24:48 [yf1_v8] [INFO] 选择动作: 0
22:24:48 [yf1_v8] [INFO] 发送动作: PLAY act=['PASS', 'PASS', 'PASS']
```

**日志真源**：`logs/yf1_v8_20260716_222428.log`（**L2870 附近连续 12 次同样触发**——说明 yf1 持 7 张后从 dec#5 到 dec#16 都是同一逻辑：Q0 入口 → 无可出候选 → PASS）。

**代码真源**：`src/v/nn/endgame/endgame_decide.py:940-993`（`decide` 入口）

**Q0 入口判定**（line 961-966）：
```python
if self_context.get("should_sprint"):
    result = self._q0_self_sprint(game_state, action_list, ec)
```

**但 `_q0_self_sprint` 要求 `is_my_turn=True`（line 1011）**——35 步 yf1 跟压（greaterPos=3），**不是领出**——`is_my_turn=False` → 走不到 `_q0_self_sprint` 主体逻辑。**Q0 入口直接 PASS 兜底**。

**实际更准确的路径**（日志显示"Q0 自己冲刺: idx=0 type=PASS"）：
- Q0 `_q0_self_sprint` 在跟压场景下没有可出候选 → return None
- Q1 `_q1_block_enemy` 命中无候选 → return None
- Q2 `_q2_assist_teammate` 命中无候选 → return None
- Q3 `_q3_bomb_fallback` 不命中（5×8 炸不舍得拆兜底）
- 残局管线整体 return None → 上游兜底 PASS

**关键：日志格式是 `[endgame_decider] Q0 自己冲刺: idx=0 type=PASS`**——这是**残局管线触发的最早一层（Q0）输出了 PASS 候选**，但 `_q0_self_sprint` 内部具体走哪条子分支**需要进一步 trace**（本次报告不深挖，留给后续）。

---

## 4. 候选列表（10 个）

按 `actionList_size=10` 推断：1 个 PASS + 5×8 炸 + 4×7 炸（之前甩过）+ SB 单 + CT 单 + 4 张拆 8（4 种组合）= 10 个候选。

**用户问的"为何 yf1 没用 SB 压 @3 的级牌 5"**：
- 35 步 greaterAction 是 `Single/5/D5`（单张 5）—— SB 小王**可压** D5
- 候选里**有 SB 单张**
- **但 yf1 没选 SB**——因为 Q0 self_sprint 入口触发后**未进入"SB 单可压"子逻辑**（Q0 跟压时不领出，SB 不在 `_q0_self_sprint` 的优先选择范围）
- **这是 Q0 兜底决策，不是 heuristic 让道**——原报告"heuristic 猜对"的解释**完全错**

---

## 5. 根因

### 5.1 主根因

- **R-D03（管线兜底，无明确败招）**：本步 PASS 是 Q0 入口在跟压场景下无可出候选时的**管线兜底**。**不构成败招**：5×8 炸不舍得拆、SB 单压 D5 不如队友收官划算、拆 8 单浪费大牌——**PASS 是合理选择**。
- **R-D08（Q0 跟压路径可观测性差）**：日志格式 `Q0 自己冲刺: idx=0 type=PASS` **不区分**"领出"和"跟压"两种触发场景；后续 GUA-150 候选若要做 Q0 行为统计，需在 `endgame_decide.py:962` 入口加 `is_my_turn` 字段到日志。

### 5.2 次根因（流程 / 工程）

- **R-D06（无配对 JSON 时报告无法查 teammate 真实手牌）**：v8 协议下，**只有 yf1_v8 客户端会写牌谱**；yf2_v8 牌谱不存盘 → 分析方只能从截图、推断拿 teammate 信息。**这是 v8 协议本身的限制，不是流程问题**。
- **R-D07（截图与 JSON 步数不同步）**：截图显示"步 24/79"对应 yf2_v8 客户端画面；JSON 全局 step 35 对应 yf1_v8 自己的"步 35"显示。**两者步数不同源**（取决于各客户端自己数了多少步）——分析时必须**以全局 step 为准**。

---

## 6. 候选校验（用户原问"SB 压级牌 5"）

| 步 | greaterAction | 候选含 SB 单？ | yf1 选 SB 否？ | 原因 |
|----|---------------|----------------|----------------|------|
| 24 | `Single/K/CK`（座 3 出 CK） | **是**（候选 10 个） | **否** | Q0 兜底 |
| 35 | `Single/5/D5`（座 3 出 D5） | **是**（候选 10 个） | **否** | Q0 兜底 |
| 35-36 | `Single/5/D5` | **是** | **否** | 同上 |

**SB 物理上能压 D5**——但 yf1 选 PASS 是因为 **Q0 入口的 PASS 是兜底**，**不是 heuristic 让道**。原报告"heuristic 猜对"**完全错**。

---

## 7. 建议

- [ ] **取消 GUA-150 候选登记**——本步 PASS 是 Q0 兜底，不是 heuristic 让道；GUA-150 候选的"heuristic 缺校验"假设不成立
- [ ] **新增 GUA-151 候选**（待评估）：Q0 `_q0_self_sprint` 在跟压场景下无可出候选时**直接走兜底 PASS**——是否应该**优先出 SB/小牌解掉当前敌控**（避免浪费一轮）？需要 12 局 V8 批跑统计"跟压场景下 Q0 兜底 PASS 比例"
- [ ] **观测**：本副最终 yf1 0 队胜利（dec#19 甩 5×8 炸、dec#20 出 SB 收官），本步 PASS **未导致 0 队输**
- [ ] **不要**：不要为这一副写特例（用户原话"禁止为单副写特例"，见 `工作流.md` §6）

---

## 8. 元信息

- **本报告写盘路径**：`docs/analysis/WF-12-20260716222448436062-副12-yf1-Q0让道决策分析.md`（已修订为 v2）
- **写盘规范修订**：v1 报告错误已记入今日 memory
- **下一步**：阿亮总如要继续深挖 Q0 跟压场景，请告知；否则本报告可视为本次 WF-12 任务的收尾
