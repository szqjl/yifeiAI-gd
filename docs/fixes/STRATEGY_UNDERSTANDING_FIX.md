# 策略理解率0%问题修复方案

## 问题分析

**问题现象：**
- 策略理解率：0.00%（动作预测和策略分类同时正确）
- 完全匹配准确率：0.00%（所有卡牌完全匹配）
- 卡牌级别准确率：97.38%（单个卡牌识别准确）
- 策略分类准确率：100.00%（策略类型识别准确）

**根本原因：**
1. 完全匹配准确率要求100%卡牌匹配，标准过于严格
2. 策略理解率 = 完全匹配 AND 策略正确，因为完全匹配为0%，所以策略理解率也是0%
3. 动作预测和策略学习之间存在一定冲突，没有联合优化

## 解决方案

### 1. 添加策略一致性损失

**目的：** 鼓励动作预测和策略分类在语义上一致

**实现：**
- 如果动作预测基本正确（卡牌匹配率>90%），策略分类也应该正确
- 权重：0.2

```python
# 策略一致性损失
if card_match_rate > 0.9:  # 动作基本正确
    strategy_consistency_loss = CrossEntropyLoss(strategy_logits, strategy_labels) * 0.2
```

### 2. 添加联合损失

**目的：** 直接鼓励动作和策略同时正确

**实现：**
- 使用卡牌匹配率作为权重，匹配率越高，策略分类损失权重越大
- 权重：0.3

```python
# 联合损失
strategy_ce_loss = CrossEntropyLoss(strategy_logits, strategy_labels, reduction='none')
weighted_loss = (strategy_ce_loss * (1.0 + card_match_rate * 2.0)).mean()
joint_loss = weighted_loss * 0.3
```

### 3. 改进策略理解率计算

**目的：** 使用更合理的匹配标准

**实现：**
- 原来：完全匹配（100%卡牌匹配）AND 策略正确
- 现在：卡牌匹配率>90% AND 策略正确

```python
# 改进的策略理解率
card_match_rate = (action_predictions == actions).float().mean(dim=1)
action_basically_correct = (card_match_rate > 0.9)  # 90%匹配率标准
strategy_understanding = action_basically_correct & strategy_correct
```

## 预期效果

### 改进前
- 策略理解率：0.00%
- 完全匹配准确率：0.00%

### 改进后（预期）
- 策略理解率：预计提升到 30-50%（基于卡牌准确率97.38%和策略准确率100%）
- 完全匹配准确率：可能仍较低（因为100%匹配标准严格）
- 但策略理解率会显著提升（因为使用90%匹配率标准）

## 训练建议

1. **重新训练模型**：使用改进后的损失函数
2. **监控指标**：
   - 策略理解率（90%匹配率标准）
   - 完全匹配准确率（100%匹配率标准，用于对比）
   - 卡牌级别准确率
   - 策略分类准确率
3. **调整权重**（如果需要）：
   - 策略一致性损失权重：0.2 → 0.3-0.5（如果策略理解率仍较低）
   - 联合损失权重：0.3 → 0.4-0.6（如果需要更强的联合优化）

## 代码修改位置

1. **损失函数**：`src/train/pretrain.py` 第1590-1662行
   - 添加策略一致性损失
   - 添加联合损失
   
2. **策略理解率计算**：`src/train/pretrain.py` 第1814-1840行
   - 使用90%匹配率标准

3. **总损失组合**：`src/train/pretrain.py` 第1700行左右
   - 添加策略一致性损失到总损失

## 验证方法

训练完成后，运行：
```bash
python evaluate_strategy_tasks.py
```

检查：
1. 策略理解率是否提升（应该>0%）
2. 6个策略任务的准确率是否保持或提升
3. 卡牌级别准确率是否保持（应该>95%）

