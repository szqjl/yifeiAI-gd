# YF硬编码优化实施指导 - 第一部分：项目理解

## 📋 给执行AI的重要说明

### 执行原则
1. **严格按照步骤顺序执行** - 不要跳跃，每个步骤都有依赖关系
2. **每个步骤完成后必须验证** - 运行测试确保功能正确
3. **遇到错误立即停止** - 不要继续执行，先解决问题
4. **保持向后兼容性** - 不要破坏现有的V4/V5功能
5. **所有修改都要备份** - 使用git commit保存每个阶段

### 项目背景理解
- **项目性质**: 掼蛋AI决策系统
- **当前版本**: V4（4层决策）和V5（3层决策）已实现
- **目标**: 创建V6版本，胜率提升至45-55%
- **核心理念**: 保持lalala的"简单直接"设计哲学

### 关键文件位置
```
src/decision/
├── decision_engine.py          # 基础决策引擎
├── hybrid_decision_engine_v4.py # V4混合决策引擎
├── hybrid_decision_engine_v5.py # V5混合决策引擎
├── endgame_strategy.py         # 残局策略（需要重构）
├── cooperation.py              # 队友配合（需要重构）
├── multi_factor_evaluator.py  # 多因素评估（需要重构）
└── card_type_handlers.py      # 牌型处理器

src/communication/
├── yf1_v4.py                   # V4客户端1
├── yf1_v5.py                   # V5客户端1
├── yf2_v4.py                   # V4客户端2
└── yf2_v5.py                   # V5客户端2

src/game_logic/
├── hand_combiner.py            # 手牌组合（需要增强）
└── enhanced_state.py           # 状态管理

config.yaml                     # 配置文件
```

### 当前架构问题
1. **复杂度过高** - 多层嵌套，难以维护
2. **缺乏阶段分离** - 主动/被动出牌逻辑混杂
3. **残局处理简单** - 只看牌数，缺少智能判断
4. **队友保护分散** - 没有统一的保护策略
5. **优先级固化** - 无法根据上下文调整

### 改进目标
1. **简化架构** - 清晰的阶段分离
2. **智能残局** - 多维度判断和多策略处理
3. **增强保护** - 多规则组合的队友保护
4. **动态优先级** - 根据上下文调整决策优先级
5. **完全可解释** - 每个决策都能追踪路径