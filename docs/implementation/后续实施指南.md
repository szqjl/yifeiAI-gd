# YF掼蛋优化后续实施指南

## 📋 当前状态

✅ **已完成**: 任务2 - 残局处理系统重构  
🔄 **进行中**: 准备任务3 - 队友保护策略系统重构  
⏳ **待完成**: 任务4-11

## 🎯 下一步详细实施步骤

### 任务3: 队友保护策略系统重构 (2-3小时)

#### 步骤1: 读取现有文件
```bash
# 查看当前的队友保护实现
cat src/decision/cooperation.py
```

#### 步骤2: 添加保护规则基类
在 `src/decision/cooperation.py` 文件开头添加：

```python
from abc import ABC, abstractmethod
from typing import Dict, List, Optional

class TeammateProtectionRule(ABC):
    """队友保护规则基类 - 需求2，属性6-10"""
    
    @abstractmethod
    def evaluate(self, message: dict, context: dict) -> float:
        """评估保护需求，返回0-1的分数"""
        pass
    
    @abstractmethod
    def get_name(self) -> str:
        """获取规则名称"""
        pass
```

#### 步骤3: 实现4个具体保护规则

```python
class HighValueProtectionRule(TeammateProtectionRule):
    """高牌值保护规则 - 需求2.1，属性6"""
    def evaluate(self, message: dict, context: dict) -> float:
        # 实现高牌值保护逻辑
        pass

class LowCardCountProtectionRule(TeammateProtectionRule):
    """低牌数保护规则 - 需求2.2，属性7"""
    def evaluate(self, message: dict, context: dict) -> float:
        # 实现低牌数保护逻辑
        pass

class CriticalStageProtectionRule(TeammateProtectionRule):
    """关键阶段保护规则 - 需求2.3，属性8"""
    def evaluate(self, message: dict, context: dict) -> float:
        # 实现关键阶段保护逻辑
        pass

class BombProtectionRule(TeammateProtectionRule):
    """炸弹保护规则 - 需求2.4，属性9"""
    def evaluate(self, message: dict, context: dict) -> float:
        # 实现炸弹保护逻辑
        pass
```

#### 步骤4: 创建保护策略管理器

```python
class TeammateProtectionStrategy:
    """队友保护策略管理器 - 需求2.5，属性10"""
    
    def __init__(self, config: dict = None):
        self.config = config or {}
        self.protection_rules = [
            BombProtectionRule(),
            HighValueProtectionRule(),
            LowCardCountProtectionRule(),
            CriticalStageProtectionRule(),
        ]
    
    def should_protect(self, message: dict) -> tuple[bool, dict]:
        """判断是否应该保护队友"""
        # 实现保护判断逻辑
        pass
```

#### 步骤5: 验证
```bash
# 语法检查
python -m py_compile src/decision/cooperation.py

# 功能测试
python -c "from src.decision.cooperation import TeammateProtectionStrategy; print('OK')"
```

### 任务4: 动态优先级系统 (3-4小时)

#### 步骤1: 读取现有文件
```bash
cat src/decision/multi_factor_evaluator.py
```

#### 步骤2: 添加上下文调整器基类

```python
from abc import ABC, abstractmethod

class ContextAdjuster(ABC):
    """上下文调整器基类 - 需求3，属性11-15"""
    
    @abstractmethod
    def adjust(self, scores: List[float], context: Dict, actions: List) -> List[float]:
        """根据上下文调整评分"""
        pass
    
    @abstractmethod
    def get_name(self) -> str:
        """获取调整器名称"""
        pass
```

#### 步骤3: 实现4个具体调整器

```python
class NextPlayerAdjuster(ContextAdjuster):
    """下家牌数调整器 - 需求3.1，属性11"""
    pass

class PassCountAdjuster(ContextAdjuster):
    """PASS次数调整器 - 需求3.2，属性12"""
    pass

class EndgameAdjuster(ContextAdjuster):
    """残局调整器 - 需求3.3，属性13"""
    pass

class TeammateAdjuster(ContextAdjuster):
    """队友状态调整器 - 需求3.4，属性14"""
    pass
```

#### 步骤4: 创建动态优先级系统

```python
class DynamicPrioritySystem:
    """动态优先级系统 - 需求3.5，属性15"""
    
    def __init__(self, config: Dict = None):
        self.config = config or {}
        self.context_adjusters = [
            TeammateAdjuster(),
            EndgameAdjuster(),
            NextPlayerAdjuster(),
            PassCountAdjuster(),
        ]
    
    def evaluate_with_context(self, action_list: List, base_scores: List[float], 
                            message: Dict) -> List[Tuple[int, float]]:
        """根据上下文动态调整优先级"""
        # 实现动态优先级逻辑
        pass
```

### 任务7: 创建V6客户端 (1-2小时)

#### 创建 yf1_v6.py

```python
# -*- coding: utf-8 -*-
"""
yf1_v6 - YiFei AI V6 Client (Player 0)
基于硬编码优化方案的增强版本
"""
import asyncio
import websockets
import json
import sys
import logging
from pathlib import Path
from datetime import datetime

# Add paths
sys.path.insert(0, str(Path(__file__).parent.parent))

from decision.decision_engine import DecisionEngine
from game_logic.enhanced_state import EnhancedGameStateManager
from communication.game_recorder import GameRecorder

# 配置日志
log_dir = Path(__file__).parent.parent.parent / "logs"
log_dir.mkdir(exist_ok=True)
log_filename = log_dir / f"yf1_v6_{datetime.now().strftime('%Y%m%d_%H%M%S')}.log"

logging.basicConfig(
    level=logging.INFO,
    format='[%(asctime)s] [%(name)s] [%(levelname)s] %(message)s',
    datefmt='%H:%M:%S',
    handlers=[
        logging.FileHandler(log_filename, encoding='utf-8'),
        logging.StreamHandler()
    ]
)

class YF1_V6_Client:
    """YiFei AI V6 Client - 基于硬编码优化方案"""
    
    def __init__(self, player_id=0):
        self.player_id = player_id
        self.user_info = "yf1_v6"
        self.logger = logging.getLogger(f"yf1_v6")
        
        # 使用增强的决策引擎
        state_manager = EnhancedGameStateManager()
        self.decision_engine = DecisionEngine(state_manager)
        
        # 游戏记录器
        self.game_recorder = GameRecorder(player_id, "yf1_v6")
        
        self.logger.info(f"✓ yf1_v6 initialized (Player {player_id})")
    
    # ... 其他方法实现
```

### 任务8-11: 测试和验证

#### 创建测试脚本

```python
# test_enhancements.py
#!/usr/bin/env python3
"""YF硬编码优化测试脚本"""

def test_endgame_strategy():
    """测试残局策略增强"""
    from src.decision.endgame_strategy import _is_endgame_enhanced
    
    test_message = {
        "handCards": ["H3", "S4", "D5"],
        "publicInfo": [
            {"rest": 15},
            {"rest": 8},
            {"rest": 5},
            {"rest": 12}
        ],
        "myPos": 0
    }
    
    is_endgame, endgame_type = _is_endgame_enhanced(test_message)
    assert is_endgame == True
    assert endgame_type in ['rush', 'defend', 'cooperate', 'control']
    print("✓ 残局策略测试通过")

def test_teammate_protection():
    """测试队友保护策略"""
    # 实现测试逻辑
    pass

def test_dynamic_priority():
    """测试动态优先级系统"""
    # 实现测试逻辑
    pass

if __name__ == "__main__":
    test_endgame_strategy()
    test_teammate_protection()
    test_dynamic_priority()
    print("🎉 所有测试通过！")
```

## 📊 验收标准检查清单

### 技术标准
- [ ] 所有代码无语法错误
- [ ] 所有单元测试通过
- [ ] 决策时间平均<200ms，最大<500ms
- [ ] 内存使用<100MB
- [ ] 100局测试无崩溃

### 功能标准
- [x] 残局识别准确率>95% (任务2已完成)
- [ ] 队友保护准确率>90% (任务3待完成)
- [ ] 优先级调整有效工作 (任务4待完成)
- [ ] 决策过程可完整追踪

### 胜率标准
- [ ] V6 vs lalala胜率>45%
- [ ] V6 vs V4胜率>52%
- [ ] V6 vs V5胜率>50%

## 🚨 注意事项

1. **保持向后兼容** - 不要删除现有函数
2. **每步都要测试** - 完成一个任务立即验证
3. **详细记录** - 记录每个步骤的结果
4. **遇到问题停止** - 不要在有错误时继续

## 📞 需要的工具

### Python环境
```bash
# 检查Python版本
python --version

# 安装依赖
pip install -r requirements.txt

# 安装测试框架
pip install hypothesis pytest
```

### 测试命令
```bash
# 语法检查
python -m py_compile src/decision/*.py

# 运行测试
python test_enhancements.py

# 运行属性测试
pytest test_properties.py
```

## 🎯 预期成果

完成所有任务后，你将得到：

1. ✅ **增强的残局处理系统** - 已完成
2. ⏳ **统一的队友保护系统** - 待完成
3. ⏳ **动态优先级系统** - 待完成
4. ⏳ **V6客户端** - 待完成
5. ⏳ **完整的测试套件** - 待完成
6. ⏳ **胜率提升验证** - 待完成

## 📈 进度追踪

- [x] 任务1: 项目准备 (已完成)
- [x] 任务2: 残局处理系统 (已完成)
- [ ] 任务3: 队友保护策略 (0%)
- [ ] 任务4: 动态优先级系统 (0%)
- [ ] 任务5: 第一次集成测试 (0%)
- [ ] 任务6: 手牌结构分析 (0%)
- [ ] 任务7: 创建V6客户端 (0%)
- [ ] 任务8: 性能测试 (0%)
- [ ] 任务9: 胜率验证 (0%)
- [ ] 任务10: 兼容性测试 (0%)
- [ ] 任务11: 最终验收 (0%)

**总体进度**: 18% (2/11任务完成)

---

**文档创建时间**: 2024-12-19  
**预计剩余时间**: 6-10小时  
**下一步**: 开始任务3 - 队友保护策略系统重构