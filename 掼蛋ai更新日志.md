# 掼蛋AI开发日志

## 日期: 2025-11-30
### 主题: 转型为自我学习型架构 (V5)

今天是掼蛋AI开发的一个重要里程碑。我们成功地从纯粹的基于规则的系统 (V4) 转型为**自我学习强化学习系统 (V5)**。这一转变使AI能够通过自我博弈和模仿来自主学习策略，而不再仅仅依赖于硬编码的逻辑。

### 1. 架构重构
我们将项目重构为五个关键组件，以支持自主学习：

*   **演武场 (The Playground - `src/rl_env`)**:
    *   创建了 `game_engine.py`: 一个高性能的本地游戏逻辑引擎。
    *   创建了 `guandan_env.py`: 一个兼容 Gymnasium 的环境包装器，允许标准的强化学习算法与游戏进行交互。

*   **大脑 (The Brain - `src/rl_agent`)**:
    *   实现了 `model.py`: 一个神经网络 (MLP)，接收游戏状态（手牌、牌桌、历史）作为输入，并输出108种可能出牌动作的概率。
    *   实现了 `agent.py`: 一个 **PPO (近端策略优化)** 代理，通过经验进行学习。

*   **导师 (The Teacher - `src/knowledge_processor`)**:
    *   实现了 `replay_parser.py`: 一个工具，用于将现有的 JSON 游戏日志解析为训练数据。
    *   实现了 `strategy_encoder.py`: 一个奖励塑造模块，将文本策略规则（例如“不要拆炸弹”）转化为数值奖励，以引导AI。

*   **道场 (The Dojo - `src/train`)**:
    *   **模仿学习**: 创建了 `pretrain.py` 以克隆人类/V4日志中的行为。我们成功在211个样本上训练了一个基准模型 (`bc_model_v1.pth`)。
    *   **自我博弈**: 创建了 `self_play.py` 让AI与自己对战。该循环生成数据、更新模型并保存检查点 (`ppo_model_v1.pth`)。
    *   **日志记录**: 添加了 `TRAINING_LOG.md` 以跟踪随时间变化的奖励和胜率。

*   **整合 (Integration - `src/decision`)**:
    *   创建了 `rl_decision_engine.py`: 一个桥梁，允许主游戏客户端 (`yf1_v4.py`) 使用训练好的 RL 模型进行决策。
    *   更新了 `yf1_v4.py`，支持在基于规则的引擎和 RL 引擎之间切换。

### 2. 主要成就
-   **自主学习循环**: AI 现在可以在本地与自己进行数百万次对局以进行提升。
-   **混合能力**: 客户端在 V5 RL 模型训练和成熟期间，仍然可以使用稳健的 V4 逻辑。
-   **知识整合**: 我们建立了一个管道，将人类知识（复盘和规则）输入到 RL 训练过程中。

### 3. 下一步计划
-   **训练**: 运行 `START_RL_TRAINING.bat` 一段较长时间（数天），让AI收敛到最优策略。
-   **优化**: 使用策略文档中更复杂的规则来改进 `strategy_encoder`。
-   **评估**: 定期测试 RL 代理对抗 V4 基于规则的代理，以衡量进步。

---
*由 Antigravity Agent 生成*

### 未来路线图：进军线上
我们制定了一份详细的计划 (`后期进行线上方案.md`)，旨在将AI推向更广阔的舞台：
*   **协议适配层 (PAL)**: 抽象通信接口以支持多平台（如腾讯、网页版）。
*   **拟人化行为**: 实现可变延迟和失误模拟，以便在与人类对战时通过“图灵测试”。
*   **在线学习管道**: 自动上传比赛日志到云端，实现持续的主动学习。
*   **扩展性**: 将代理容器化，实现大规模部署。
