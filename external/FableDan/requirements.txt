# Inference / evaluation / Botzone bot:
numpy>=1.24

# Training (PyTorch Q-network). Install the CUDA build matching your driver,
# e.g. `pip install torch --index-url https://download.pytorch.org/whl/cu124`.
# torch>=2.1
